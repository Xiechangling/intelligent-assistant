---
phase: 05-review-skills-workflow-polish
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/state/types.ts
  - src/app/state/appShellStore.ts
  - src/app/layout/BottomPanel.tsx
  - src/app/layout/CenterWorkspace.tsx
  - src/styles/app-shell.css
  - tests/e2e/review-flow.spec.ts
autonomous: true
requirements:
  - REVW-01
  - REVW-02
must_haves:
  truths:
    - User can inspect changed files from the active session's latest approved execution, not from a detached project-wide browser.
    - User can move between changed-file rows and see the matching diff preview without losing session context.
    - User sees explicit inline and tray-level messaging when review artifacts are unavailable, distinct from a true no-change state.
  artifacts:
    - path: src/app/state/types.ts
      provides: Review contracts covering selection, degraded-review state, and changed-file metadata
    - path: src/app/state/appShellStore.ts
      provides: Canonical review-ready state derivation and selected-file lifecycle tied to executionRecord
    - path: src/app/layout/BottomPanel.tsx
      provides: Split changed-file list and diff preview review surface in the bottom panel
    - path: src/app/layout/CenterWorkspace.tsx
      provides: Inline review-ready and degraded-review summaries in the active session surface
    - path: tests/e2e/review-flow.spec.ts
      provides: Automated review-surface regression coverage
  key_links:
    - from: src/app/state/appShellStore.ts
      to: src/app/layout/BottomPanel.tsx
      via: executionRecord + selectedReviewFileId + tray mode derivation
      pattern: executionRecord.*changedFiles|selectedReviewFileId|bottomPanelTab
    - from: src/app/state/appShellStore.ts
      to: src/app/layout/CenterWorkspace.tsx
      via: inline review-ready/degraded summary state
      pattern: changedFiles.length|review unavailable|Review ready
    - from: tests/e2e/review-flow.spec.ts
      to: src/app/layout/BottomPanel.tsx
      via: Playwright review selection and degraded-state assertions
      pattern: bottom-panel__review|Review unavailable
---

<objective>
Lock review behavior to the active coding session and latest approved execution so the bottom-panel review surface feels cohesive, trustworthy, and clearly connected to the center workspace.

Purpose: Fulfill D-01 through D-04 and D-11 through D-13 by making changed-file review feel like part of one desktop workflow instead of a detached diff utility.
Output: Stable review contracts, bottom-panel review cohesion, inline center-workspace review visibility, and regression coverage for review-ready/degraded states.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-review-skills-workflow-polish/05-CONTEXT.md
@.planning/phases/05-review-skills-workflow-polish/05-RESEARCH.md
@src/app/state/types.ts
@src/app/state/appShellStore.ts
@src/app/layout/BottomPanel.tsx
@src/app/layout/CenterWorkspace.tsx
@tests/e2e/review-flow.spec.ts

<interfaces>
From src/app/state/types.ts:
```typescript
export interface ChangedFileReview {
  id: string
  path: string
  summary: string
  diff: string
}

export interface ExecutionRecord {
  id: string
  proposalId: string
  summary: string
  command: string
  projectPath: string
  workingDirectory: string
  status: ExecutionStatus
  output: ExecutionOutputEntry[]
  changedFiles: ChangedFileReview[]
  startedAt: string | null
  completedAt: string | null
}
```

From src/app/state/appShellStore.ts:
```typescript
selectReviewFile: (fileId: string) => void
getDesktopTrayMode: () => DesktopTrayMode
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Stabilize execution-scoped review contracts and selection state</name>
  <read_first>
    - .planning/phases/05-review-skills-workflow-polish/05-CONTEXT.md
    - .planning/phases/05-review-skills-workflow-polish/05-RESEARCH.md
    - src/app/state/types.ts
    - src/app/state/appShellStore.ts
  </read_first>
  <files>src/app/state/types.ts, src/app/state/appShellStore.ts, tests/e2e/review-flow.spec.ts</files>
  <behavior>
    - Test review artifacts remain bound to the active session's latest approved execution per D-01.
    - Test first changed file auto-selects when review artifacts arrive and manual selection persists until the execution changes.
    - Test degraded review messaging stays distinct from the no-changed-files state per D-04.
  </behavior>
  <action>Update `src/app/state/types.ts` and `src/app/state/appShellStore.ts` so review state is explicitly execution-scoped per D-01, selection behavior is deterministic, and degraded-review status can be derived without conflating it with true zero-change output. Extend `tests/e2e/review-flow.spec.ts` first to cover auto-selection, file switching, and degraded review messaging before updating the store logic; do not introduce any repo-wide diff browser or project-level review cache.</action>
  <verify>
    <automated>npm run test:e2e:review</automated>
  </verify>
  <done>Store state cleanly differentiates review-ready, degraded-review, and no-review cases; selected diff behavior is deterministic and covered by Playwright.</done>
  <acceptance_criteria>
    - Review state is derived from `executionRecord` and latest approved execution only, per D-01.
    - Selecting a changed file updates the preview target and stays stable until a new execution replaces the review set.
    - Degraded review and no-changes use different state/copy paths rather than one shared empty-state branch.
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Polish bottom-panel review surface and center-workspace inline review summary</name>
  <read_first>
    - src/app/layout/BottomPanel.tsx
    - src/app/layout/CenterWorkspace.tsx
    - src/styles/app-shell.css
    - src/app/state/appShellStore.ts
  </read_first>
  <files>src/app/layout/BottomPanel.tsx, src/app/layout/CenterWorkspace.tsx, src/styles/app-shell.css, tests/e2e/review-flow.spec.ts</files>
  <behavior>
    - Test the bottom panel shows a split changed-file list and diff preview per D-02.
    - Test the center workspace exposes inline review-ready or degraded-review context per D-03.
    - Test review copy makes it clear whether artifacts are unavailable versus simply absent per D-04.
  </behavior>
  <action>Refine `src/app/layout/BottomPanel.tsx` into a clearer split review surface per D-02, improve changed-file row/diff typography cohesion in `src/styles/app-shell.css`, and update `src/app/layout/CenterWorkspace.tsx` so the active session shows inline review-ready or review-unavailable context per D-03. Keep review ownership in the bottom panel and center workspace only; do not move capability or settings controls into the center workspace, and do not present review as project-wide history.</action>
  <verify>
    <automated>npm run test:e2e:review && npm run test:e2e:status</automated>
  </verify>
  <done>The desktop session surface visibly signals review readiness inline, the bottom panel shows cohesive changed-file/diff behavior, and degraded review states remain explicit.</done>
  <acceptance_criteria>
    - The bottom panel retains a two-pane review layout with changed-file list and diff preview per D-02.
    - The center workspace shows review-ready or review-unavailable inline context per D-03 without becoming a settings surface.
    - Empty review, degraded review, and active review states have distinct copy and styling cues per D-04.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| center workspace → shared store | Session UI consumes derived review state and must not invent detached review context |
| shared store → bottom review tray | Untrusted execution output crosses into review-ready/degraded messaging |
| Playwright mocks → UI assertions | Test fixtures simulate execution artifacts and must reflect the canonical execution-bound review contract |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-01 | T | `src/app/state/appShellStore.ts` | mitigate | Derive review state from `executionRecord` only and reset selection on execution replacement to prevent stale or mixed review artifacts |
| T-05-02 | R | `src/app/layout/BottomPanel.tsx` | mitigate | Preserve explicit degraded-review messaging parsed from execution output so unavailable review is distinguishable from no changes |
| T-05-03 | I | `src/app/layout/CenterWorkspace.tsx` | accept | Inline summaries expose workflow status only, not secrets; keep credentials out of review UI and reuse existing session-safe metadata |
</threat_model>

<verification>
- `npm run test:e2e:review`
- `npm run test:e2e:status`
- `npm run build`
</verification>

<success_criteria>
- Review remains tied to the active session and latest approved execution.
- Changed-file selection and diff preview feel cohesive across center workspace and bottom panel.
- Users can tell the difference between review-ready, no changed files, and review unavailable states.
</success_criteria>

<output>
After completion, create `.planning/phases/05-review-skills-workflow-polish/05-review-skills-workflow-polish-01-SUMMARY.md`
</output>
