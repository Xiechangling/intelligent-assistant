---
phase: 05-review-skills-workflow-polish
plan: 03
type: execute
wave: 3
depends_on:
  - 05-01
  - 05-02
files_modified:
  - package.json
  - tests/e2e/review-flow.spec.ts
  - tests/e2e/presets.spec.ts
  - tests/e2e/capabilities.spec.ts
  - .planning/phases/05-review-skills-workflow-polish/05-HUMAN-UAT.md
autonomous: false
requirements:
  - REVW-01
  - REVW-02
  - CONF-03
  - CONF-04
must_haves:
  truths:
    - Review, presets, and capability toggles are covered by repeatable automated verification.
    - Manual checks clearly distinguish review-ready, degraded review, preset reuse, and capability ergonomics.
    - The polished workflow can be validated as one cohesive desktop experience across center workspace, bottom panel, and right panel.
  artifacts:
    - path: package.json
      provides: Stable commands for targeted and full Phase 5 verification
    - path: tests/e2e/review-flow.spec.ts
      provides: Finalized review regression assertions used in phase verification
    - path: tests/e2e/presets.spec.ts
      provides: Finalized preset regression assertions used in phase verification
    - path: tests/e2e/capabilities.spec.ts
      provides: Finalized capability-toggle regression assertions used in phase verification
    - path: .planning/phases/05-review-skills-workflow-polish/05-HUMAN-UAT.md
      provides: Manual verification checklist for review and settings polish
  key_links:
    - from: package.json
      to: Playwright specs
      via: phase-specific verification scripts
      pattern: test:e2e
    - from: .planning/phases/05-review-skills-workflow-polish/05-HUMAN-UAT.md
      to: implemented shell surfaces
      via: manual checklist steps spanning center workspace, bottom panel, and right panel
      pattern: Review ready|Review unavailable|Preset|Workflow capabilities
---

<objective>
Finish Phase 5 with explicit automated verification and human validation so review polish and right-panel ergonomics can be checked as one connected workflow before signoff.

Purpose: Satisfy the user's requirement for verification and manual checks, and make Phase 5 safe to verify without rediscovering expected behavior.
Output: Repeatable validation commands, completed e2e coverage footprint, and a manual checklist aligned to locked decisions.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-review-skills-workflow-polish/05-CONTEXT.md
@.planning/phases/05-review-skills-workflow-polish/05-RESEARCH.md
@.planning/phases/05-review-skills-workflow-polish/05-HUMAN-UAT.md
@package.json
@playwright.config.ts
@tests/e2e/review-flow.spec.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Consolidate Phase 5 automated verification entrypoints</name>
  <read_first>
    - package.json
    - playwright.config.ts
    - tests/e2e/review-flow.spec.ts
    - tests/e2e/presets.spec.ts
    - tests/e2e/capabilities.spec.ts
  </read_first>
  <files>package.json, tests/e2e/review-flow.spec.ts, tests/e2e/presets.spec.ts, tests/e2e/capabilities.spec.ts</files>
  <behavior>
    - Test a narrow review command exists and remains green for changed-file selection and degraded-review behavior.
    - Test preset and capability specs can be run directly and as part of a Phase 5 validation sweep.
    - Test the full Phase 5 verification command exercises review plus settings ergonomics without requiring ad hoc command assembly.
  </behavior>
  <action>Update `package.json` to expose clear Phase 5 verification commands that bundle the targeted Playwright specs introduced in Plans 01 and 02, then finalize any missing assertions in `tests/e2e/review-flow.spec.ts`, `tests/e2e/presets.spec.ts`, and `tests/e2e/capabilities.spec.ts` so they collectively cover the locked decisions for review cohesion, presets, and capability toggles. Keep the commands short and repeatable; do not add a new test framework or duplicate the existing Playwright harness.</action>
  <verify>
    <automated>npm run build && npm run test:e2e:review && npx playwright test tests/e2e/presets.spec.ts tests/e2e/capabilities.spec.ts</automated>
  </verify>
  <done>Phase 5 has explicit automated entrypoints for review, presets, capabilities, and full-shell polish verification.</done>
  <acceptance_criteria>
    - `package.json` exposes a repeatable command set for Phase 5 verification.
    - Review, preset, and capability regressions can be run without ad hoc CLI assembly.
    - The automated suite covers the behaviors required by REVW-01, REVW-02, CONF-03, and CONF-04.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Refresh manual UAT for review-degraded-state and settings ergonomics</name>
  <read_first>
    - .planning/phases/05-review-skills-workflow-polish/05-HUMAN-UAT.md
    - .planning/phases/05-review-skills-workflow-polish/05-CONTEXT.md
    - tests/e2e/review-flow.spec.ts
    - tests/e2e/presets.spec.ts
    - tests/e2e/capabilities.spec.ts
  </read_first>
  <files>.planning/phases/05-review-skills-workflow-polish/05-HUMAN-UAT.md</files>
  <action>Rewrite `05-HUMAN-UAT.md` into a concise manual checklist that mirrors the implemented workflow: changed-file review selection, diff cohesion, explicit degraded-review handling, preset save/apply ergonomics, and capability toggle clarity. Include exact user steps, expected UI copy/state, and note which checks verify D-01 through D-13; do not add out-of-scope collaboration, sync, marketplace, or admin-console checks.</action>
  <verify>
    <automated>python - <<'PY'
from pathlib import Path
path = Path(r'E:/work/ai/agent/.planning/phases/05-review-skills-workflow-polish/05-HUMAN-UAT.md')
text = path.read_text(encoding='utf-8')
required = ['Review ready', 'Review unavailable', 'Preset', 'Workflow capabilities']
missing = [item for item in required if item not in text]
if missing:
    raise SystemExit(f'Missing manual checklist anchors: {missing}')
print('manual checklist anchors present')
PY</automated>
  </verify>
  <done>The manual checklist gives a human exact steps to validate review polish and right-panel ergonomics as one cohesive workflow.</done>
  <acceptance_criteria>
    - Manual verification explicitly distinguishes review-ready, no-changes, and degraded-review states.
    - Manual verification covers preset save/apply behavior and confirms presets affect future defaults only.
    - Manual verification covers capability-toggle placement, labels, and enabled/disabled ergonomics in the right panel.
  </acceptance_criteria>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Human verify cohesive review and settings workflow</name>
  <read_first>
    - .planning/phases/05-review-skills-workflow-polish/05-HUMAN-UAT.md
    - package.json
  </read_first>
  <what-built>Phase 5 review cohesion, presets, and workflow-capability ergonomics with automated regression coverage.</what-built>
  <how-to-verify>
    1. Run `npm run build`.
    2. Run the targeted Phase 5 automated checks defined in `package.json`.
    3. Launch the app with the local dev workflow used by Playwright.
    4. Follow `E:/work/ai/agent/.planning/phases/05-review-skills-workflow-polish/05-HUMAN-UAT.md` end to end.
    5. Confirm review artifacts stay session-bound, degraded review is explicit, presets feel compact and future-facing, and capability toggles remain understandable in the right panel.
  </how-to-verify>
  <resume-signal>Type "approved" if the checklist passes, or reply with exact failures grouped by review, presets, or capabilities.</resume-signal>
  <acceptance_criteria>
    - Human verification confirms the shell feels cohesive across center workspace, bottom panel, and right panel.
    - Human verification finds no ambiguity between degraded review and no-change review states.
    - Human verification confirms presets and capability toggles feel like workflow polish rather than a detached admin area.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| verification scripts → application surfaces | Automated commands exercise the shipped shell behavior and must cover the intended review/settings states |
| manual checklist → human signoff | Human verification depends on precise expected outcomes to avoid false acceptance |
| package scripts → local environment | Validation commands run locally and should avoid unsafe hidden behavior or scope expansion |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-07 | R | `05-HUMAN-UAT.md` | mitigate | Document exact expected copy/state for review-ready, degraded review, presets, and capabilities so manual signoff is evidence-based |
| T-05-08 | T | `package.json` verification scripts | mitigate | Keep scripts pointing to explicit Playwright specs and build commands so validation does not silently skip critical review/settings coverage |
| T-05-09 | D | local Playwright verification | accept | Tests depend on local dev server startup, but existing Playwright config already provisions it; fallback remains the manual checklist if one narrow spec flakes |
</threat_model>

<verification>
- `npm run build`
- `npm run test:e2e:review`
- `npx playwright test tests/e2e/presets.spec.ts tests/e2e/capabilities.spec.ts`
- Manual checklist in `E:/work/ai/agent/.planning/phases/05-review-skills-workflow-polish/05-HUMAN-UAT.md`
</verification>

<success_criteria>
- Phase 5 has explicit automated and manual verification for review, presets, and workflow capabilities.
- A human can verify cohesive workflow polish without guessing expected copy or state transitions.
- The phase is ready for signoff after automated checks and one end-to-end manual pass.
</success_criteria>

<output>
After completion, create `.planning/phases/05-review-skills-workflow-polish/05-review-skills-workflow-polish-03-SUMMARY.md`
</output>
