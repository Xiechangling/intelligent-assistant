---
phase: 05-review-skills-workflow-polish
plan: 02
type: execute
wave: 2
depends_on:
  - 05-01
files_modified:
  - src/app/state/types.ts
  - src/app/state/appShellStore.ts
  - src/app/layout/RightPanel.tsx
  - src/styles/app-shell.css
  - tests/e2e/presets.spec.ts
  - tests/e2e/capabilities.spec.ts
autonomous: true
requirements:
  - CONF-03
  - CONF-04
must_haves:
  truths:
    - User can save compact presets from the current shell state and reuse them later.
    - Applying a preset changes future shell defaults without rewriting prior session history.
    - User can enable or disable workflow capabilities from the settings surface using understandable product-facing labels.
  artifacts:
    - path: src/app/state/types.ts
      provides: Preset and capability contracts that capture workflow-facing choices only
    - path: src/app/state/appShellStore.ts
      provides: Save/apply/toggle behaviors scoped to future defaults and lightweight capability state
    - path: src/app/layout/RightPanel.tsx
      provides: Ergonomic presets and workflow capability controls in the right-panel settings surface
    - path: tests/e2e/presets.spec.ts
      provides: Automated preset save/apply coverage
    - path: tests/e2e/capabilities.spec.ts
      provides: Automated capability-toggle coverage
  key_links:
    - from: src/app/layout/RightPanel.tsx
      to: src/app/state/appShellStore.ts
      via: savePreset/applyPreset/toggleSkill actions
      pattern: savePreset|applyPreset|toggleSkill
    - from: src/app/state/appShellStore.ts
      to: active session and shell defaults
      via: preset application updates only future-facing defaults
      pattern: globalDefaultModel|mode|bottomPanelTab
    - from: tests/e2e/presets.spec.ts
      to: src/app/layout/RightPanel.tsx
      via: save/apply flow assertions
      pattern: Presets|Save current|Active|Apply
---

<objective>
Make presets and capability toggles feel compact, understandable, and reusable from the right-panel settings surface without turning Phase 5 into a heavy admin/config subsystem.

Purpose: Fulfill D-05 through D-10 and D-11 through D-13 by polishing reusable shell defaults and lightweight workflow controls in the right place.
Output: Ergonomic preset save/apply flow, product-facing capability toggles, and dedicated automated coverage for both settings behaviors.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-review-skills-workflow-polish/05-CONTEXT.md
@.planning/phases/05-review-skills-workflow-polish/05-RESEARCH.md
@src/app/state/types.ts
@src/app/state/appShellStore.ts
@src/app/layout/RightPanel.tsx
@tests/e2e/review-flow.spec.ts

<interfaces>
From src/app/state/types.ts:
```typescript
export interface ReviewPreset {
  id: string
  name: string
  mode: AppMode
  modelId: ModelId
  openReviewByDefault: boolean
}

export interface SkillToggle {
  id: string
  label: string
  enabled: boolean
}
```

From src/app/state/appShellStore.ts:
```typescript
savePreset: (name: string) => void
applyPreset: (presetId: string) => void
toggleSkill: (skillId: string) => void
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add preset regression coverage and future-default-only application rules</name>
  <read_first>
    - .planning/phases/05-review-skills-workflow-polish/05-CONTEXT.md
    - src/app/state/types.ts
    - src/app/state/appShellStore.ts
    - src/app/layout/RightPanel.tsx
  </read_first>
  <files>tests/e2e/presets.spec.ts, src/app/state/types.ts, src/app/state/appShellStore.ts, src/app/layout/RightPanel.tsx</files>
  <behavior>
    - Test saving a preset captures compact workflow-facing shell choices per D-05 and D-06.
    - Test applying a preset updates future defaults and shell ergonomics without rewriting prior session history per D-07.
    - Test preset affordances remain in the right panel rather than moving into the center workspace per D-12.
  </behavior>
  <action>Create `tests/e2e/presets.spec.ts` first, then refine `src/app/state/types.ts`, `src/app/state/appShellStore.ts`, and `src/app/layout/RightPanel.tsx` so presets remain compact shell-state snapshots per D-05/D-06 and applying them only changes future-facing defaults per D-07. Keep preset payloads free of credentials, transcript data, or historical session mutation; do not introduce a heavy schema editor, versioned config system, or project-sync behavior.</action>
  <verify>
    <automated>npx playwright test tests/e2e/presets.spec.ts</automated>
  </verify>
  <done>Presets can be saved and applied from the right panel, capture meaningful workflow choices, and leave prior session history unchanged.</done>
  <acceptance_criteria>
    - Presets store mode, model, and review preference state from the existing shell state per D-06.
    - Applying a preset changes default behavior for future interactions only and does not rewrite transcript/session history per D-07.
    - Preset save/apply controls stay in the right-panel settings surface per D-12.
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add capability-toggle coverage and polish product-facing settings ergonomics</name>
  <read_first>
    - src/app/layout/RightPanel.tsx
    - src/app/state/appShellStore.ts
    - src/app/state/types.ts
    - src/styles/app-shell.css
  </read_first>
  <files>tests/e2e/capabilities.spec.ts, src/app/layout/RightPanel.tsx, src/app/state/appShellStore.ts, src/app/state/types.ts, src/styles/app-shell.css</files>
  <behavior>
    - Test workflow capabilities can be toggled on/off from the right-panel settings surface per D-08 and D-09.
    - Test labels remain product-facing and understandable rather than internal jargon per D-10.
    - Test settings presentation remains adjacent to presets and connection settings as one cohesive workflow-support area per D-11.
  </behavior>
  <action>Create `tests/e2e/capabilities.spec.ts` first, then polish `src/app/layout/RightPanel.tsx`, `src/app/state/appShellStore.ts`, `src/app/state/types.ts`, and `src/styles/app-shell.css` so capability toggles are lightweight, understandable, and visually cohesive with presets per D-08 through D-11. Keep capability controls in the right panel only per D-09, keep labels product-facing per D-10, and avoid any plugin marketplace, shared-skills manager, or implementation-jargon copy.</action>
  <verify>
    <automated>npx playwright test tests/e2e/capabilities.spec.ts</automated>
  </verify>
  <done>The right panel provides concise, product-facing capability toggles with automated coverage for state changes and placement.</done>
  <acceptance_criteria>
    - Capabilities are enable/disable toggles in the settings/support surface per D-08 and D-09.
    - Labels and helper copy are tool-like and product-facing rather than internal module names per D-10.
    - Presets, connection settings, and capability toggles feel like one cohesive support surface per D-11 and D-12.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| settings UI → shared store | User-triggered preset/toggle actions update shell defaults and must not mutate protected session history |
| shared store → session metadata | Preset application must not overwrite prior transcript/model history |
| settings UI → credential service context | Settings surface sits near secure credential controls and must not leak secrets into presets |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-04 | I | `src/app/state/appShellStore.ts` preset handling | mitigate | Restrict presets to workflow-facing shell fields only and explicitly exclude API key/base URL/session transcript data |
| T-05-05 | T | `src/app/state/appShellStore.ts` applyPreset | mitigate | Scope preset application to `mode`, `globalDefaultModel`, and review preference defaults so existing session history is not rewritten |
| T-05-06 | R | `src/app/layout/RightPanel.tsx` capability settings | mitigate | Keep capability labels product-facing and add Playwright assertions for placement and visible enabled/disabled state to prevent ambiguous settings drift |
</threat_model>

<verification>
- `npx playwright test tests/e2e/presets.spec.ts`
- `npx playwright test tests/e2e/capabilities.spec.ts`
- `npm run build`
</verification>

<success_criteria>
- Users can save and reuse compact presets derived from the current shell state.
- Applying presets changes future defaults without mutating current or historical session identity.
- Workflow capabilities are easy to understand and configure from the right panel.
</success_criteria>

<output>
After completion, create `.planning/phases/05-review-skills-workflow-polish/05-review-skills-workflow-polish-02-SUMMARY.md`
</output>
