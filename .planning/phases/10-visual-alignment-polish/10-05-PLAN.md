---
phase: 10-visual-alignment-polish
plan: 05
type: execute
wave: 3
depends_on:
  - 10-01
  - 10-02
  - 10-03
  - 10-04
files_modified:
  - public/panda-mascot.svg
  - src/app/components/EmptyState/EmptyState.tsx
  - src/app/components/EmptyState/EmptyState.module.css
  - src/app/layout/CenterWorkspace.tsx
autonomous: true
requirements:
  - VISUAL-04

must_haves:
  truths:
    - "用户在无会话时看到大熊猫吉祥物"
    - "用户看到欢迎文案"
    - "空状态设计简洁友好"
  artifacts:
    - path: "public/panda-mascot.svg"
      provides: "大熊猫吉祥物 SVG 图标"
      min_lines: 10
    - path: "src/app/components/EmptyState/EmptyState.tsx"
      provides: "空状态组件"
      exports: ["EmptyState"]
      min_lines: 30
    - path: "src/app/components/EmptyState/EmptyState.module.css"
      provides: "空状态样式"
      min_lines: 20
  key_links:
    - from: "CenterWorkspace.tsx"
      to: "EmptyState"
      via: "条件渲染"
      pattern: "transcript.length === 0.*EmptyState"
---

<objective>
创建大熊猫吉祥物空状态设计，替换现有的空状态文案，提供友好的欢迎体验。

Purpose: 提升空状态的视觉吸引力和品牌亲和力，对齐官方设计语言
Output: 大熊猫吉祥物 + 欢迎文案的空状态组件
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@src/app/layout/CenterWorkspace.tsx
</context>

<interfaces>
<!-- 当前空状态实现（从 CenterWorkspace.tsx 提取） -->

From CenterWorkspace.tsx (lines 171-180):
```typescript
if (events.length === 0) {
  return (
    <div className="conversation-empty">
      <h3>{mode === 'project' ? 'Start a coding session' : 'Start a conversation'}</h3>
      <p>
        {mode === 'project'
          ? 'Describe the task for this workspace to begin an attached coding session.'
          : 'Send a message without opening a workspace.'}
      </p>
    </div>
  )
}
```

需要替换为带有大熊猫吉祥物的 EmptyState 组件。
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: 创建大熊猫吉祥物 SVG</name>
  <files>public/panda-mascot.svg</files>
  <action>
创建简洁的大熊猫吉祥物 SVG 图标（线性风格，对齐 lucide-react 设计语言）：

```svg
<svg width="120" height="120" viewBox="0 0 120 120" fill="none" xmlns="http://www.w3.org/2000/svg">
  <!-- 熊猫头部 -->
  <circle cx="60" cy="60" r="35" fill="currentColor" opacity="0.1" stroke="currentColor" stroke-width="2"/>
  
  <!-- 左耳 -->
  <circle cx="40" cy="35" r="12" fill="currentColor" opacity="0.8"/>
  
  <!-- 右耳 -->
  <circle cx="80" cy="35" r="12" fill="currentColor" opacity="0.8"/>
  
  <!-- 左眼 -->
  <ellipse cx="48" cy="55" rx="8" ry="10" fill="currentColor" opacity="0.8"/>
  <circle cx="48" cy="55" r="3" fill="currentColor"/>
  
  <!-- 右眼 -->
  <ellipse cx="72" cy="55" rx="8" ry="10" fill="currentColor" opacity="0.8"/>
  <circle cx="72" cy="55" r="3" fill="currentColor"/>
  
  <!-- 鼻子 -->
  <ellipse cx="60" cy="68" rx="6" ry="4" fill="currentColor" opacity="0.6"/>
  
  <!-- 嘴巴（微笑） -->
  <path d="M 50 72 Q 60 78 70 72" stroke="currentColor" stroke-width="2" fill="none" opacity="0.6" stroke-linecap="round"/>
</svg>
```

这个 SVG 使用 `currentColor`，可以自动适配主题颜色。
  </action>
  <verify>
    <automated>test -f public/panda-mascot.svg && echo "SVG exists"</automated>
  </verify>
  <done>大熊猫吉祥物 SVG 已创建</done>
</task>

<task type="auto">
  <name>Task 2: 创建 EmptyState 组件</name>
  <files>
    src/app/components/EmptyState/EmptyState.tsx
    src/app/components/EmptyState/EmptyState.module.css
  </files>
  <action>
创建 EmptyState 组件和样式：

**EmptyState.tsx:**
```typescript
import React from 'react'
import styles from './EmptyState.module.css'

interface EmptyStateProps {
  mode: 'project' | 'conversation'
}

export function EmptyState({ mode }: EmptyStateProps) {
  const title = mode === 'project' 
    ? 'Start a coding session' 
    : 'Start a conversation'
  
  const description = mode === 'project'
    ? 'Describe the task for this workspace to begin an attached coding session.'
    : 'Send a message without opening a workspace.'

  return (
    <div className={styles.emptyState}>
      <div className={styles.mascot}>
        <img src="/panda-mascot.svg" alt="Panda mascot" />
      </div>
      <h3 className={styles.title}>{title}</h3>
      <p className={styles.description}>{description}</p>
    </div>
  )
}
```

**EmptyState.module.css:**
```css
.emptyState {
  display: flex;
  flex-direction: column;
  align-items: center;
  justify-content: center;
  padding: var(--space-6, 48px);
  text-align: center;
  min-height: 400px;
}

.mascot {
  width: 120px;
  height: 120px;
  margin-bottom: var(--space-4, 32px);
  color: var(--text-tertiary);
  opacity: 0.8;
  transition: opacity 200ms ease-out, transform 200ms ease-out;
}

.mascot:hover {
  opacity: 1;
  transform: scale(1.05);
}

.mascot img {
  width: 100%;
  height: 100%;
}

.title {
  font-size: 20px;
  font-weight: 600;
  color: var(--text-primary);
  margin: 0 0 var(--space-2, 16px) 0;
}

.description {
  font-size: 14px;
  color: var(--text-secondary);
  max-width: 400px;
  margin: 0;
  line-height: 1.6;
}
```
  </action>
  <verify>
    <automated>test -f src/app/components/EmptyState/EmptyState.tsx && test -f src/app/components/EmptyState/EmptyState.module.css && echo "EmptyState component created"</automated>
  </verify>
  <done>EmptyState 组件已创建</done>
</task>

<task type="auto">
  <name>Task 3: 集成 EmptyState 到 CenterWorkspace</name>
  <files>src/app/layout/CenterWorkspace.tsx</files>
  <action>
在 CenterWorkspace.tsx 中替换现有的空状态为 EmptyState 组件：

**1. 添加 import:**
```typescript
import { EmptyState } from '../components/EmptyState'
```

**2. 替换 Transcript 函数中的空状态逻辑（lines 171-180）:**
```typescript
function Transcript({
  events,
  mode,
  assistantStatus,
  currentStageLabel,
}: {
  events: SessionTranscriptEvent[]
  mode: 'project' | 'conversation'
  assistantStatus: 'idle' | 'streaming' | 'error'
  currentStageLabel: string | null
}) {
  if (events.length === 0) {
    return <EmptyState mode={mode} />
  }

  return (
    <div className="conversation-transcript" role="log" aria-live="polite">
      {/* 现有代码保持不变 */}
    </div>
  )
}
```

移除旧的 `conversation-empty` 类名和内联 JSX。
  </action>
  <verify>
    <automated>grep -n "EmptyState" src/app/layout/CenterWorkspace.tsx</automated>
  </verify>
  <done>EmptyState 已集成到 CenterWorkspace</done>
</task>

<task type="auto">
  <name>Task 4: 提交空状态设计更改</name>
  <files>
    public/panda-mascot.svg
    src/app/components/EmptyState/EmptyState.tsx
    src/app/components/EmptyState/EmptyState.module.css
    src/app/layout/CenterWorkspace.tsx
  </files>
  <action>
提交空状态设计更改：
```bash
git add public/panda-mascot.svg \
        src/app/components/EmptyState/EmptyState.tsx \
        src/app/components/EmptyState/EmptyState.module.css \
        src/app/layout/CenterWorkspace.tsx
git commit -m "feat(10-05): add panda mascot empty state design"
```
  </action>
  <verify>
    <automated>git log --oneline -1 | grep "10-05"</automated>
  </verify>
  <done>空状态设计更改已提交到 git</done>
</task>

</tasks>

<verification>
1. 启动应用，创建新会话（无消息）
2. 检查是否显示大熊猫吉祥物
3. 检查欢迎文案是否正确显示
4. 测试 hover 动画是否流畅
5. 在浅色和深色主题下验证显示效果
</verification>

<success_criteria>
- 无会话时显示大熊猫吉祥物
- 欢迎文案清晰友好
- 吉祥物支持 hover 动画
- 在两种主题下显示正常
- 对齐官方 Claude Code Desktop 空状态设计
</success_criteria>

<output>
After completion, create `.planning/phases/10-visual-alignment-polish/10-05-SUMMARY.md`
</output>
