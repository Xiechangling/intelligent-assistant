---
phase: 10-visual-alignment-polish
plan: 03
type: execute
wave: 2
depends_on:
  - 10-01
files_modified:
  - src/styles/app-shell.css
  - src/app/components/CodeBlock/CodeBlock.module.css
  - src/app/components/Composer/Composer.module.css
  - src/app/components/CustomizeMenu/CustomizeMenu.module.css
  - src/app/components/GlobalSearch/GlobalSearch.module.css
  - src/app/components/MarkdownRenderer/MarkdownRenderer.module.css
autonomous: true
requirements:
  - VISUAL-03

must_haves:
  truths:
    - "所有消息卡片使用 24px 圆角"
    - "所有模态框使用 24px 圆角"
    - "圆角值统一，视觉一致"
  artifacts:
    - path: "src/styles/app-shell.css"
      provides: "更新的圆角 CSS 变量"
      contains: "--radius-card: 24px"
    - path: "src/app/components/CodeBlock/CodeBlock.module.css"
      provides: "24px 圆角代码块"
      contains: "border-radius: 24px"
  key_links:
    - from: "组件 CSS Modules"
      to: "app-shell.css"
      via: "CSS 变量引用"
      pattern: "var\\(--radius-"
---

<objective>
统一所有卡片和消息组件使用 24px 圆角，对齐官方设计语言。

Purpose: 建立统一的圆角规范，提升视觉柔和度和现代感
Output: 所有卡片组件使用 24px 圆角
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@src/styles/app-shell.css
@src/app/components/CodeBlock/CodeBlock.module.css
@src/app/components/Composer/Composer.module.css
@src/app/components/CustomizeMenu/CustomizeMenu.module.css
@src/app/components/GlobalSearch/GlobalSearch.module.css
@src/app/components/MarkdownRenderer/MarkdownRenderer.module.css
</context>

<tasks>

<task type="auto">
  <name>Task 1: 更新圆角 CSS 变量</name>
  <files>src/styles/app-shell.css</files>
  <action>
在 `:root` 选择器中添加新的圆角变量，并更新现有变量：

```css
:root {
  /* 现有变量保持 */
  --radius-sm: 8px;    /* 小圆角 - 按钮、输入框 */
  --radius-md: 12px;   /* 中圆角 - 小卡片 */
  --radius-lg: 16px;   /* 大圆角 - 面板 */
  --radius-card: 24px; /* 卡片圆角 - 消息、模态框 */
  --radius-xl: 24px;   /* 超大圆角 - 主要卡片 */
}
```

这样保持向后兼容，同时提供新的 24px 圆角变量。
  </action>
  <verify>
    <automated>grep "radius-card: 24px" src/styles/app-shell.css</automated>
  </verify>
  <done>圆角 CSS 变量已更新，添加 --radius-card: 24px</done>
</task>

<task type="auto">
  <name>Task 2: 应用 24px 圆角到组件</name>
  <files>
    src/app/components/CodeBlock/CodeBlock.module.css
    src/app/components/Composer/Composer.module.css
    src/app/components/CustomizeMenu/CustomizeMenu.module.css
    src/app/components/GlobalSearch/GlobalSearch.module.css
    src/app/components/MarkdownRenderer/MarkdownRenderer.module.css
  </files>
  <action>
更新以下组件的 border-radius 为 24px 或 var(--radius-card)：

**1. CodeBlock.module.css** - 代码块卡片：
```css
.codeBlock {
  border-radius: 24px; /* 从 6px 改为 24px */
}
```

**2. Composer.module.css** - 输入框容器：
```css
.composer {
  border-radius: 24px; /* 从 6px 改为 24px */
}

/* 保持小元素使用小圆角 */
.attachmentChip {
  border-radius: 8px; /* 保持 8px */
}

.modelSelector {
  border-radius: 8px; /* 保持 6px 或改为 8px */
}
```

**3. CustomizeMenu.module.css** - 自定义菜单：
```css
.customizeMenu {
  border-radius: 24px; /* 从 12px 改为 24px */
}
```

**4. GlobalSearch.module.css** - 搜索模态框：
```css
.searchModal {
  border-radius: 24px; /* 从 12px 改为 24px */
}
```

**5. MarkdownRenderer.module.css** - Markdown 内容卡片：
```css
.markdownContent blockquote,
.markdownContent pre {
  border-radius: 24px; /* 从 6px 改为 24px */
}
```

**原则：**
- 主要卡片/容器 → 24px
- 小元素（按钮、标签、输入框）→ 保持 6-8px
- 中等元素（工具栏、面板）→ 12-16px
  </action>
  <verify>
    <automated>grep -r "border-radius.*24px" src/app/components --include="*.css" | wc -l</automated>
  </verify>
  <done>所有主要卡片组件已应用 24px 圆角</done>
</task>

<task type="auto">
  <name>Task 3: 提交圆角统一更改</name>
  <files>
    src/styles/app-shell.css
    src/app/components/CodeBlock/CodeBlock.module.css
    src/app/components/Composer/Composer.module.css
    src/app/components/CustomizeMenu/CustomizeMenu.module.css
    src/app/components/GlobalSearch/GlobalSearch.module.css
    src/app/components/MarkdownRenderer/MarkdownRenderer.module.css
  </files>
  <action>
提交圆角统一更改：
```bash
git add src/styles/app-shell.css \
        src/app/components/CodeBlock/CodeBlock.module.css \
        src/app/components/Composer/Composer.module.css \
        src/app/components/CustomizeMenu/CustomizeMenu.module.css \
        src/app/components/GlobalSearch/GlobalSearch.module.css \
        src/app/components/MarkdownRenderer/MarkdownRenderer.module.css
git commit -m "feat(10-03): unify card border-radius to 24px"
```
  </action>
  <verify>
    <automated>git log --oneline -1 | grep "10-03"</automated>
  </verify>
  <done>圆角统一更改已提交到 git</done>
</task>

</tasks>

<verification>
1. 检查所有消息卡片是否使用 24px 圆角
2. 检查模态框是否使用 24px 圆角
3. 检查小元素（按钮、标签）是否保持小圆角
4. 视觉检查圆角是否统一协调
</verification>

<success_criteria>
- 主要卡片组件使用 24px 圆角
- 小元素保持 6-8px 圆角
- 视觉上圆角统一协调
- 对齐官方 Claude Code Desktop 圆角规范
</success_criteria>

<output>
After completion, create `.planning/phases/10-visual-alignment-polish/10-03-SUMMARY.md`
</output>
