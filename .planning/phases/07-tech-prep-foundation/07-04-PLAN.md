---
phase: 07-tech-prep-foundation
plan: 04
type: execute
wave: 2
depends_on: [07-02]
files_modified:
  - src/components/ModeTabs/ModeTabs.tsx
  - src/components/ModeTabs/ModeTabs.module.css
  - src/components/ModeTabs/index.ts
autonomous: true
requirements: [TECH-03, TECH-04]

must_haves:
  truths:
    - "用户可以看到三个模式标签（Chat/Cowork/Code）"
    - "用户点击标签可以切换模式"
    - "当前激活的标签有视觉区分"
    - "标签状态与 appShellStore 同步"
  artifacts:
    - path: "src/components/ModeTabs/ModeTabs.tsx"
      provides: "模式切换标签组件"
      exports: ["ModeTabs"]
      min_lines: 30
    - path: "src/components/ModeTabs/ModeTabs.module.css"
      provides: "组件样式（CSS Modules）"
      contains: ".tabs"
    - path: "src/components/ModeTabs/index.ts"
      provides: "组件导出"
      exports: ["ModeTabs"]
  key_links:
    - from: "src/components/ModeTabs/ModeTabs.tsx"
      to: "src/state/appShellStore.ts"
      via: "useAppShellStore hook"
      pattern: "useAppShellStore.*currentMode"
    - from: "src/components/ModeTabs/ModeTabs.tsx"
      to: "src/components/ModeTabs/ModeTabs.module.css"
      via: "CSS Modules import"
      pattern: "import styles from"
---

<objective>
创建 ModeTabs 组件，实现 Chat/Cowork/Code 三模式切换 UI。

Purpose: 为 Phase 8 顶栏重构提供模式切换组件，使用 CSS Modules（TECH-04）
Output: 完整的 ModeTabs 组件，连接到 appShellStore 状态管理
</objective>

<execution_context>
@C:\Users\chris\.claude\get-shit-done\workflows\execute-plan.md
@C:\Users\chris\.claude\get-shit-done\templates\summary.md
</execution_context>

<context>
@E:\work\ai\agent\.planning\PROJECT.md
@E:\work\ai\agent\.planning\ROADMAP.md
@E:\work\ai\agent\.planning\STATE.md
@E:\work\ai\agent\.planning\phases\07-tech-prep-foundation\07-RESEARCH.md
@E:\work\ai\agent\src\state\appShellStore.ts
</context>

<interfaces>
<!-- 从 appShellStore 提取的状态接口 -->

```typescript
// appShellStore 中的模式状态（来自 07-02-PLAN）
type Mode = 'chat' | 'search' | 'navigate';

interface AppShellState {
  currentMode: Mode;
  setMode: (mode: Mode) => void;
}

// ModeTabs 组件接口
interface ModeTabsProps {
  // 无 props，直接从 store 读取状态
}
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: 创建 ModeTabs 组件</name>
  <files>
    src/components/ModeTabs/ModeTabs.tsx,
    src/components/ModeTabs/ModeTabs.module.css,
    src/components/ModeTabs/index.ts
  </files>
  <action>
    创建 ModeTabs 组件目录和文件：
    
    **ModeTabs.tsx:**
    - 导入 useAppShellStore
    - 读取 currentMode 和 setMode
    - 渲染三个按钮：Chat, Cowork, Code
    - 每个按钮对应一个模式值：'chat', 'search', 'navigate'
    - 当前激活的按钮应用 .tabActive 样式
    - 点击按钮调用 setMode(mode)
    - 添加 aria-label 和 role="tablist" 提升可访问性
    
    **ModeTabs.module.css:**
    - 使用研究报告中的 CSS Module with Variables 示例
    - 定义 .container（flex 布局，gap, background, border-radius, padding）
    - 定义 .tab（基础样式，hover 状态，focus-visible 状态）
    - 定义 .tabActive（composes: tab，激活状态样式）
    - 使用 CSS Variables：var(--spacing-*), var(--bg-*), var(--text-*), var(--accent-primary), var(--radius-*)
    - 不要定义新的 CSS Variables，只引用现有的
    
    **index.ts:**
    - 导出 ModeTabs 组件
    
    遵循 TECH-04 要求：使用 CSS Modules + CSS Variables。
  </action>
  <verify>
    <automated>npm run type-check</automated>
  </verify>
  <done>
    - ModeTabs.tsx 存在且导出 ModeTabs 组件
    - ModeTabs.module.css 存在且使用 CSS Variables
    - index.ts 导出组件
    - 组件连接到 appShellStore（读取 currentMode，调用 setMode）
    - TypeScript 编译通过
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 用户点击 → 状态变更 | 用户交互触发模式切换 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-07 | Tampering | 模式状态 | accept | 低风险：本地状态，用户控制 |
</threat_model>

<verification>
- 运行 `npm run type-check` 确认无 TypeScript 错误
- 检查 ModeTabs.tsx 导入 useAppShellStore
- 检查 ModeTabs.module.css 使用 CSS Variables（不定义新变量）
- 确认 index.ts 导出组件
</verification>

<success_criteria>
- ModeTabs 组件已创建，包含 .tsx, .module.css, index.ts
- 组件连接到 appShellStore
- 组件使用 CSS Modules（TECH-04）
- 三个模式标签已实现（Chat/Cowork/Code）
- TypeScript 编译通过
</success_criteria>

<output>
After completion, create `.planning/phases/07-tech-prep-foundation/07-04-SUMMARY.md`
</output>
