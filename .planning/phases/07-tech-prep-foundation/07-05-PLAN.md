---
phase: 07-tech-prep-foundation
plan: 05
type: execute
wave: 2
depends_on: [07-02]
files_modified:
  - src/components/AttachmentList/AttachmentList.tsx
  - src/components/AttachmentList/AttachmentList.module.css
  - src/components/AttachmentList/index.ts
  - src/services/attachmentService.ts
autonomous: true
requirements: [TECH-02, TECH-04]

must_haves:
  truths:
    - "用户可以看到附件列表"
    - "用户可以删除附件"
    - "附件显示文件名和类型图标"
  artifacts:
    - path: "src/components/AttachmentList/AttachmentList.tsx"
      provides: "附件列表组件"
      exports: ["AttachmentList"]
      min_lines: 30
    - path: "src/components/AttachmentList/AttachmentList.module.css"
      provides: "组件样式（CSS Modules）"
      contains: ".list"
    - path: "src/components/AttachmentList/index.ts"
      provides: "组件导出"
      exports: ["AttachmentList"]
    - path: "src/services/attachmentService.ts"
      provides: "扩展的附件服务（drag-drop 支持）"
      exports: ["setupDragDrop", "useDragDrop"]
  key_links:
    - from: "src/components/AttachmentList/AttachmentList.tsx"
      to: "src/state/appShellStore.ts"
      via: "useAppShellStore hook"
      pattern: "useAppShellStore.*attachments"
    - from: "src/services/attachmentService.ts"
      to: "@tauri-apps/api/event"
      via: "Tauri drag-drop API"
      pattern: "listen.*tauri://drag-drop"
---

<objective>
创建 AttachmentList 组件和扩展 attachmentService，实现文件附件显示和 drag-drop 支持。

Purpose: 实现 TECH-02 文件附件处理，使用 CSS Modules（TECH-04）
Output: AttachmentList 组件 + 扩展的 attachmentService（包含 drag-drop hook）
</objective>

<execution_context>
@C:\Users\chris\.claude\get-shit-done\workflows\execute-plan.md
@C:\Users\chris\.claude\get-shit-done\templates\summary.md
</execution_context>

<context>
@E:\work\ai\agent\.planning\PROJECT.md
@E:\work\ai\agent\.planning\ROADMAP.md
@E:\work\ai\agent\.planning\STATE.md
@E:\work\ai\agent\.planning\phases\07-tech-prep-foundation\07-RESEARCH.md
@E:\work\ai\agent\src\state\appShellStore.ts
@E:\work\ai\agent\src\services\attachmentService.ts
</context>

<interfaces>
<!-- 从 appShellStore 和研究报告提取的接口 -->

```typescript
// Attachment 类型（来自 07-02-PLAN）
interface Attachment {
  id: string;
  path: string;
  name: string;
  type: 'file' | 'image';
}

// appShellStore 中的附件状态
interface AppShellState {
  attachments: Attachment[];
  addAttachment: (file: Attachment) => void;
  removeAttachment: (id: string) => void;
  clearAttachments: () => void;
}

// useDragDrop hook（来自 07-RESEARCH.md Pattern 2）
interface DragDropFile {
  path: string;
  name: string;
  size: number;
}

function useDragDrop(onFilesDropped: (files: DragDropFile[]) => void): {
  isDragging: boolean;
};
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: 扩展 attachmentService 添加 drag-drop 支持</name>
  <files>src/services/attachmentService.ts</files>
  <action>
    扩展现有的 attachmentService.ts：
    
    1. 添加 useDragDrop hook（使用研究报告中的 Pattern 2: Tauri Drag-Drop Integration）
       - 导入 @tauri-apps/api/event 的 listen 和 UnlistenFn
       - 监听 'tauri://drag-drop' 事件
       - 监听 'tauri://drag-enter' 和 'tauri://drag-leave' 事件（用于 isDragging 状态）
       - 返回 { isDragging } 状态
       - 在 useEffect cleanup 中调用 unlisten
    
    2. 添加文件验证函数（可选，为后续使用准备）
       - validateFileType(path: string): boolean
       - 检查文件扩展名是否在允许列表中
    
    保持现有的 attachmentService 功能不变，只添加新功能。
  </action>
  <verify>
    <automated>npm run type-check</automated>
  </verify>
  <done>
    - attachmentService.ts 包含 useDragDrop hook
    - useDragDrop 监听 Tauri drag-drop 事件
    - TypeScript 编译通过
  </done>
</task>

<task type="auto">
  <name>Task 2: 创建 AttachmentList 组件</name>
  <files>
    src/components/AttachmentList/AttachmentList.tsx,
    src/components/AttachmentList/AttachmentList.module.css,
    src/components/AttachmentList/index.ts
  </files>
  <action>
    创建 AttachmentList 组件目录和文件：
    
    **AttachmentList.tsx:**
    - 导入 useAppShellStore
    - 读取 attachments 和 removeAttachment
    - 渲染附件列表（如果为空则不显示）
    - 每个附件显示：文件名、类型图标（File 或 Image from lucide-react）、删除按钮（X icon）
    - 点击删除按钮调用 removeAttachment(id)
    - 添加 aria-label 提升可访问性
    
    **AttachmentList.module.css:**
    - 使用 CSS Variables（不定义新变量）
    - 定义 .list（flex 或 grid 布局）
    - 定义 .item（附件项样式）
    - 定义 .deleteButton（删除按钮样式，hover 状态）
    - 使用 var(--spacing-*), var(--bg-*), var(--text-*), var(--radius-*)
    
    **index.ts:**
    - 导出 AttachmentList 组件
    
    遵循 TECH-04 要求：使用 CSS Modules。
  </action>
  <verify>
    <automated>npm run type-check</automated>
  </verify>
  <done>
    - AttachmentList.tsx 存在且导出 AttachmentList 组件
    - AttachmentList.module.css 存在且使用 CSS Variables
    - index.ts 导出组件
    - 组件连接到 appShellStore（读取 attachments，调用 removeAttachment）
    - TypeScript 编译通过
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| OS 文件系统 → 应用 | 用户拖拽文件到应用 |
| 文件路径 → 显示 | 文件路径在 UI 中显示 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-08 | Tampering | 文件路径 | mitigate | 使用 Tauri path 模块规范化路径，防止路径遍历 |
| T-07-09 | Denial of Service | 大文件拖拽 | accept | Phase 7 不实现文件大小限制，Phase 9 添加 100MB 软限制 |
| T-07-10 | Tampering | 文件类型伪装 | accept | Phase 7 不实现 MIME 验证，Phase 9 添加文件类型白名单 |
</threat_model>

<verification>
- 运行 `npm run type-check` 确认无 TypeScript 错误
- 检查 attachmentService.ts 包含 useDragDrop hook
- 检查 AttachmentList.tsx 连接到 appShellStore
- 检查 AttachmentList.module.css 使用 CSS Variables
</verification>

<success_criteria>
- attachmentService.ts 包含 useDragDrop hook
- AttachmentList 组件已创建，包含 .tsx, .module.css, index.ts
- 组件连接到 appShellStore
- 组件使用 CSS Modules（TECH-04）
- TypeScript 编译通过
</success_criteria>

<output>
After completion, create `.planning/phases/07-tech-prep-foundation/07-05-SUMMARY.md`
</output>
