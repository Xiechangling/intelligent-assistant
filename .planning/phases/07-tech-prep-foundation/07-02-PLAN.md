---
phase: 07-tech-prep-foundation
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/state/appShellStore.ts
autonomous: true
requirements: [TECH-03]

must_haves:
  truths:
    - "应用可以存储当前模式（chat/search/navigate）"
    - "应用可以存储语音输入激活状态"
    - "应用可以存储附件列表"
    - "旧的审批/审查状态已从 store 中移除"
  artifacts:
    - path: "src/state/appShellStore.ts"
      provides: "调整后的状态管理结构"
      exports: ["useAppShellStore"]
      min_lines: 50
  key_links:
    - from: "src/state/appShellStore.ts"
      to: "localStorage"
      via: "Zustand persist middleware"
      pattern: "persist.*app-shell-storage"
---

<objective>
调整 Zustand store 结构，移除审批/审查状态，新增导航/搜索/语音状态。

Purpose: 为新 UI 组件提供状态管理基础，同时清理旧功能的状态
Output: 更新的 appShellStore.ts，包含向后兼容的迁移逻辑
</objective>

<execution_context>
@C:\Users\chris\.claude\get-shit-done\workflows\execute-plan.md
@C:\Users\chris\.claude\get-shit-done\templates\summary.md
</execution_context>

<context>
@E:\work\ai\agent\.planning\PROJECT.md
@E:\work\ai\agent\.planning\ROADMAP.md
@E:\work\ai\agent\.planning\STATE.md
@E:\work\ai\agent\.planning\phases\07-tech-prep-foundation\07-RESEARCH.md
@E:\work\ai\agent\src\state\appShellStore.ts
</context>

<interfaces>
<!-- 从研究报告提取的目标状态结构 -->

```typescript
// 目标状态接口（来自 07-RESEARCH.md）
type Mode = 'chat' | 'search' | 'navigate';

interface Attachment {
  id: string;
  path: string;
  name: string;
  type: 'file' | 'image';
}

interface AppShellState {
  // 新增：导航状态
  currentMode: Mode;
  setMode: (mode: Mode) => void;

  // 新增：语音输入状态
  voiceInputActive: boolean;
  toggleVoiceInput: () => void;

  // 新增：附件状态
  attachments: Attachment[];
  addAttachment: (file: Attachment) => void;
  removeAttachment: (id: string) => void;
  clearAttachments: () => void;
}
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: 调整 appShellStore 状态结构</name>
  <files>src/state/appShellStore.ts</files>
  <action>
    修改 appShellStore.ts：
    
    1. 新增状态字段：
       - currentMode: 'chat' | 'search' | 'navigate' (默认 'chat')
       - voiceInputActive: boolean (默认 false)
       - attachments: Attachment[] (默认 [])
    
    2. 新增 actions：
       - setMode(mode)
       - toggleVoiceInput()
       - addAttachment(file)
       - removeAttachment(id)
       - clearAttachments()
    
    3. 移除旧状态（如果存在）：
       - pendingProposal
       - executionRecord
       - 任何与审批/审查相关的字段
    
    4. 更新 persist 配置：
       - 将 version 增加到 2
       - 添加 migrate 函数，移除旧的审批/审查状态
    
    使用研究报告中的代码示例作为参考（Pattern 3: Zustand Store Refactoring）。
    
    保持现有的其他状态字段不变（如主题、窗口位置等）。
  </action>
  <verify>
    <automated>npm run type-check</automated>
  </verify>
  <done>
    - appShellStore.ts 包含新的状态字段和 actions
    - TypeScript 编译通过
    - persist 配置包含 version 2 和 migrate 函数
    - 旧的审批/审查状态已移除
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| localStorage → app state | 持久化状态从浏览器存储加载到应用 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-03 | Tampering | localStorage | accept | 低风险：本地桌面应用，用户控制存储 |
| T-07-04 | Information Disclosure | 附件路径 | accept | 路径存储在本地，不传输到外部 |
</threat_model>

<verification>
- 运行 `npm run type-check` 确认 TypeScript 无错误
- 检查 appShellStore.ts 包含所有新状态字段
- 确认 migrate 函数存在且正确处理版本升级
</verification>

<success_criteria>
- appShellStore.ts 包含 currentMode, voiceInputActive, attachments 状态
- 所有新 actions 已实现
- persist 配置版本为 2，包含 migrate 函数
- TypeScript 编译通过
</success_criteria>

<output>
After completion, create `.planning/phases/07-tech-prep-foundation/07-02-SUMMARY.md`
</output>
