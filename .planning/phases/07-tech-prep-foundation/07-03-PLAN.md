---
phase: 07-tech-prep-foundation
plan: 03
type: execute
wave: 2
depends_on: [07-01]
files_modified:
  - src/components/VoiceInput/VoiceInput.tsx
  - src/components/VoiceInput/VoiceInput.module.css
  - src/components/VoiceInput/index.ts
autonomous: true
requirements: [TECH-01, TECH-04]

must_haves:
  truths:
    - "用户可以看到语音输入按钮"
    - "用户点击按钮可以启动/停止语音识别"
    - "不支持的浏览器显示降级提示"
    - "语音识别激活时按钮状态改变"
  artifacts:
    - path: "src/components/VoiceInput/VoiceInput.tsx"
      provides: "语音输入组件"
      exports: ["VoiceInput"]
      min_lines: 40
    - path: "src/components/VoiceInput/VoiceInput.module.css"
      provides: "组件样式（CSS Modules）"
      contains: ".button"
    - path: "src/components/VoiceInput/index.ts"
      provides: "组件导出"
      exports: ["VoiceInput"]
  key_links:
    - from: "src/components/VoiceInput/VoiceInput.tsx"
      to: "react-speech-recognition"
      via: "useSpeechRecognition hook"
      pattern: "import.*useSpeechRecognition"
    - from: "src/components/VoiceInput/VoiceInput.tsx"
      to: "src/components/VoiceInput/VoiceInput.module.css"
      via: "CSS Modules import"
      pattern: "import styles from"
---

<objective>
创建 VoiceInput 组件，集成 Web Speech API，提供语音输入功能。

Purpose: 实现 TECH-01 语音输入支持，使用 CSS Modules 统一样式方法论（TECH-04）
Output: 完整的 VoiceInput 组件，包含 TypeScript 组件、CSS Modules 样式、导出文件
</objective>

<execution_context>
@C:\Users\chris\.claude\get-shit-done\workflows\execute-plan.md
@C:\Users\chris\.claude\get-shit-done\templates\summary.md
</execution_context>

<context>
@E:\work\ai\agent\.planning\PROJECT.md
@E:\work\ai\agent\.planning\ROADMAP.md
@E:\work\ai\agent\.planning\STATE.md
@E:\work\ai\agent\.planning\phases\07-tech-prep-foundation\07-RESEARCH.md
</context>

<interfaces>
<!-- 从研究报告提取的组件接口 -->

```typescript
// VoiceInput 组件接口（来自 07-RESEARCH.md Code Examples）
interface VoiceInputProps {
  onTranscript: (text: string) => void;
}

// react-speech-recognition hooks
import { useSpeechRecognition } from 'react-speech-recognition';
import SpeechRecognition from 'react-speech-recognition';

// lucide-react icons
import { Mic, MicOff } from 'lucide-react';
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: 创建 VoiceInput 组件</name>
  <files>
    src/components/VoiceInput/VoiceInput.tsx,
    src/components/VoiceInput/VoiceInput.module.css,
    src/components/VoiceInput/index.ts
  </files>
  <action>
    创建 VoiceInput 组件目录和文件：
    
    **VoiceInput.tsx:**
    - 使用研究报告中的代码示例（Voice Input Component TECH-01）
    - 导入 useSpeechRecognition 和 SpeechRecognition
    - 导入 Mic 和 MicOff 图标（lucide-react）
    - 实现 props: onTranscript(text: string)
    - 检测浏览器兼容性（browserSupportsSpeechRecognition）
    - 不支持时显示降级提示
    - 支持时显示按钮，点击切换 listening 状态
    - listening 为 true 时显示 MicOff 图标，false 时显示 Mic 图标
    - 停止录音时调用 onTranscript(transcript)
    
    **VoiceInput.module.css:**
    - 使用 CSS Variables（不要定义新变量，只引用现有的）
    - 定义 .button, .buttonActive, .unsupported 样式
    - 参考研究报告中的 CSS Module with Variables 示例
    - 使用 var(--spacing-*), var(--bg-*), var(--text-*), var(--accent-primary)
    - 添加 hover 和 focus-visible 状态
    
    **index.ts:**
    - 导出 VoiceInput 组件
    
    遵循 TECH-04 要求：所有新组件必须使用 CSS Modules，不使用全局 CSS。
  </action>
  <verify>
    <automated>npm run type-check</automated>
  </verify>
  <done>
    - VoiceInput.tsx 存在且导出 VoiceInput 组件
    - VoiceInput.module.css 存在且使用 CSS Variables
    - index.ts 导出组件
    - TypeScript 编译通过
    - 组件可以被导入（import { VoiceInput } from '@/components/VoiceInput'）
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 浏览器麦克风权限 → 应用 | 用户授权麦克风访问 |
| Web Speech API → 应用 | 语音转文本结果传入应用 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-05 | Information Disclosure | 麦克风权限 | mitigate | 浏览器原生权限提示，用户明确授权 |
| T-07-06 | Tampering | 语音转录内容 | mitigate | React 自动转义，transcript 作为纯文本渲染 |
</threat_model>

<verification>
- 运行 `npm run type-check` 确认无 TypeScript 错误
- 检查 VoiceInput.tsx 导入 react-speech-recognition
- 检查 VoiceInput.module.css 使用 CSS Variables（不定义新变量）
- 确认 index.ts 导出组件
</verification>

<success_criteria>
- VoiceInput 组件已创建，包含 .tsx, .module.css, index.ts
- 组件使用 useSpeechRecognition hook
- 组件使用 CSS Modules（TECH-04）
- 浏览器兼容性检测已实现
- TypeScript 编译通过
</success_criteria>

<output>
After completion, create `.planning/phases/07-tech-prep-foundation/07-03-SUMMARY.md`
</output>
