---
phase: 04-safe-execution-visibility
plan: 03
type: execute
wave: 3
depends_on:
  - 01
  - 02
files_modified:
  - tests/e2e/approval-flow.spec.ts
  - tests/e2e/review-flow.spec.ts
  - tests/e2e/status-system.spec.ts
  - playwright.config.ts
  - .planning/phases/04-safe-execution-visibility/04-HUMAN-UAT.md
autonomous: false
requirements:
  - EXEC-01
  - EXEC-02
  - EXEC-03
must_haves:
  truths:
    - The full Phase 4 workflow can be proven through automated checks for approval, status alignment, and review visibility.
    - A human can run one desktop verification pass that confirms approve, reject, success, failure, and project-boundary safety behavior in the real Tauri shell.
    - Verification artifacts make it obvious whether the phase is complete or which trust-critical path still fails.
  artifacts:
    - path: tests/e2e/approval-flow.spec.ts
      provides: Approval visibility and reject/approve flow coverage
      contains: "Awaiting approval"
    - path: tests/e2e/status-system.spec.ts
      provides: Canonical workflow vocabulary coverage across shell states
      contains: "canonical status labels"
    - path: tests/e2e/review-flow.spec.ts
      provides: Review-ready and output/tray visibility coverage
      contains: "review-ready tray"
    - path: .planning/phases/04-safe-execution-visibility/04-HUMAN-UAT.md
      provides: Manual Tauri verification checklist for trust-critical flows
      contains: "approve"
  key_links:
    - from: tests/e2e/approval-flow.spec.ts
      to: src/app/layout/BottomPanel.tsx
      via: exact approval copy and context-field assertions
      pattern: "Approve and run|Reject command|Workspace path|Working directory"
    - from: tests/e2e/status-system.spec.ts
      to: src/app/state/appShellStore.ts
      via: canonical workflow status assertions
      pattern: "Ready|Working|Awaiting approval|Review ready|Failed"
    - from: .planning/phases/04-safe-execution-visibility/04-HUMAN-UAT.md
      to: npm run tauri:dev
      via: manual desktop validation steps
      pattern: "tauri:dev"
---

<objective>
Close Phase 4 with executable verification coverage and one focused manual desktop check so approval, execution visibility, and project-boundary safety are provable rather than assumed.

Purpose: Trust-critical workflow work is incomplete until the exact approve/reject/run/fail paths are exercised both automatically and in the real desktop shell.
Output: Tight automated verification plus a human verification checklist for native-runtime behavior.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-safe-execution-visibility/04-CONTEXT.md
@.planning/phases/04-safe-execution-visibility/04-RESEARCH.md
@.planning/phases/04-safe-execution-visibility/04-HUMAN-UAT.md
@tests/e2e/approval-flow.spec.ts
@tests/e2e/review-flow.spec.ts
@tests/e2e/status-system.spec.ts
@playwright.config.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Complete automated Phase 4 verification coverage</name>
  <read_first>
    - tests/e2e/approval-flow.spec.ts
    - tests/e2e/review-flow.spec.ts
    - tests/e2e/status-system.spec.ts
    - playwright.config.ts
    - .planning/phases/04-safe-execution-visibility/04-RESEARCH.md
  </read_first>
  <files>tests/e2e/approval-flow.spec.ts, tests/e2e/review-flow.spec.ts, tests/e2e/status-system.spec.ts, playwright.config.ts, package.json</files>
  <action>Expand the existing Playwright coverage so Phase 4 explicitly proves the trust-critical paths called out in research: approve path, reject path, failed execution path, review-ready path, degraded review/unavailable path, and exact workflow vocabulary per D-01 through D-13. Adjust `playwright.config.ts` only if needed to make the approval/output/status suite stable and fast in the current web-shell harness. If the repo scripts need a narrow verification entrypoint for this phase, add it in `package.json`; otherwise reuse existing commands. Keep the tests assertion-heavy on visible user outcomes rather than implementation internals.</action>
  <verify>
    <automated>npm run test:e2e:approval && npm run test:e2e:review && npm run test:e2e:status && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - Automated tests cover approve, reject, failure, review-ready, and degraded-review visibility.
    - Canonical workflow labels remain protected against copy drift across shell surfaces.
    - Phase-specific verification can run repeatably in under the normal e2e workflow without manual setup changes.
  </acceptance_criteria>
  <done>Automated Phase 4 verification covers all trust-critical visible behaviors and passes reliably.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Run one native desktop trust verification pass</name>
  <read_first>
    - .planning/phases/04-safe-execution-visibility/04-HUMAN-UAT.md
    - .planning/phases/04-safe-execution-visibility/04-CONTEXT.md
    - .planning/phases/04-safe-execution-visibility/04-RESEARCH.md
  </read_first>
  <files>.planning/phases/04-safe-execution-visibility/04-HUMAN-UAT.md</files>
  <action>Update `.planning/phases/04-safe-execution-visibility/04-HUMAN-UAT.md` into a concrete native-runtime checklist that mirrors the automated proof points from Task 1 and Phase 4 decisions D-01 through D-13. Keep it focused on trust-critical manual checks only: approval inline + tray alignment, reject-path continuity, labeled output visibility, failed execution visibility, out-of-project working-directory rejection, and degraded review messaging. Do not ask the human to perform setup Claude could automate; this checkpoint is only for final native-shell verification after automated checks already pass.</action>
  <what-built>Automated approval, output, and status coverage plus the completed Phase 4 UI/native workflow.</what-built>
  <how-to-verify>
    1. Run `npm run tauri:dev` from `E:/work/ai/agent`.
    2. In project mode, trigger a proposal requiring approval and confirm the center workspace stays conversation-first while the bottom tray auto-expands.
    3. Confirm the approval surface shows summary, exact command, workspace path, and working directory together.
    4. Reject once and verify the session stays attached and clearly indicates rejection/ready-to-continue state.
    5. Approve a safe command once and verify output appears in labeled `system`/`stdout`/`stderr` lines in the bottom tray.
    6. Trigger a failing command and verify the session/timeline visibly enters failed or needs-attention state without losing context.
    7. Attempt a command with an out-of-project working directory using the dev/test harness and verify execution is blocked with explicit safety messaging.
    8. If the workspace is non-git or review artifacts are unavailable, confirm the tray shows explicit degraded review messaging rather than a silent empty success.
  </how-to-verify>
  <resume-signal>Type "approved" if all checks pass, or list the failing steps and observed behavior.</resume-signal>
  <verify>
    <automated>npm run tauri:dev</automated>
  </verify>
  <acceptance_criteria>
    - Native desktop runtime matches the automated web-shell workflow on approval, output, and status behaviors.
    - Reject/fail/safety-block paths remain understandable and do not strand the user outside the active session.
    - Manual results are captured back into the phase UAT artifact for follow-up if any step fails.
  </acceptance_criteria>
  <done>Human verification confirms the real Tauri shell preserves the trust guarantees proven by automated checks, or produces a concrete issue list for replanning.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| mocked browser harness → real Tauri desktop runtime | Web-shell automation can diverge from the actual native execution environment |
| automated assertions → human trust decision | Passing tests still require one desktop confirmation for the native shell behavior |
| UAT checklist → phase completion | Missing manual evidence can hide native-only safety regressions |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-08 | Repudiation | Phase verification evidence | mitigate | Expand Playwright coverage for every trust-critical user-visible path and record manual Tauri verification in `04-HUMAN-UAT.md`. |
| T-04-09 | Tampering | workflow copy/status regressions | mitigate | Keep assertion-heavy tests for exact approval labels and canonical status vocabulary across approval/output/review states. |
| T-04-10 | Elevation of privilege | native-only safety regression | mitigate | Include a manual desktop check for out-of-project working-directory rejection because browser mocks cannot fully prove native command-boundary behavior. |
</threat_model>

<verification>
- `npm run test:e2e:approval`
- `npm run test:e2e:review`
- `npm run test:e2e:status`
- `npm run build`
- Manual: `npm run tauri:dev` with `04-HUMAN-UAT.md`
</verification>

<success_criteria>
- Automated checks cover all visible Phase 4 trust-critical paths.
- A human can verify the same behaviors in the real Tauri shell using a single checklist.
- Any remaining discrepancy is captured as a concrete failing step rather than vague feedback.
</success_criteria>

<output>
After completion, create `.planning/phases/04-safe-execution-visibility/04-safe-execution-visibility-03-SUMMARY.md`
</output>
