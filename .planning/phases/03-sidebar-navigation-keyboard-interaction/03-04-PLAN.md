---
phase: 03-sidebar-navigation-keyboard-interaction
plan: 04
type: execute
wave: 2
depends_on: [03-01, 03-02, 03-03]
files_modified:
  - tests/e2e/keyboard-navigation.spec.ts
autonomous: true
requirements: [REQ-03, REQ-08, REQ-10]

must_haves:
  truths:
    - "E2E 测试验证左侧栏导航化"
    - "E2E 测试验证全局快捷键"
    - "E2E 测试验证输入历史导航"
  artifacts:
    - path: "tests/e2e/keyboard-navigation.spec.ts"
      provides: "键盘导航 E2E 测试"
      min_lines: 100
  key_links:
    - from: "tests/e2e/keyboard-navigation.spec.ts"
      to: "Playwright test runner"
      via: "E2E 测试执行"
      pattern: "test\\(|expect\\("
---

<objective>
创建键盘导航 E2E 测试，验证左侧栏导航化、全局快捷键和输入历史导航功能。

Purpose: 确保 Phase 3 所有功能正常工作，防止回归
Output: keyboard-navigation.spec.ts E2E 测试套件
</objective>

<execution_context>
@C:\Users\chris\.claude\get-shit-done\workflows\execute-plan.md
@C:\Users\chris\.claude\get-shit-done\templates\summary.md
</execution_context>

<context>
@E:\work\ai\agent\.planning\PROJECT.md
@E:\work\ai\agent\.planning\ROADMAP.md
@E:\work\ai\agent\.planning\STATE.md
@E:\work\ai\agent\tests\e2e\startup.spec.ts
@E:\work\ai\agent\tests\e2e\approval-flow.spec.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: 创建键盘导航 E2E 测试</name>
  <files>tests/e2e/keyboard-navigation.spec.ts</files>
  <action>
创建新文件 tests/e2e/keyboard-navigation.spec.ts：

```typescript
import { test, expect } from '@playwright/test'

test.describe('Keyboard Navigation', () => {
  test.beforeEach(async ({ page }) => {
    await page.goto('http://localhost:1420')
    await page.waitForSelector('.app-shell', { timeout: 10000 })
  })

  test.describe('Sidebar Navigation', () => {
    test('sidebar has no brand area', async ({ page }) => {
      // 验证品牌区已移除
      const brandArea = page.locator('.sidebar__section--brand')
      await expect(brandArea).toHaveCount(0)
    })

    test('sidebar has no expand/collapse buttons', async ({ page }) => {
      // 验证无展开/折叠按钮
      const toggleButtons = page.locator('.sidebar__section-toggle')
      await expect(toggleButtons).toHaveCount(0)
    })

    test('sidebar shows project picker at top', async ({ page }) => {
      // 验证项目选择器在左侧栏顶部
      const projectPicker = page.locator('.sidebar__project-picker')
      await expect(projectPicker).toBeVisible()
      
      const projectButton = page.locator('.sidebar__project-button')
      await expect(projectButton).toBeVisible()
      await expect(projectButton).toContainText('Open workspace')
    })

    test('sidebar always shows project and session lists', async ({ page }) => {
      // 验证项目列表和会话历史始终可见
      const projectList = page.locator('.sidebar__list').first()
      const sessionList = page.locator('.sidebar__list').last()
      
      // 列表应该存在（即使为空）
      await expect(projectList).toBeAttached()
      await expect(sessionList).toBeAttached()
    })

    test('topbar has no project breadcrumb', async ({ page }) => {
      // 验证顶栏无项目面包屑
      const breadcrumb = page.locator('.toolbar__breadcrumb')
      await expect(breadcrumb).toHaveCount(0)
    })

    test('topbar only has model selector and settings', async ({ page }) => {
      // 验证顶栏仅有模型选择器和设置按钮
      const modelSelect = page.locator('.toolbar__model-select')
      const settingsButton = page.locator('.toolbar__icon-button')
      
      await expect(modelSelect).toBeVisible()
      await expect(settingsButton).toBeVisible()
    })
  })

  test.describe('Global Keybindings', () => {
    test('ctrl+t creates new session', async ({ page }) => {
      // 记录初始会话数
      const initialSessions = await page.locator('.sidebar__list .sidebar__item').count()
      
      // 按 ctrl+t
      await page.keyboard.press('Control+t')
      
      // 等待新会话创建
      await page.waitForTimeout(500)
      
      // 验证会话数增加
      const newSessions = await page.locator('.sidebar__list .sidebar__item').count()
      expect(newSessions).toBeGreaterThan(initialSessions)
    })

    test('ctrl+e opens settings panel', async ({ page }) => {
      // 确保右侧面板初始关闭
      const rightPanel = page.locator('.panel')
      await expect(rightPanel).not.toBeVisible()
      
      // 按 ctrl+e
      await page.keyboard.press('Control+e')
      
      // 验证设置面板打开
      await expect(rightPanel).toBeVisible()
      
      // 验证显示设置内容
      const settingsTitle = page.locator('.panel__section-title')
      await expect(settingsTitle).toContainText(/settings|keyboard/i)
    })

    test('keybindings can be disabled in settings', async ({ page }) => {
      // 打开设置
      await page.keyboard.press('Control+e')
      
      // 找到快捷键启用复选框
      const keybindingsCheckbox = page.locator('input[type="checkbox"]').filter({ hasText: /enable.*keyboard/i })
      await expect(keybindingsCheckbox).toBeChecked()
      
      // 禁用快捷键
      await keybindingsCheckbox.uncheck()
      
      // 关闭设置面板
      await page.keyboard.press('Escape')
      
      // 验证 ctrl+e 不再工作
      await page.keyboard.press('Control+e')
      await page.waitForTimeout(300)
      const rightPanel = page.locator('.panel')
      await expect(rightPanel).not.toBeVisible()
    })
  })

  test.describe('Input History Navigation', () => {
    test('up arrow loads previous input', async ({ page }) => {
      // 找到输入框
      const input = page.locator('.workspace__input, textarea[placeholder*="message"]')
      await expect(input).toBeVisible()
      
      // 输入并提交第一条消息
      await input.fill('First message')
      await page.keyboard.press('Enter')
      await page.waitForTimeout(500)
      
      // 输入并提交第二条消息
      await input.fill('Second message')
      await page.keyboard.press('Enter')
      await page.waitForTimeout(500)
      
      // 清空输入框
      await input.fill('')
      
      // 按 ↑ 键
      await input.focus()
      await page.keyboard.press('ArrowUp')
      
      // 验证加载了上一条消息
      await expect(input).toHaveValue('Second message')
      
      // 再按 ↑ 键
      await page.keyboard.press('ArrowUp')
      
      // 验证加载了更早的消息
      await expect(input).toHaveValue('First message')
    })

    test('down arrow navigates forward in history', async ({ page }) => {
      const input = page.locator('.workspace__input, textarea[placeholder*="message"]')
      
      // 输入并提交两条消息
      await input.fill('Message 1')
      await page.keyboard.press('Enter')
      await page.waitForTimeout(500)
      
      await input.fill('Message 2')
      await page.keyboard.press('Enter')
      await page.waitForTimeout(500)
      
      // 清空并导航到历史
      await input.fill('')
      await input.focus()
      await page.keyboard.press('ArrowUp') // Message 2
      await page.keyboard.press('ArrowUp') // Message 1
      
      // 按 ↓ 键返回
      await page.keyboard.press('ArrowDown')
      await expect(input).toHaveValue('Message 2')
      
      // 再按 ↓ 键回到空白
      await page.keyboard.press('ArrowDown')
      await expect(input).toHaveValue('')
    })

    test('history persists across page reload', async ({ page }) => {
      const input = page.locator('.workspace__input, textarea[placeholder*="message"]')
      
      // 输入并提交消息
      await input.fill('Persistent message')
      await page.keyboard.press('Enter')
      await page.waitForTimeout(500)
      
      // 刷新页面
      await page.reload()
      await page.waitForSelector('.app-shell', { timeout: 10000 })
      
      // 验证历史记录仍然存在
      const inputAfterReload = page.locator('.workspace__input, textarea[placeholder*="message"]')
      await inputAfterReload.focus()
      await page.keyboard.press('ArrowUp')
      
      await expect(inputAfterReload).toHaveValue('Persistent message')
    })
  })
})
```

关键点：
- 使用现有测试的模式（startup.spec.ts, approval-flow.spec.ts）
- 测试左侧栏导航化（无品牌区、无展开/折叠）
- 测试全局快捷键（ctrl+t/e）
- 测试输入历史导航（↑/↓）
- 测试历史持久化（页面刷新后仍存在）
- 使用合理的超时和等待
  </action>
  <verify>
    <automated>npm run test:e2e -- keyboard-navigation.spec.ts</automated>
  </verify>
  <done>键盘导航 E2E 测试已创建并通过</done>
</task>

<task type="auto">
  <name>Task 2: 运行完整 E2E 测试套件</name>
  <files>tests/e2e/*.spec.ts</files>
  <action>
运行完整的 E2E 测试套件，确保 Phase 3 改动没有破坏现有功能：

```bash
npm run test:e2e
```

预期结果：
- 所有现有测试通过（21/22 或更好，Phase 2 有 1 个已知延期测试）
- 新增的 keyboard-navigation.spec.ts 测试通过

如果有测试失败：
1. 检查失败原因（定位器变化、时序问题、功能回归）
2. 修复相关代码或更新测试定位器
3. 重新运行测试直到全部通过

特别关注：
- startup.spec.ts（项目选择器移动可能影响）
- chooser.spec.ts（左侧栏改动可能影响）
- approval-flow.spec.ts（已知有 1 个延期测试）

如果 startup.spec.ts 或 chooser.spec.ts 失败，更新定位器以匹配新的左侧栏结构。
  </action>
  <verify>
    <automated>npm run test:e2e</automated>
  </verify>
  <done>完整 E2E 测试套件通过，无回归</done>
</task>

<task type="auto">
  <name>Task 3: 更新测试文档</name>
  <files>tests/e2e/README.md</files>
  <action>
如果存在 tests/e2e/README.md，更新测试文档以包含新的 keyboard-navigation.spec.ts：

如果文件不存在，创建简单的测试文档：

```markdown
# E2E Tests

## Test Suites

- **startup.spec.ts** - Application startup and initialization
- **approval-flow.spec.ts** - Command approval workflow (1 deferred test)
- **review-flow.spec.ts** - Code review workflow
- **status-system.spec.ts** - Status tracking system
- **streaming-flow.spec.ts** - Streaming assistant responses
- **chooser.spec.ts** - Session chooser and navigation
- **capabilities.spec.ts** - Feature capabilities
- **presets.spec.ts** - Review presets
- **keyboard-navigation.spec.ts** - Keyboard shortcuts and input history (Phase 3)

## Running Tests

```bash
# Run all tests
npm run test:e2e

# Run specific test file
npm run test:e2e -- keyboard-navigation.spec.ts

# Run in headed mode (see browser)
npm run test:e2e -- --headed
```

## Known Issues

- 1 test deferred in approval-flow.spec.ts: "blocks mode switching while approval is pending"
  - Out of scope for Phase 3, mode switcher removed in Phase 1
```

如果文件已存在，只需添加 keyboard-navigation.spec.ts 的条目。
  </action>
  <verify>
    <automated>test -f tests/e2e/README.md && echo "README exists" || echo "README created"</automated>
  </verify>
  <done>测试文档已更新，包含 keyboard-navigation.spec.ts 说明</done>
</task>

</tasks>

<verification>
手动验证：
1. 运行 `npm run test:e2e -- keyboard-navigation.spec.ts`
2. 验证所有测试通过
3. 运行 `npm run test:e2e` 验证无回归
4. 检查测试覆盖率（左侧栏、快捷键、输入历史）

自动验证：
- `npm run test:e2e` 通过（22/23 或更好，考虑 1 个已知延期测试）
- keyboard-navigation.spec.ts 所有测试通过
</verification>

<success_criteria>
- keyboard-navigation.spec.ts 测试套件已创建
- 测试覆盖左侧栏导航化（无品牌区、无展开/折叠）
- 测试覆盖全局快捷键（ctrl+t/e）
- 测试覆盖输入历史导航（↑/↓）
- 测试覆盖历史持久化
- 所有新测试通过
- 现有测试无回归（21/22 或更好）
- 测试文档已更新
</success_criteria>

<output>
After completion, create `.planning/phases/03-sidebar-navigation-keyboard-interaction/03-04-SUMMARY.md`
</output>
