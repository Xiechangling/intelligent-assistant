---
phase: 03-sidebar-navigation-keyboard-interaction
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/layout/LeftSidebar.tsx
  - src/styles/app-shell.css
autonomous: true
requirements: [REQ-03]

must_haves:
  truths:
    - "左侧栏仅包含项目列表和会话历史（无品牌区）"
    - "项目选择器位于左侧栏顶部"
    - "左侧栏无展开/折叠逻辑（始终显示内容）"
  artifacts:
    - path: "src/app/layout/LeftSidebar.tsx"
      provides: "纯导航列表左侧栏"
      min_lines: 80
    - path: "src/styles/app-shell.css"
      provides: "简化后的左侧栏样式"
      contains: ".sidebar"
  key_links:
    - from: "src/app/layout/LeftSidebar.tsx"
      to: "useAppShellStore"
      via: "项目选择和会话恢复"
      pattern: "setActiveProject|resumeSession"
---

<objective>
简化左侧栏为纯导航列表，移除品牌区和展开/折叠逻辑，将项目选择器从顶栏移至左侧栏顶部。

Purpose: 对齐官方 Claude Code Desktop 的左侧栏导航模式，简化信息架构
Output: 纯导航列表左侧栏（项目列表 + 会话历史）
</objective>

<execution_context>
@C:\Users\chris\.claude\get-shit-done\workflows\execute-plan.md
@C:\Users\chris\.claude\get-shit-done\templates\summary.md
</execution_context>

<context>
@E:\work\ai\agent\.planning\PROJECT.md
@E:\work\ai\agent\.planning\ROADMAP.md
@E:\work\ai\agent\.planning\STATE.md
@E:\work\ai\agent\.planning\research\UI-PATTERNS.md
@E:\work\ai\agent\.planning\research\SUMMARY.md
@E:\work\ai\agent\.planning\phases\01-topbar-visual-foundation\01-02-SUMMARY.md
@E:\work\ai\agent\.planning\phases\02-center-workspace-single-stream\02-01-SUMMARY.md
</context>

<interfaces>
<!-- Key types from appShellStore.ts -->
```typescript
interface AppShellState {
  activeProjectPath: string | null
  recentProjects: ProjectRecord[]
  activeSession: SessionDetail | null
  sessionHistory: SessionRecord[]
  sidebarProjectsExpanded: boolean
  sidebarSessionsExpanded: boolean
  setActiveProject: (project: ProjectRecord | null) => void
  resumeSession: (sessionId: string) => void
  setSidebarProjectsExpanded: (expanded: boolean) => void
  setSidebarSessionsExpanded: (expanded: boolean) => void
}
```

<!-- From projectService.ts -->
```typescript
export async function pickProjectDirectory(): Promise<ProjectRecord | null>
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: 移除品牌区和展开/折叠逻辑</name>
  <files>src/app/layout/LeftSidebar.tsx, src/styles/app-shell.css</files>
  <action>
从 LeftSidebar.tsx 移除：
- `.sidebar__section--brand` 区域（lines 85-93）
- `sidebarProjectsExpanded` / `sidebarSessionsExpanded` 状态读取
- `setSidebarProjectsExpanded` / `setSidebarSessionsExpanded` 调用
- 所有 `ChevronDown` / `ChevronRight` 图标和切换按钮
- `.sidebar__section-toggle` 按钮（保留 `.sidebar__section-header` 作为静态标题）

保留：
- 项目列表和会话历史的完整功能
- `setActiveProject` 和 `resumeSession` 调用
- 所有现有的项目/会话元数据显示

从 app-shell.css 移除：
- `.sidebar__section--brand` 样式
- `.sidebar__section-toggle` 样式
- `.sidebar__brand-mark` 和 `.sidebar__brand-copy` 样式

结果：左侧栏始终显示项目列表和会话历史，无折叠状态。
  </action>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <done>品牌区已移除，左侧栏无展开/折叠按钮，项目和会话列表始终可见</done>
</task>

<task type="auto">
  <name>Task 2: 将项目选择器移至左侧栏顶部</name>
  <files>src/app/layout/LeftSidebar.tsx, src/app/layout/TopToolbar.tsx, src/styles/app-shell.css</files>
  <action>
在 LeftSidebar.tsx 顶部添加项目选择器区域（在项目列表之前）：
- 创建 `.sidebar__project-picker` 区域
- 显示当前项目名称（从 `activeProjectPath` 提取最后一段）
- 添加 "Open workspace" 按钮（使用 `FolderOpen` 图标）
- 调用现有的 `handleProjectPick` 逻辑（已在 LeftSidebar.tsx 中）
- 使用 KeyboardShortcutHint 组件显示 `ctrl+o` 提示

从 TopToolbar.tsx 移除：
- `.toolbar__breadcrumb` 区域（lines 32-35）
- 项目名称显示逻辑（lines 26-28）

更新 TopToolbar.tsx 布局：
- 保留中央模型选择器和右侧设置按钮
- 调整 `.toolbar` 为两元素布局（center + right）

在 app-shell.css 添加：
```css
.sidebar__project-picker {
  padding: 12px;
  border-bottom: 1px solid var(--border-subtle);
  display: flex;
  flex-direction: column;
  gap: 8px;
}

.sidebar__project-name {
  font-size: 13px;
  font-weight: 500;
  color: var(--text-primary);
  overflow: hidden;
  text-overflow: ellipsis;
  white-space: nowrap;
}

.sidebar__project-button {
  display: flex;
  align-items: center;
  gap: 6px;
  padding: 6px 8px;
  font-size: 12px;
  background: var(--bg-secondary);
  border: 1px solid var(--border-default);
  border-radius: 4px;
  cursor: pointer;
  transition: all 160ms ease;
}

.sidebar__project-button:hover {
  background: var(--bg-tertiary);
  border-color: var(--border-hover);
}
```

注意：KeyboardShortcutHint 组件已在 Phase 1 创建，位于 src/app/components/KeyboardShortcutHint.tsx。
  </action>
  <verify>
    <automated>npm run build && npm run type-check</automated>
  </verify>
  <done>项目选择器已移至左侧栏顶部，顶栏仅保留模型选择器和设置按钮</done>
</task>

<task type="auto">
  <name>Task 3: 更新 E2E 测试定位器</name>
  <files>tests/e2e/startup.spec.ts, tests/e2e/chooser.spec.ts</files>
  <action>
更新所有引用项目选择器的测试：

在 startup.spec.ts 中：
- 将 `.toolbar__breadcrumb` 或 `.toolbar__project-badge` 定位器改为 `.sidebar__project-name`
- 将 `.toolbar__project-button` 定位器改为 `.sidebar__project-button`

在 chooser.spec.ts 中（如果存在项目选择相关测试）：
- 更新项目选择器定位器为左侧栏选择器

验证测试覆盖：
- 项目名称显示在左侧栏
- "Open workspace" 按钮在左侧栏可点击
- 左侧栏无品牌区和展开/折叠按钮

运行测试确保无回归：
```bash
npm run test:e2e
```

如果测试失败，修复定位器直到所有测试通过。
  </action>
  <verify>
    <automated>npm run test:e2e</automated>
  </verify>
  <done>E2E 测试通过，项目选择器定位器已更新为左侧栏选择器</done>
</task>

</tasks>

<verification>
手动验证：
1. 启动应用，左侧栏顶部显示项目选择器
2. 左侧栏无品牌区（无 "Claude Desktop" 文字）
3. 左侧栏无展开/折叠按钮（项目和会话列表始终可见）
4. 点击 "Open workspace" 按钮可选择项目
5. 顶栏仅显示模型选择器和设置按钮（无项目面包屑）

自动验证：
- `npm run build` 通过
- `npm run type-check` 通过
- `npm run test:e2e` 通过（21/22 或更好）
</verification>

<success_criteria>
- 左侧栏仅包含：项目选择器 + 项目列表 + 会话历史
- 左侧栏无品牌区（无 "Claude Desktop" 标识）
- 左侧栏无展开/折叠逻辑（内容始终可见）
- 项目选择器位于左侧栏顶部（带 ctrl+o 快捷键提示）
- 顶栏简化为 2 元素（模型选择器 + 设置按钮）
- 所有现有功能保持正常（项目选择、会话恢复）
- E2E 测试通过
- 构建通过
</success_criteria>

<output>
After completion, create `.planning/phases/03-sidebar-navigation-keyboard-interaction/03-01-SUMMARY.md`
</output>
