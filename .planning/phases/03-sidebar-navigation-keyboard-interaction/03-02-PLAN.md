---
phase: 03-sidebar-navigation-keyboard-interaction
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/hooks/useGlobalKeybindings.ts
  - src/app/state/appShellStore.ts
  - src/app/layout/AppShell.tsx
autonomous: true
requirements: [REQ-08]

must_haves:
  truths:
    - "ctrl+t 创建新会话"
    - "ctrl+o 打开项目选择器"
    - "ctrl+e 打开设置面板"
    - "macOS Option 键映射工作（†/π/ø 触发快捷键）"
    - "快捷键可配置禁用"
  artifacts:
    - path: "src/app/hooks/useGlobalKeybindings.ts"
      provides: "全局快捷键系统"
      min_lines: 80
      exports: ["useGlobalKeybindings"]
    - path: "src/app/state/appShellStore.ts"
      provides: "快捷键配置状态"
      contains: "keybindingsEnabled"
  key_links:
    - from: "src/app/hooks/useGlobalKeybindings.ts"
      to: "useAppShellStore"
      via: "快捷键触发 store 方法"
      pattern: "createProjectSession|pickProjectDirectory|setRightPanelOpen"
---

<objective>
实现全局快捷键系统，支持 ctrl+t/o/e 快捷键和 macOS Option 键映射。

Purpose: 提升键盘操作效率，对齐官方 Claude Code Desktop 的快捷键体验
Output: useGlobalKeybindings hook + 快捷键配置
</objective>

<execution_context>
@C:\Users\chris\.claude\get-shit-done\workflows\execute-plan.md
@C:\Users\chris\.claude\get-shit-done\templates\summary.md
</execution_context>

<context>
@E:\work\ai\agent\.planning\PROJECT.md
@E:\work\ai\agent\.planning\ROADMAP.md
@E:\work\ai\agent\.planning\STATE.md
@E:\work\ai\agent\.planning\research\INTERACTION-MODEL.md
@E:\work\ai\agent\.planning\research\SUMMARY.md
</context>

<interfaces>
<!-- From appShellStore.ts -->
```typescript
interface AppShellState {
  createProjectSession: () => Promise<void>
  setRightPanelOpen: (open: boolean) => void
  setRightPanelView: (view: RightPanelView) => void
}
```

<!-- From projectService.ts -->
```typescript
export async function pickProjectDirectory(): Promise<ProjectRecord | null>
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: 创建 useGlobalKeybindings hook</name>
  <files>src/app/hooks/useGlobalKeybindings.ts</files>
  <action>
创建新文件 src/app/hooks/useGlobalKeybindings.ts：

```typescript
import { useEffect } from 'react'
import { useAppShellStore } from '../state/appShellStore'
import { pickProjectDirectory } from '../services/projectService'

interface KeybindingConfig {
  enabled: boolean
  macOSOptionMapping: boolean
}

export function useGlobalKeybindings(config: KeybindingConfig = { enabled: true, macOSOptionMapping: true }) {
  const { createProjectSession, setRightPanelOpen, setRightPanelView, setActiveProject } = useAppShellStore()

  useEffect(() => {
    if (!config.enabled) {
      return
    }

    const handleKeyDown = async (event: KeyboardEvent) => {
      // macOS Option key mapping: Option+t = †, Option+o = ø, Option+e = ´
      // 检测 macOS Option 键特殊字符
      const isMacOptionT = config.macOSOptionMapping && event.key === '†'
      const isMacOptionO = config.macOSOptionMapping && event.key === 'ø'
      const isMacOptionE = config.macOSOptionMapping && (event.key === '´' || event.key === 'é')

      // ctrl+t: 新建会话
      if ((event.ctrlKey && event.key === 't') || isMacOptionT) {
        event.preventDefault()
        await createProjectSession()
        return
      }

      // ctrl+o: 打开项目
      if ((event.ctrlKey && event.key === 'o') || isMacOptionO) {
        event.preventDefault()
        try {
          const project = await pickProjectDirectory()
          if (project) {
            setActiveProject(project)
          }
        } catch (error) {
          console.error('Failed to pick project:', error)
        }
        return
      }

      // ctrl+e: 打开设置
      if ((event.ctrlKey && event.key === 'e') || isMacOptionE) {
        event.preventDefault()
        setRightPanelView('settings')
        setRightPanelOpen(true)
        return
      }
    }

    window.addEventListener('keydown', handleKeyDown)
    return () => window.removeEventListener('keydown', handleKeyDown)
  }, [config.enabled, config.macOSOptionMapping, createProjectSession, setRightPanelOpen, setRightPanelView, setActiveProject])
}
```

关键点：
- 使用 `event.preventDefault()` 防止浏览器默认行为
- 支持 macOS Option 键映射（†/ø/´ 字符）
- 提供配置选项禁用快捷键或 macOS 映射
- 使用 useEffect 清理监听器
  </action>
  <verify>
    <automated>npm run type-check</automated>
  </verify>
  <done>useGlobalKeybindings hook 已创建，支持 ctrl+t/o/e 和 macOS Option 键映射</done>
</task>

<task type="auto">
  <name>Task 2: 在 AppShell 中集成全局快捷键</name>
  <files>src/app/layout/AppShell.tsx, src/app/state/appShellStore.ts</files>
  <action>
在 appShellStore.ts 添加快捷键配置状态：

```typescript
interface AppShellState {
  // ... existing fields
  keybindingsEnabled: boolean
  macOSOptionMappingEnabled: boolean
  setKeybindingsEnabled: (enabled: boolean) => void
  setMacOSOptionMappingEnabled: (enabled: boolean) => void
}

// 在 create() 初始化中添加：
keybindingsEnabled: true,
macOSOptionMappingEnabled: true,
setKeybindingsEnabled: (enabled) => set({ keybindingsEnabled: enabled }),
setMacOSOptionMappingEnabled: (enabled) => set({ macOSOptionMappingEnabled: enabled }),
```

在 AppShell.tsx 中集成 hook：

```typescript
import { useGlobalKeybindings } from '../hooks/useGlobalKeybindings'

export function AppShell() {
  const { keybindingsEnabled, macOSOptionMappingEnabled } = useAppShellStore()
  
  // 注册全局快捷键
  useGlobalKeybindings({
    enabled: keybindingsEnabled,
    macOSOptionMapping: macOSOptionMappingEnabled,
  })

  // ... rest of component
}
```

注意：确保 hook 在组件顶层调用，不在条件语句中。
  </action>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <done>全局快捷键已集成到 AppShell，快捷键配置状态已添加到 store</done>
</task>

<task type="auto">
  <name>Task 3: 在设置面板添加快捷键配置选项</name>
  <files>src/app/layout/RightPanel.tsx</files>
  <action>
在 RightPanel.tsx 的设置视图中添加快捷键配置：

找到设置面板的渲染逻辑（`rightPanelView === 'settings'`），添加新的配置区域：

```typescript
{rightPanelView === 'settings' && (
  <div className="panel__content">
    {/* ... existing settings ... */}
    
    <section className="panel__section">
      <h3 className="panel__section-title">Keyboard Shortcuts</h3>
      
      <label className="panel__checkbox-label">
        <input
          type="checkbox"
          checked={keybindingsEnabled}
          onChange={(e) => setKeybindingsEnabled(e.target.checked)}
        />
        <span>Enable global keyboard shortcuts</span>
      </label>
      
      <div className="panel__helper-text">
        <p>ctrl+t: New session</p>
        <p>ctrl+o: Open project</p>
        <p>ctrl+e: Open settings</p>
      </div>
      
      {keybindingsEnabled && (
        <label className="panel__checkbox-label">
          <input
            type="checkbox"
            checked={macOSOptionMappingEnabled}
            onChange={(e) => setMacOSOptionMappingEnabled(e.target.checked)}
          />
          <span>Enable macOS Option key mapping</span>
        </label>
      )}
      
      {keybindingsEnabled && macOSOptionMappingEnabled && (
        <div className="panel__helper-text">
          <p>Option+t (†), Option+o (ø), Option+e (´) also work on macOS</p>
        </div>
      )}
    </section>
  </div>
)}
```

从 store 读取状态：
```typescript
const { keybindingsEnabled, macOSOptionMappingEnabled, setKeybindingsEnabled, setMacOSOptionMappingEnabled } = useAppShellStore()
```

注意：使用现有的 `.panel__section`, `.panel__checkbox-label`, `.panel__helper-text` 样式类（已在 app-shell.css 中定义）。
  </action>
  <verify>
    <automated>npm run build && npm run type-check</automated>
  </verify>
  <done>设置面板已添加快捷键配置选项，用户可启用/禁用快捷键和 macOS 映射</done>
</task>

</tasks>

<verification>
手动验证：
1. 按 ctrl+t 创建新会话
2. 按 ctrl+o 打开项目选择器
3. 按 ctrl+e 打开设置面板
4. 在 macOS 上测试 Option+t/o/e（如果可用）
5. 在设置面板禁用快捷键，验证快捷键不再工作
6. 重新启用快捷键，验证恢复正常

自动验证：
- `npm run build` 通过
- `npm run type-check` 通过
</verification>

<success_criteria>
- ctrl+t 创建新会话
- ctrl+o 打开项目选择器
- ctrl+e 打开设置面板
- macOS Option 键映射工作（†/π/ø）
- 设置面板可配置启用/禁用快捷键
- 设置面板可配置启用/禁用 macOS Option 映射
- 快捷键使用 preventDefault() 避免冲突
- 构建通过
- 类型检查通过
</success_criteria>

<output>
After completion, create `.planning/phases/03-sidebar-navigation-keyboard-interaction/03-02-SUMMARY.md`
</output>
