---
phase: 03-sidebar-navigation-keyboard-interaction
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/hooks/useInputHistory.ts
  - src/app/layout/CenterWorkspace.tsx
  - src/app/state/appShellStore.ts
autonomous: true
requirements: [REQ-10]

must_haves:
  truths:
    - "输入框按 ↑ 加载上一条历史"
    - "输入框按 ↓ 加载下一条历史"
    - "历史记录持久化到 localStorage"
    - "历史记录最多保存 50 条"
    - "历史记录不包含敏感数据"
  artifacts:
    - path: "src/app/hooks/useInputHistory.ts"
      provides: "输入历史导航 hook"
      min_lines: 60
      exports: ["useInputHistory"]
    - path: "src/app/layout/CenterWorkspace.tsx"
      provides: "输入框历史导航集成"
      contains: "useInputHistory"
  key_links:
    - from: "src/app/hooks/useInputHistory.ts"
      to: "localStorage"
      via: "历史记录持久化"
      pattern: "localStorage.getItem|localStorage.setItem"
---

<objective>
实现输入框历史导航功能，支持 ↑/↓ 键浏览历史记录，并持久化到 localStorage。

Purpose: 提升输入效率，避免重复输入相似的 prompt
Output: useInputHistory hook + localStorage 持久化
</objective>

<execution_context>
@C:\Users\chris\.claude\get-shit-done\workflows\execute-plan.md
@C:\Users\chris\.claude\get-shit-done\templates\summary.md
</execution_context>

<context>
@E:\work\ai\agent\.planning\PROJECT.md
@E:\work\ai\agent\.planning\ROADMAP.md
@E:\work\ai\agent\.planning\STATE.md
@E:\work\ai\agent\.planning\research\INTERACTION-MODEL.md
@E:\work\ai\agent\.planning\research\SUMMARY.md
</context>

<interfaces>
<!-- From appShellStore.ts -->
```typescript
interface AppShellState {
  draftPrompt: string
  setDraftPrompt: (prompt: string) => void
  submitPrompt: () => Promise<void>
}
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: 创建 useInputHistory hook</name>
  <files>src/app/hooks/useInputHistory.ts</files>
  <action>
创建新文件 src/app/hooks/useInputHistory.ts：

```typescript
import { useEffect, useRef, useState } from 'react'

const STORAGE_KEY = 'intelligent-assistant-input-history'
const MAX_HISTORY_SIZE = 50

// 敏感数据过滤器（简单实现）
function isSensitive(text: string): boolean {
  // 过滤包含明显敏感信息的 prompt
  const sensitivePatterns = [
    /api[_-]?key/i,
    /password/i,
    /secret/i,
    /token/i,
    /credential/i,
    /\b[A-Za-z0-9._%+-]+@[A-Za-z0-9.-]+\.[A-Z|a-z]{2,}\b/, // email
    /\b\d{3}-\d{2}-\d{4}\b/, // SSN-like
  ]
  
  return sensitivePatterns.some(pattern => pattern.test(text))
}

function loadHistory(): string[] {
  try {
    const stored = localStorage.getItem(STORAGE_KEY)
    if (!stored) return []
    const parsed = JSON.parse(stored)
    return Array.isArray(parsed) ? parsed : []
  } catch {
    return []
  }
}

function saveHistory(history: string[]): void {
  try {
    localStorage.setItem(STORAGE_KEY, JSON.stringify(history))
  } catch (error) {
    console.error('Failed to save input history:', error)
  }
}

export function useInputHistory(currentValue: string, onChange: (value: string) => void) {
  const [history, setHistory] = useState<string[]>(loadHistory)
  const [historyIndex, setHistoryIndex] = useState<number>(-1)
  const [tempDraft, setTempDraft] = useState<string>('')
  const isNavigating = useRef(false)

  // 添加新条目到历史
  const addToHistory = (text: string) => {
    if (!text.trim() || isSensitive(text)) {
      return
    }

    setHistory(prev => {
      // 去重：如果已存在相同内容，移除旧的
      const filtered = prev.filter(item => item !== text)
      // 添加到开头
      const updated = [text, ...filtered].slice(0, MAX_HISTORY_SIZE)
      saveHistory(updated)
      return updated
    })
    
    // 重置导航状态
    setHistoryIndex(-1)
    setTempDraft('')
  }

  // 处理 ↑/↓ 键导航
  const handleKeyDown = (event: React.KeyboardEvent<HTMLTextAreaElement>) => {
    if (history.length === 0) return

    if (event.key === 'ArrowUp') {
      event.preventDefault()
      isNavigating.current = true

      // 首次按 ↑：保存当前草稿
      if (historyIndex === -1) {
        setTempDraft(currentValue)
        setHistoryIndex(0)
        onChange(history[0])
      } else if (historyIndex < history.length - 1) {
        const nextIndex = historyIndex + 1
        setHistoryIndex(nextIndex)
        onChange(history[nextIndex])
      }
    } else if (event.key === 'ArrowDown') {
      event.preventDefault()
      isNavigating.current = true

      if (historyIndex > 0) {
        const nextIndex = historyIndex - 1
        setHistoryIndex(nextIndex)
        onChange(history[nextIndex])
      } else if (historyIndex === 0) {
        // 回到草稿
        setHistoryIndex(-1)
        onChange(tempDraft)
      }
    }
  }

  return {
    addToHistory,
    handleKeyDown,
    historySize: history.length,
  }
}
```

关键点：
- 使用 localStorage 持久化历史记录
- 最多保存 50 条记录
- 过滤敏感数据（API key、password、email 等）
- 支持 ↑/↓ 键导航
- 保存临时草稿，按 ↓ 可恢复
- 去重：相同内容只保留最新的
  </action>
  <verify>
    <automated>npm run type-check</automated>
  </verify>
  <done>useInputHistory hook 已创建，支持历史导航和 localStorage 持久化</done>
</task>

<task type="auto">
  <name>Task 2: 在 CenterWorkspace 集成输入历史</name>
  <files>src/app/layout/CenterWorkspace.tsx</files>
  <action>
在 CenterWorkspace.tsx 中集成 useInputHistory hook：

找到输入框的 textarea 元素（通常在组件底部），添加历史导航：

```typescript
import { useInputHistory } from '../hooks/useInputHistory'

export function CenterWorkspace() {
  const { draftPrompt, setDraftPrompt, submitPrompt } = useAppShellStore()
  
  // 集成输入历史
  const { addToHistory, handleKeyDown } = useInputHistory(draftPrompt, setDraftPrompt)

  const handleSubmit = async () => {
    // 提交前添加到历史
    addToHistory(draftPrompt)
    await submitPrompt()
  }

  const handleTextareaKeyDown = (event: React.KeyboardEvent<HTMLTextAreaElement>) => {
    // 处理历史导航
    handleKeyDown(event)
    
    // 保留现有的 Enter 提交逻辑
    if (event.key === 'Enter' && !event.shiftKey) {
      event.preventDefault()
      handleSubmit()
    }
  }

  return (
    <div className="workspace">
      {/* ... existing content ... */}
      
      <textarea
        className="workspace__input"
        value={draftPrompt}
        onChange={(e) => setDraftPrompt(e.target.value)}
        onKeyDown={handleTextareaKeyDown}
        placeholder="Type your message..."
      />
      
      <button onClick={handleSubmit}>Send</button>
    </div>
  )
}
```

注意：
- 在 submitPrompt 之前调用 addToHistory
- handleKeyDown 在 Enter 提交逻辑之前调用
- 保留现有的 Shift+Enter 换行行为
  </action>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <done>输入框已集成历史导航，↑/↓ 键可浏览历史记录</done>
</task>

<task type="auto">
  <name>Task 3: 添加历史导航视觉提示</name>
  <files>src/app/layout/CenterWorkspace.tsx, src/styles/app-shell.css</files>
  <action>
在输入框区域添加历史导航提示（可选，提升可发现性）：

在 CenterWorkspace.tsx 中，在输入框附近添加提示文本：

```typescript
const { addToHistory, handleKeyDown, historySize } = useInputHistory(draftPrompt, setDraftPrompt)

// 在输入框下方或旁边添加提示
{historySize > 0 && (
  <div className="workspace__input-hint">
    <KeyboardShortcutHint shortcut="↑" /> / <KeyboardShortcutHint shortcut="↓" /> to browse history ({historySize} saved)
  </div>
)}
```

在 app-shell.css 添加样式：

```css
.workspace__input-hint {
  font-size: 11px;
  color: var(--text-tertiary);
  padding: 4px 8px;
  display: flex;
  align-items: center;
  gap: 4px;
}
```

注意：KeyboardShortcutHint 组件已在 Phase 1 创建，可直接使用。如果不适合显示箭头键，可以使用纯文本 "↑/↓"。
  </action>
  <verify>
    <automated>npm run build && npm run type-check</automated>
  </verify>
  <done>输入框已添加历史导航视觉提示，用户可发现 ↑/↓ 功能</done>
</task>

</tasks>

<verification>
手动验证：
1. 输入一条 prompt 并提交
2. 清空输入框，按 ↑ 键，验证上一条 prompt 出现
3. 继续按 ↑ 键，验证可浏览更早的历史
4. 按 ↓ 键，验证可返回较新的历史
5. 在历史导航中按 ↓ 回到空白，验证恢复原始草稿
6. 刷新页面，验证历史记录持久化
7. 输入包含 "api_key" 或 "password" 的 prompt，验证不被保存到历史

自动验证：
- `npm run build` 通过
- `npm run type-check` 通过

localStorage 验证：
- 打开浏览器 DevTools > Application > Local Storage
- 查找 key: `intelligent-assistant-input-history`
- 验证历史记录为 JSON 数组，最多 50 条
</verification>

<success_criteria>
- 输入框按 ↑ 加载上一条历史
- 输入框按 ↓ 加载下一条历史
- 历史记录持久化到 localStorage
- 历史记录最多保存 50 条
- 敏感数据（api_key, password, email 等）不被保存
- 相同内容去重（只保留最新的）
- 历史导航时保存临时草稿，可恢复
- 构建通过
- 类型检查通过
</success_criteria>

<output>
After completion, create `.planning/phases/03-sidebar-navigation-keyboard-interaction/03-03-SUMMARY.md`
</output>
