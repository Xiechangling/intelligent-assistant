---
phase: 05-theme-system-enhancement
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/layout/RightPanel.tsx
  - src/styles/app-shell.css
autonomous: true
requirements: [REQ-09]
must_haves:
  truths:
    - User can see theme selector in settings panel
    - Theme selector shows current theme selection
    - Clicking theme option changes theme immediately
    - Theme selector has visual feedback for selected option
  artifacts:
    - path: src/app/layout/RightPanel.tsx
      provides: Theme selector UI in settings panel
      contains: "theme"
    - path: src/styles/app-shell.css
      provides: Theme selector styling
      contains: "theme-selector"
  key_links:
    - from: src/app/layout/RightPanel.tsx
      to: appShellStore.setTheme
      via: onClick handler
      pattern: "setTheme.*light.*dark.*auto"
---

<objective>
Add theme selector UI to settings panel for easy theme switching.

Purpose: Provide accessible UI for users to change theme preference.
Output: Theme selector in settings panel with visual feedback.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@src/app/layout/RightPanel.tsx
@src/styles/app-shell.css
@src/app/state/appShellStore.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add theme selector to settings panel</name>
  <files>src/app/layout/RightPanel.tsx</files>
  <action>
Add theme selector to RightPanel settings view:

1. Import theme state: `const theme = useAppShellStore((state) => state.theme)` and `const setTheme = useAppShellStore((state) => state.setTheme)`

2. Find the settings sections (Connection, Workspace, Keyboard, Capabilities groups). Add a new "Appearance" section after Connection section:

```tsx
{/* Appearance Section */}
<div className="right-panel__settings-section">
  <h3 className="right-panel__heading">Appearance</h3>
  <div className="right-panel__settings-list">
    <div className="right-panel__settings-item">
      <div className="right-panel__settings-item-head">
        <div>
          <strong>Theme</strong>
          <span>Choose your preferred color scheme</span>
        </div>
      </div>
      <div className="right-panel__theme-selector">
        <button
          className={`right-panel__theme-option ${theme === 'light' ? 'right-panel__theme-option--active' : ''}`}
          onClick={() => setTheme('light')}
          title="Light theme"
        >
          <Sun size={16} />
          <span>Light</span>
        </button>
        <button
          className={`right-panel__theme-option ${theme === 'dark' ? 'right-panel__theme-option--active' : ''}`}
          onClick={() => setTheme('dark')}
          title="Dark theme"
        >
          <Moon size={16} />
          <span>Dark</span>
        </button>
        <button
          className={`right-panel__theme-option ${theme === 'auto' ? 'right-panel__theme-option--active' : ''}`}
          onClick={() => setTheme('auto')}
          title="Auto (follows system)"
        >
          <Monitor size={16} />
          <span>Auto</span>
        </button>
      </div>
    </div>
  </div>
</div>
```

3. Import icons from lucide-react at top of file: `import { Sun, Moon, Monitor } from 'lucide-react'` (add to existing lucide-react import if present)

Place the Appearance section as the first section in settings (before Connection), since theme is a high-priority visual setting.
  </action>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <done>Theme selector appears in settings panel with three options (Light/Dark/Auto), current theme is visually indicated</done>
</task>

<task type="auto">
  <name>Task 2: Style theme selector</name>
  <files>src/styles/app-shell.css</files>
  <action>
Add theme selector styles to app-shell.css (add after `.right-panel__settings-item` styles around line 2300):

```css
.right-panel__theme-selector {
  display: flex;
  gap: 8px;
  margin-top: 12px;
}

.right-panel__theme-option {
  flex: 1;
  display: flex;
  flex-direction: column;
  align-items: center;
  gap: 6px;
  padding: 12px 8px;
  border-radius: var(--radius-sm);
  border: 1px solid var(--border-subtle);
  background: var(--bg-elevated);
  color: var(--text-secondary);
  cursor: pointer;
  transition: all 160ms ease;
}

.right-panel__theme-option:hover {
  background: var(--bg-muted);
  border-color: var(--border-highlight);
  color: var(--text-primary);
}

.right-panel__theme-option--active {
  background: var(--bg-accent-soft);
  border-color: var(--accent);
  color: var(--accent-strong);
}

.right-panel__theme-option--active:hover {
  background: var(--bg-accent-deep);
}

.right-panel__theme-option span {
  font-size: 12px;
  font-weight: 600;
}
```

Styles follow existing design system patterns: use design tokens, 160ms transitions, accent colors for active state, hover states for all options.
  </action>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <done>Theme selector styled with three equal-width buttons, active state highlighted, hover states present</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| UI → theme state | User clicks theme button to change preference |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-03 | Tampering | Theme button clicks | accept | Theme changes are cosmetic, no security impact |

</threat_model>

<verification>
1. Build passes: `npm run build`
2. Manual verification:
   - Open settings panel (click settings button in topbar)
   - Verify "Appearance" section appears first
   - Verify three theme buttons: Light (sun icon), Dark (moon icon), Auto (monitor icon)
   - Click Light button, verify theme changes to light immediately
   - Verify Light button shows active state (accent color)
   - Click Dark button, verify theme changes to dark immediately
   - Verify Dark button shows active state
   - Click Auto button, verify theme follows system
   - Verify Auto button shows active state
   - Hover over non-active buttons, verify hover state
   - Reload page, verify selected theme persists
</verification>

<success_criteria>
- Theme selector appears in settings panel Appearance section
- Three buttons: Light, Dark, Auto with icons
- Clicking button changes theme immediately
- Active button has visual indicator (accent color)
- Hover states work on all buttons
- Theme selection persists across reloads
- Build passes
</success_criteria>

<output>
After completion, create `.planning/phases/05-theme-system-enhancement/05-02-SUMMARY.md`
</output>
