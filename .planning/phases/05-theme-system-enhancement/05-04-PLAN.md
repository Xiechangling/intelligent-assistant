---
phase: 05-theme-system-enhancement
plan: 04
type: execute
wave: 2
depends_on: []
files_modified:
  - tests/e2e/approval-flow.spec.ts
  - tests/e2e/drawer-panel.spec.ts
  - tests/e2e/keyboard-navigation.spec.ts
  - tests/e2e/startup.spec.ts
autonomous: true
requirements: [REQ-09]
must_haves:
  truths:
    - All failing E2E tests are fixed or properly skipped
    - Test suite pass rate is 100% or documented exceptions exist
    - No regressions introduced by Phase 1-4 changes
  artifacts:
    - path: tests/e2e/approval-flow.spec.ts
      provides: Fixed approval flow tests
    - path: tests/e2e/drawer-panel.spec.ts
      provides: Fixed drawer panel tests
    - path: tests/e2e/keyboard-navigation.spec.ts
      provides: Fixed keyboard navigation tests
    - path: tests/e2e/startup.spec.ts
      provides: Fixed startup tests
  key_links:
    - from: tests/e2e/*.spec.ts
      to: app implementation
      via: playwright selectors
      pattern: "page\\.locator|page\\.click|page\\.waitForSelector"
---

<objective>
Fix remaining E2E test failures to achieve 100% pass rate before milestone completion.

Purpose: Ensure all functionality works correctly and no regressions exist.
Output: All E2E tests passing or properly documented as skipped.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@tests/e2e/approval-flow.spec.ts
@tests/e2e/drawer-panel.spec.ts
@tests/e2e/keyboard-navigation.spec.ts
@tests/e2e/startup.spec.ts
@src/app/layout/TopToolbar.tsx
@src/app/layout/RightPanel.tsx
@src/app/layout/LeftSidebar.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Investigate and fix failing E2E tests</name>
  <files>tests/e2e/approval-flow.spec.ts, tests/e2e/drawer-panel.spec.ts, tests/e2e/keyboard-navigation.spec.ts, tests/e2e/startup.spec.ts</files>
  <action>
Run E2E tests to identify failures, then fix each one:

1. **Run tests to see current failures:**
   ```bash
   npx playwright test --reporter=list
   ```

2. **For each failing test, investigate and fix:**

   **approval-flow.spec.ts:71** - "blocks mode switching while approval is pending"
   - Issue: Test expects mode switcher buttons that were removed in Phase 1
   - Fix: Skip this test with `.skip` and add comment explaining mode switcher was removed in v2.2 topbar simplification
   - Rationale: Mode switching UI no longer exists (moved to sidebar project picker), test is obsolete

   **drawer-panel.spec.ts:9** - "right panel opens as drawer when settings button clicked"
   - Issue: Selector mismatch or timing issue
   - Fix: Update selector to match actual implementation, add proper wait
   - Check: Verify settings button selector is `button[title="Settings"]`

   **keyboard-navigation.spec.ts:55** - "ctrl+t creates new session"
   - Issue: Keyboard shortcut may not be triggering or session creation not detected
   - Fix: Add wait after keyboard press, verify session count selector is correct
   - Check: Use `page.waitForTimeout(500)` after keyboard press to allow async operation

   **keyboard-navigation.spec.ts:115** - "up arrow loads previous input"
   - Issue: Input history may not be populated or selector wrong
   - Fix: Ensure input has focus before sending arrow key, verify history exists
   - Check: Add `await page.focus('.composer__input')` before arrow key

   **keyboard-navigation.spec.ts:147** - "down arrow navigates forward in history"
   - Issue: Similar to up arrow test
   - Fix: Same as above - ensure focus and proper timing

   **keyboard-navigation.spec.ts:174** - "history persists across page reload"
   - Issue: localStorage may not be persisting or selector wrong
   - Fix: Add wait after submit before reload, verify localStorage key
   - Check: Use `await page.waitForTimeout(500)` after submit to ensure persistence

   **startup.spec.ts:129** - "shows no workspace entry state with exact copy"
   - Issue: Text content mismatch or selector wrong
   - Fix: Update expected text to match actual implementation
   - Check: Verify exact text in workspace empty state

3. **Apply fixes systematically:**
   - Read each failing test file
   - Identify the specific assertion or selector causing failure
   - Update to match current implementation
   - Add appropriate waits for async operations
   - Re-run test to verify fix

4. **Document skipped tests:**
   - Add `.skip` to obsolete tests (like mode switcher)
   - Add comment explaining why skipped and when it was removed
   - Example: `test.skip('blocks mode switching...', async ({ page }) => { /* Skipped: mode switcher removed in Phase 1 topbar simplification */ })`

Goal: Achieve 100% pass rate on non-skipped tests. Skipped tests must have clear documentation.
  </action>
  <verify>
    <automated>npx playwright test</automated>
  </verify>
  <done>All E2E tests pass or are properly skipped with documentation, test suite at 100% pass rate for active tests</done>
</task>

<task type="auto">
  <name>Task 2: Verify no regressions in core flows</name>
  <files>tests/e2e/startup.spec.ts, tests/e2e/approval-flow.spec.ts, tests/e2e/review-flow.spec.ts</files>
  <action>
Run full E2E test suite and verify core flows still work:

1. **Run all tests:**
   ```bash
   npx playwright test --reporter=html
   ```

2. **Verify critical flows pass:**
   - Startup flow (5 tests)
   - Approval flow (4 active tests after skip)
   - Review flow (5 tests)
   - Status flow (1 test)
   - Drawer panel (11 tests)
   - Keyboard navigation (8 active tests after fixes)
   - Theme switching (9 tests from Plan 03)

3. **Check for unexpected failures:**
   - If any previously passing test now fails, investigate
   - Likely causes: selector changes from Phase 1-4, timing issues
   - Fix by updating selectors or adding appropriate waits

4. **Generate test report:**
   ```bash
   npx playwright show-report
   ```

5. **Document final test status:**
   - Total tests: ~54 (including new theme tests)
   - Expected pass rate: 100% (excluding skipped)
   - Skipped tests: 1 (mode switcher test)

Goal: Confirm no regressions from Phase 1-4 changes, all core functionality works.
  </action>
  <verify>
    <automated>npx playwright test --reporter=list | grep -E "passed|failed"</automated>
  </verify>
  <done>Full E2E suite passes, no regressions detected, test report generated</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Test → app | E2E tests interact with app via UI |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-05 | Denial of Service | Test suite execution time | accept | E2E tests run in CI, not production |

</threat_model>

<verification>
1. Run full test suite: `npx playwright test`
2. Verify pass rate: Should be 100% for active tests
3. Verify skipped tests have documentation
4. Check test report: `npx playwright show-report`
5. Verify no regressions in:
   - Startup flow
   - Approval flow
   - Review flow
   - Drawer panel
   - Keyboard navigation
   - Theme switching
</verification>

<success_criteria>
- All E2E tests pass or are properly skipped
- Skipped tests have clear documentation
- No regressions in core flows
- Test suite at 100% pass rate for active tests
- Test report generated successfully
- Build passes
</success_criteria>

<output>
After completion, create `.planning/phases/05-theme-system-enhancement/05-04-SUMMARY.md`
</output>
