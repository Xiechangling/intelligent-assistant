---
phase: 02-center-workspace-single-stream
plan: 03
type: execute
wave: 2
depends_on: [02-01, 02-02]
files_modified:
  - tests/e2e/approval-flow.spec.ts
  - tests/e2e/status-system.spec.ts
autonomous: true
requirements: [REQ-02]

must_haves:
  truths:
    - "E2E tests pass after status chip moved to session header"
    - "Approval flow tests find status chip in new location"
    - "Status system tests find status chip in new location"
  artifacts:
    - path: "tests/e2e/approval-flow.spec.ts"
      provides: "Updated approval flow tests"
      contains: "workspace__session-header"
    - path: "tests/e2e/status-system.spec.ts"
      provides: "Updated status system tests"
      contains: "workspace__session-header"
  key_links:
    - from: "E2E tests"
      to: "SessionHeader StatusChip"
      via: "Locator updated to new DOM location"
      pattern: "\\.workspace__session-header.*\\.workspace__status-pill"
---

<objective>
修复 3 个失败的 E2E 测试，将状态芯片定位器从 `.toolbar__status-chip` 更新为 session header 中的新位置。

Purpose: Phase 1 移除了顶栏的状态芯片，Phase 2 Plan 2 将其移至 session header。E2E 测试需要更新定位器以反映新的 DOM 结构。

Output: approval-flow.spec.ts 和 status-system.spec.ts 中的 3 个测试通过。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/02-center-workspace-single-stream/02-02-PLAN.md

## Known Test Failures

From STATE.md:
- approval-flow.spec.ts: 2 tests expecting `.toolbar__status-chip`
- status-system.spec.ts: 1 test expecting `.toolbar__status-chip`

Grep results show exact locations:
- `tests/e2e/approval-flow.spec.ts:57`: `await expect(page.locator('.toolbar__status-chip')).toHaveText('Awaiting approval')`
- `tests/e2e/approval-flow.spec.ts:129`: `await expect(page.locator('.toolbar__status-chip')).toHaveText('Awaiting approval')`
- `tests/e2e/status-system.spec.ts:64`: `await expect(page.locator('.toolbar__status-chip')).toHaveText(expectedToolbarStatus)`

## New DOM Structure

After Plan 02-02, StatusChip is now inside SessionHeader:
```html
<div class="workspace__session-header">
  <div class="workspace__session-header-main">
    <div class="workspace__session-title-group">...</div>
    <span class="workspace__status-pill workspace__status-pill--{tone}">...</span>
  </div>
</div>
```

New locator should be: `.workspace__session-header .workspace__status-pill`
</context>

<tasks>

<task type="auto">
  <name>Task 1: 更新 approval-flow.spec.ts 中的状态芯片定位器</name>
  <files>tests/e2e/approval-flow.spec.ts</files>
  <action>
更新 approval-flow.spec.ts 中的两处状态芯片定位器：

1. **Line 57 附近**: 将 `.toolbar__status-chip` 改为 `.workspace__session-header .workspace__status-pill`
   - 查找: `page.locator('.toolbar__status-chip')`
   - 替换为: `page.locator('.workspace__session-header .workspace__status-pill')`
   - 保持 `.toHaveText('Awaiting approval')` 不变

2. **Line 129 附近**: 同样的替换
   - 查找: `page.locator('.toolbar__status-chip')`
   - 替换为: `page.locator('.workspace__session-header .workspace__status-pill')`
   - 保持 `.toHaveText('Awaiting approval')` 不变

**注意**: 
- 只修改定位器字符串，不修改测试逻辑
- StatusChip 组件渲染的类名是 `workspace__status-pill`（不是 `status-chip`）
- 需要通过父容器 `.workspace__session-header` 定位以确保唯一性
  </action>
  <verify>
    <automated>grep -c "workspace__session-header.*workspace__status-pill" tests/e2e/approval-flow.spec.ts</automated>
  </verify>
  <done>
- approval-flow.spec.ts 中的两处定位器已更新
- 使用新的 DOM 路径 `.workspace__session-header .workspace__status-pill`
- 测试断言逻辑保持不变
  </done>
</task>

<task type="auto">
  <name>Task 2: 更新 status-system.spec.ts 中的状态芯片定位器</name>
  <files>tests/e2e/status-system.spec.ts</files>
  <action>
更新 status-system.spec.ts 中的状态芯片定位器：

1. **Line 64 附近**: 将 `.toolbar__status-chip` 改为 `.workspace__session-header .workspace__status-pill`
   - 查找: `page.locator('.toolbar__status-chip')`
   - 替换为: `page.locator('.workspace__session-header .workspace__status-pill')`
   - 保持 `.toHaveText(expectedToolbarStatus)` 不变

**注意**: 
- 变量名 `expectedToolbarStatus` 可以保持不变（虽然不再在 toolbar 中，但变量名不影响功能）
- 如果想要更准确的命名，可以将变量重命名为 `expectedStatus`，但这是可选的
  </action>
  <verify>
    <automated>grep -c "workspace__session-header.*workspace__status-pill" tests/e2e/status-system.spec.ts</automated>
  </verify>
  <done>
- status-system.spec.ts 中的定位器已更新
- 使用新的 DOM 路径 `.workspace__session-header .workspace__status-pill`
- 测试断言逻辑保持不变
  </done>
</task>

<task type="auto">
  <name>Task 3: 运行 E2E 测试验证修复</name>
  <files>tests/e2e/approval-flow.spec.ts, tests/e2e/status-system.spec.ts</files>
  <action>
运行更新后的 E2E 测试，验证所有测试通过：

1. **运行 approval-flow 测试**:
```bash
npx playwright test tests/e2e/approval-flow.spec.ts
```
预期: 所有测试通过（包括之前失败的 2 个）

2. **运行 status-system 测试**:
```bash
npx playwright test tests/e2e/status-system.spec.ts
```
预期: 所有测试通过（包括之前失败的 1 个）

3. **运行完整 E2E 套件**（可选，确保没有破坏其他测试）:
```bash
npx playwright test
```
预期: 所有测试通过

**如果测试失败**:
- 检查 SessionHeader 是否正确渲染 StatusChip
- 检查 StatusChip 的类名是否为 `workspace__status-pill`
- 检查测试中的文本断言是否与实际渲染的文本匹配
- 使用 Playwright 的 `--debug` 模式检查 DOM 结构
  </action>
  <verify>
    <automated>npx playwright test tests/e2e/approval-flow.spec.ts tests/e2e/status-system.spec.ts</automated>
  </verify>
  <done>
- approval-flow.spec.ts 的 2 个测试通过
- status-system.spec.ts 的 1 个测试通过
- 没有引入新的测试失败
- E2E 测试套件整体健康
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| N/A | Test-only changes, no production code affected |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| N/A | N/A | N/A | N/A | Test updates only - no security impact |

**Note**: This plan only updates E2E test locators. No production code changes, no security implications.
</threat_model>

<verification>
## Test Execution Verification
```bash
# Run specific failing tests
npx playwright test tests/e2e/approval-flow.spec.ts
npx playwright test tests/e2e/status-system.spec.ts

# Run full E2E suite
npx playwright test
```

Expected results:
- approval-flow.spec.ts: All tests pass (previously 2 failing)
- status-system.spec.ts: All tests pass (previously 1 failing)
- Full suite: All tests pass (startup 5/5, approval 5/5, review 5/5, status 1/1)

## Locator Verification
```bash
# Verify new locators are in place
grep "workspace__session-header.*workspace__status-pill" tests/e2e/approval-flow.spec.ts
grep "workspace__session-header.*workspace__status-pill" tests/e2e/status-system.spec.ts

# Verify old locators are removed
grep "toolbar__status-chip" tests/e2e/approval-flow.spec.ts
grep "toolbar__status-chip" tests/e2e/status-system.spec.ts
```

Expected:
- New locators found in both files
- Old locators NOT found (grep returns no results)

## Debug Mode (if tests fail)
```bash
npx playwright test tests/e2e/approval-flow.spec.ts --debug
```
Use Playwright Inspector to verify:
1. SessionHeader renders with `.workspace__session-header` class
2. StatusChip renders with `.workspace__status-pill` class
3. StatusChip text matches expected values
</verification>

<success_criteria>
- [ ] approval-flow.spec.ts updated with new status chip locator (2 occurrences)
- [ ] status-system.spec.ts updated with new status chip locator (1 occurrence)
- [ ] All 3 previously failing tests now pass
- [ ] No new test failures introduced
- [ ] Old `.toolbar__status-chip` locators completely removed from test files
- [ ] New locators use `.workspace__session-header .workspace__status-pill` pattern
- [ ] Full E2E suite passes (startup, approval, review, status)
</success_criteria>

<output>
After completion, create `.planning/phases/02-center-workspace-single-stream/02-03-SUMMARY.md`
</output>
