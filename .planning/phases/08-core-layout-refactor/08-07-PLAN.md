---
phase: 08-core-layout-refactor
plan: 07
type: checkpoint:human-verify
wave: 4
depends_on: [08-06]
files_modified: []
autonomous: false
requirements: [LAYOUT-01, LAYOUT-02, LAYOUT-03, LAYOUT-04, REMOVE-01, REMOVE-02, REMOVE-03, REMOVE-04, REMOVE-05]

must_haves:
  truths:
    - "用户可以看到极简顶栏（窗口控制 + 导航 + 三模式标签）"
    - "用户可以看到重构后的左侧栏（顶部操作区 + 列表）"
    - "用户可以看到纯净的对话流"
    - "用户可以使用增强的输入框"
    - "审批/审查面板已完全消失"
  artifacts: []
  key_links: []
---

<objective>
人工验证 Phase 8 的所有布局重构成果

Purpose: 确保新布局符合官方 UI 设计，所有功能正常工作
Output: 验证通过或问题反馈
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-core-layout-refactor/08-RESEARCH.md
</context>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
Phase 8 核心布局重构完成，包括：
1. 状态清理：移除审批/审查状态，添加导航历史
2. 组件归档：RightPanel 和 BottomPanel 移至 _archived
3. AppShell 简化：两列布局（360px 左侧栏 + 自适应中央区域）
4. TopToolbar 重写：窗口控制 + 导航按钮 + 三模式标签
5. LeftSidebar 重写：顶部操作区 + Projects/Sessions 列表
6. CenterWorkspace 清理：移除内联审批/审查卡片
7. Composer 增强：附件预览 + 语音按钮 + 发送按钮
  </what-built>
  
  <how-to-verify>
**启动应用：**
```bash
npm run tauri dev
```

**验证清单：**

1. **顶栏布局（LAYOUT-01）**
   - [ ] 顶栏高度为 48px
   - [ ] 左侧显示窗口控制按钮（最小化/最大化/关闭）
   - [ ] 窗口控制按钮旁边显示前进后退按钮
   - [ ] 中央显示 Chat/Cowork/Code 三模式标签
   - [ ] 点击窗口控制按钮可以最小化/最大化/关闭窗口
   - [ ] 前进后退按钮根据导航历史正确启用/禁用

2. **左侧栏布局（LAYOUT-02）**
   - [ ] 左侧栏宽度固定为 360px
   - [ ] 顶部显示 "New session" 按钮（带 Plus 图标）
   - [ ] 顶部显示 Search 和 Customize 图标按钮
   - [ ] 下方显示 Projects 列表
   - [ ] 下方显示 Sessions 列表
   - [ ] 点击 "New session" 可以创建新会话

3. **中央工作区（LAYOUT-03）**
   - [ ] 中央区域为单列对话流布局
   - [ ] 不再显示右侧抽屉（设置面板）
   - [ ] 不再显示底部托盘（审批/审查面板）
   - [ ] 对话流中不显示内联审批卡片
   - [ ] 对话流中不显示内联审查卡片
   - [ ] 空状态显示简单文本提示（无快速操作卡片）

4. **增强输入框（LAYOUT-04）**
   - [ ] 输入框左侧显示附件按钮（回形针图标）
   - [ ] 输入框右侧显示语音输入按钮
   - [ ] 输入文本后显示发送按钮
   - [ ] 点击附件按钮可以选择文件
   - [ ] 选择文件后显示附件预览
   - [ ] 点击语音按钮可以触发语音输入

5. **功能测试**
   - [ ] 创建新会话正常工作
   - [ ] 发送消息正常工作
   - [ ] 切换会话正常工作
   - [ ] 前进后退导航正常工作
   - [ ] 窗口调整大小时布局正常（左侧栏保持 360px）

6. **视觉检查**
   - [ ] 整体布局简洁，无多余面板
   - [ ] 分隔线清晰可见
   - [ ] 按钮 hover 效果正常
   - [ ] 主题切换正常工作（light/dark）

**测试场景：**

场景 1 - 创建和导航会话：
1. 点击 "New session" 创建会话 A
2. 发送一条消息
3. 点击 "New session" 创建会话 B
4. 发送一条消息
5. 点击后退按钮，应该回到会话 A
6. 点击前进按钮，应该回到会话 B

场景 2 - 附件和语音：
1. 点击附件按钮，选择一个文件
2. 确认附件预览显示
3. 点击语音按钮，确认语音输入激活
4. 输入文本，确认发送按钮出现
5. 点击发送按钮，确认消息发送

场景 3 - 窗口控制：
1. 点击最小化按钮，窗口应该最小化
2. 恢复窗口
3. 点击最大化按钮，窗口应该最大化
4. 再次点击，窗口应该恢复正常大小

**问题记录：**
如果发现问题，请记录：
- 问题描述
- 复现步骤
- 预期行为 vs 实际行为
- 截图（如果适用）
  </how-to-verify>
  
  <resume-signal>
输入 "approved" 表示验证通过，或描述发现的问题
  </resume-signal>
</task>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| N/A | 人工验证阶段，无信任边界 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| N/A | N/A | N/A | N/A | N/A |
</threat_model>

<verification>
人工验证所有布局和功能是否符合预期
</verification>

<success_criteria>
- 所有验证清单项目通过
- 三个测试场景成功执行
- 无阻塞性问题
- 用户确认布局符合官方 UI 设计
</success_criteria>

<output>
After completion, create `.planning/phases/08-core-layout-refactor/08-07-SUMMARY.md`
</output>
