---
phase: 08-core-layout-refactor
plan: 03
type: execute
wave: 2
depends_on: [08-01, 08-02]
files_modified:
  - src/app/layout/AppShell.tsx
  - src/styles/app-shell.css
autonomous: true
requirements: [REMOVE-01, REMOVE-02, LAYOUT-04]

must_haves:
  truths:
    - "AppShell 不再渲染 RightPanel 和 BottomPanel"
    - "CSS Grid 布局简化为两列（左侧栏 + 中央区域）"
    - "左侧栏固定宽度 360px"
  artifacts:
    - path: "src/app/layout/AppShell.tsx"
      provides: "简化的 AppShell 布局"
      min_lines: 40
    - path: "src/styles/app-shell.css"
      provides: "更新的 Grid 布局样式"
      min_lines: 50
  key_links:
    - from: "AppShell.tsx"
      to: "app-shell.css"
      via: "className references"
      pattern: "app-shell__"
---

<objective>
更新 AppShell 组件，移除 RightPanel 和 BottomPanel 引用，简化布局为两列结构

Purpose: 实现新的极简布局结构，移除旧面板
Output: 简化的 AppShell 和对应的 CSS Grid 布局
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-core-layout-refactor/08-RESEARCH.md
@src/app/layout/AppShell.tsx
@src/styles/app-shell.css
</context>

<tasks>

<task type="auto">
  <name>Task 1: 更新 AppShell 组件移除旧面板</name>
  <files>src/app/layout/AppShell.tsx</files>
  <action>
从 AppShell.tsx 中移除：
1. 删除 RightPanel 和 BottomPanel 的 import 语句
2. 删除 rightPanelOpen, rightPanelWidth, bottomPanelExpanded, executionRecord, pendingProposal 的状态读取
3. 删除 showBottomPanel 计算逻辑
4. 删除 `<RightPanel />` JSX
5. 删除 `<BottomPanel />` JSX 和条件渲染
6. 删除 `app-shell--drawer-open` 和 `app-shell--bottom-open` 的 className 条件
7. 删除 `--right-panel-width` CSS 变量

保留：
- TopToolbar, LeftSidebar, CenterWorkspace
- 主题相关逻辑
- 全局快捷键 hook

简化后的结构：
```tsx
<div className="app-shell">
  <header className="app-shell__top">
    <TopToolbar />
  </header>
  <aside className="app-shell__left">
    <LeftSidebar />
  </aside>
  <main className="app-shell__center">
    <CenterWorkspace />
  </main>
</div>
```
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <done>AppShell 组件已简化，不再引用 RightPanel 和 BottomPanel，TypeScript 编译通过</done>
</task>

<task type="auto">
  <name>Task 2: 更新 CSS Grid 布局为两列结构</name>
  <files>src/styles/app-shell.css</files>
  <action>
更新 app-shell.css 的 Grid 布局（参考 RESEARCH.md Pattern 3）：

```css
.app-shell {
  display: grid;
  grid-template-columns: 360px 1fr;
  grid-template-rows: 48px 1fr;
  height: 100vh;
  width: 100vw;
  overflow: hidden;
}

.app-shell__top {
  grid-column: 1 / -1;
  grid-row: 1;
  height: 48px;
  border-bottom: 1px solid var(--border-subtle);
}

.app-shell__left {
  grid-column: 1;
  grid-row: 2;
  width: 360px;
  min-width: 360px;
  max-width: 360px;
  overflow-y: auto;
  border-right: 1px solid var(--border-subtle);
}

.app-shell__center {
  grid-column: 2;
  grid-row: 2;
  overflow-y: auto;
}
```

移除以下样式：
- .app-shell__drawer 及相关样式
- .app-shell__bottom 及相关样式
- .app-shell--drawer-open 修饰符
- .app-shell--bottom-open 修饰符
- --right-panel-width 变量相关样式
  </action>
  <verify>
    <automated>grep -q "grid-template-columns: 360px 1fr" src/styles/app-shell.css && ! grep -q "app-shell__drawer" src/styles/app-shell.css</automated>
  </verify>
  <done>CSS Grid 布局已更新为两列结构，旧面板样式已移除</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| N/A | 纯 UI 布局变更，无信任边界 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-04 | Denial of Service | 固定 360px 宽度可能在小屏幕溢出 | accept | 后续 Phase 添加最小窗口尺寸限制（800x600） |
</threat_model>

<verification>
1. AppShell.tsx 不再 import RightPanel 和 BottomPanel
2. AppShell.tsx 的 JSX 只包含 TopToolbar, LeftSidebar, CenterWorkspace
3. app-shell.css 使用 grid-template-columns: 360px 1fr
4. app-shell.css 不包含 drawer 和 bottom 相关样式
5. TypeScript 编译通过
</verification>

<success_criteria>
- AppShell 组件简化为三个区域（顶栏、左侧栏、中央区域）
- CSS Grid 布局为 360px 固定左侧栏 + 自适应中央区域
- 旧面板的样式和引用已完全移除
- 应用可以正常启动和渲染
</success_criteria>

<output>
After completion, create `.planning/phases/08-core-layout-refactor/08-03-SUMMARY.md`
</output>
