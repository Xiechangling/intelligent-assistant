---
phase: 08-core-layout-refactor
plan: 04
type: execute
wave: 2
depends_on: [08-01, 08-02]
files_modified:
  - src/app/layout/TopToolbar.tsx
  - src/app/components/WindowControls/WindowControls.tsx
  - src/app/components/WindowControls/WindowControls.module.css
  - src/app/components/NavigationButtons/NavigationButtons.tsx
  - src/app/components/NavigationButtons/NavigationButtons.module.css
autonomous: true
requirements: [LAYOUT-01]

must_haves:
  truths:
    - "顶栏包含窗口控制按钮（最小化/最大化/关闭）"
    - "顶栏包含前进后退导航按钮"
    - "顶栏包含三模式标签（Chat/Cowork/Code）"
    - "顶栏高度为 48px"
  artifacts:
    - path: "src/app/layout/TopToolbar.tsx"
      provides: "重写的顶栏组件"
      min_lines: 30
    - path: "src/app/components/WindowControls/WindowControls.tsx"
      provides: "窗口控制组件"
      min_lines: 30
    - path: "src/app/components/NavigationButtons/NavigationButtons.tsx"
      provides: "导航按钮组件"
      min_lines: 30
  key_links:
    - from: "TopToolbar"
      to: "WindowControls"
      via: "component composition"
      pattern: "<WindowControls"
    - from: "TopToolbar"
      to: "NavigationButtons"
      via: "component composition"
      pattern: "<NavigationButtons"
    - from: "NavigationButtons"
      to: "appShellStore.goBack/goForward"
      via: "onClick handlers"
      pattern: "goBack|goForward"
---

<objective>
重写 TopToolbar 组件，实现窗口控制 + 导航按钮 + 三模式标签的极简顶栏

Purpose: 实现官方 UI 的极简顶栏布局
Output: 新的 TopToolbar 和子组件（WindowControls, NavigationButtons）
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-core-layout-refactor/08-RESEARCH.md
@src/app/layout/TopToolbar.tsx
@src/app/components/ModeTabs/ModeTabs.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: 创建 WindowControls 组件</name>
  <files>src/app/components/WindowControls/WindowControls.tsx, src/app/components/WindowControls/WindowControls.module.css</files>
  <action>
创建 WindowControls 组件（参考 RESEARCH.md Pattern 1）：

WindowControls.tsx:
```typescript
import { Minus, Square, X } from 'lucide-react'
import { getCurrentWindow } from '@tauri-apps/api/window'
import styles from './WindowControls.module.css'

export function WindowControls() {
  const appWindow = getCurrentWindow()

  const handleMinimize = () => appWindow.minimize()
  const handleMaximize = () => appWindow.toggleMaximize()
  const handleClose = () => appWindow.close()

  return (
    <div className={styles.windowControls}>
      <button onClick={handleMinimize} aria-label="Minimize" className={styles.button}>
        <Minus size={16} />
      </button>
      <button onClick={handleMaximize} aria-label="Maximize" className={styles.button}>
        <Square size={16} />
      </button>
      <button onClick={handleClose} aria-label="Close" className={styles.closeButton}>
        <X size={16} />
      </button>
    </div>
  )
}
```

WindowControls.module.css:
```css
.windowControls {
  display: flex;
  gap: 8px;
  align-items: center;
}

.button {
  width: 32px;
  height: 32px;
  display: flex;
  align-items: center;
  justify-content: center;
  border: none;
  background: transparent;
  color: var(--text-secondary);
  cursor: pointer;
  border-radius: 4px;
  transition: background-color 0.2s ease-out;
}

.button:hover {
  background-color: var(--hover-bg);
}

.closeButton {
  composes: button;
}

.closeButton:hover {
  background-color: var(--error-bg);
  color: var(--error-text);
}
```
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <done>WindowControls 组件已创建，包含最小化/最大化/关闭按钮</done>
</task>

<task type="auto">
  <name>Task 2: 创建 NavigationButtons 组件</name>
  <files>src/app/components/NavigationButtons/NavigationButtons.tsx, src/app/components/NavigationButtons/NavigationButtons.module.css</files>
  <action>
创建 NavigationButtons 组件（参考 RESEARCH.md Pattern 2）：

NavigationButtons.tsx:
```typescript
import { ChevronLeft, ChevronRight } from 'lucide-react'
import { useAppShellStore } from '../../state/appShellStore'
import styles from './NavigationButtons.module.css'

export function NavigationButtons() {
  const { navigationHistory, navigationIndex, goBack, goForward } = useAppShellStore()

  const canGoBack = navigationIndex > 0
  const canGoForward = navigationIndex < navigationHistory.length - 1

  return (
    <div className={styles.navigationButtons}>
      <button
        onClick={goBack}
        disabled={!canGoBack}
        aria-label="Go back"
        className={styles.button}
      >
        <ChevronLeft size={20} />
      </button>
      <button
        onClick={goForward}
        disabled={!canGoForward}
        aria-label="Go forward"
        className={styles.button}
      >
        <ChevronRight size={20} />
      </button>
    </div>
  )
}
```

NavigationButtons.module.css:
```css
.navigationButtons {
  display: flex;
  gap: 4px;
  align-items: center;
}

.button {
  width: 32px;
  height: 32px;
  display: flex;
  align-items: center;
  justify-content: center;
  border: none;
  background: transparent;
  color: var(--text-secondary);
  cursor: pointer;
  border-radius: 4px;
  transition: background-color 0.2s ease-out;
}

.button:hover:not(:disabled) {
  background-color: var(--hover-bg);
}

.button:disabled {
  opacity: 0.3;
  cursor: not-allowed;
}
```
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <done>NavigationButtons 组件已创建，连接到 goBack/goForward actions</done>
</task>

<task type="auto">
  <name>Task 3: 重写 TopToolbar 组件</name>
  <files>src/app/layout/TopToolbar.tsx</files>
  <action>
完全重写 TopToolbar.tsx，移除旧的模型选择器和设置按钮，使用新布局：

```typescript
import { WindowControls } from '../components/WindowControls/WindowControls'
import { NavigationButtons } from '../components/NavigationButtons/NavigationButtons'
import { ModeTabs } from '../components/ModeTabs/ModeTabs'

export function TopToolbar() {
  return (
    <div className="top-toolbar">
      <div className="top-toolbar__left">
        <WindowControls />
        <NavigationButtons />
      </div>
      <div className="top-toolbar__center">
        <ModeTabs />
      </div>
      <div className="top-toolbar__right">
        {/* 预留空间，未来可能添加其他控件 */}
      </div>
    </div>
  )
}
```

在 app-shell.css 中添加 TopToolbar 样式：
```css
.top-toolbar {
  display: flex;
  align-items: center;
  justify-content: space-between;
  height: 48px;
  padding: 0 12px;
  background: var(--bg-primary);
}

.top-toolbar__left {
  display: flex;
  align-items: center;
  gap: 16px;
}

.top-toolbar__center {
  flex: 1;
  display: flex;
  justify-content: center;
}

.top-toolbar__right {
  width: 100px; /* 占位，保持对称 */
}
```

移除所有旧的项目选择器、模型选择器、设置按钮相关代码。
  </action>
  <verify>
    <automated>npx tsc --noEmit && grep -q "WindowControls" src/app/layout/TopToolbar.tsx && grep -q "NavigationButtons" src/app/layout/TopToolbar.tsx</automated>
  </verify>
  <done>TopToolbar 已重写，包含窗口控制、导航按钮、三模式标签</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| UI → Tauri window API | 窗口控制调用原生 API |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-05 | Denial of Service | 快速点击窗口控制按钮 | accept | Tauri 内部有速率限制，OS 级别保护 |
</threat_model>

<verification>
1. WindowControls 组件存在并使用 Tauri window API
2. NavigationButtons 组件存在并连接到 appShellStore
3. TopToolbar 组合了三个子组件（WindowControls, NavigationButtons, ModeTabs）
4. TopToolbar 高度为 48px
5. TypeScript 编译通过
</verification>

<success_criteria>
- 顶栏包含窗口控制按钮（最小化/最大化/关闭）
- 顶栏包含前进后退导航按钮
- 顶栏包含三模式标签（来自 Phase 7）
- 顶栏高度为 48px
- 窗口控制按钮可以正常工作
- 导航按钮根据历史状态正确启用/禁用
</success_criteria>

<output>
After completion, create `.planning/phases/08-core-layout-refactor/08-04-SUMMARY.md`
</output>
