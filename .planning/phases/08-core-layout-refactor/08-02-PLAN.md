---
phase: 08-core-layout-refactor
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/layout/_archived/RightPanel.tsx
  - src/app/layout/_archived/BottomPanel.tsx
autonomous: true
requirements: [REMOVE-01, REMOVE-02]

must_haves:
  truths:
    - "RightPanel 和 BottomPanel 组件已移动到 _archived 目录"
    - "组件文件保留在 git 历史中，可随时恢复"
  artifacts:
    - path: "src/app/layout/_archived/RightPanel.tsx"
      provides: "归档的右侧面板组件"
      min_lines: 50
    - path: "src/app/layout/_archived/BottomPanel.tsx"
      provides: "归档的底部面板组件"
      min_lines: 50
  key_links: []
---

<objective>
归档 RightPanel 和 BottomPanel 组件到 _archived 目录

Purpose: 保留组件历史的同时，为新布局清理旧组件
Output: RightPanel 和 BottomPanel 移动到 _archived 目录
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-core-layout-refactor/08-RESEARCH.md
@src/app/layout/RightPanel.tsx
@src/app/layout/BottomPanel.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: 创建 _archived 目录并移动组件</name>
  <files>src/app/layout/_archived/RightPanel.tsx, src/app/layout/_archived/BottomPanel.tsx</files>
  <action>
创建归档目录：
```bash
mkdir -p src/app/layout/_archived
```

使用 git mv 移动组件（保留 git 历史）：
```bash
git mv src/app/layout/RightPanel.tsx src/app/layout/_archived/
git mv src/app/layout/BottomPanel.tsx src/app/layout/_archived/
```

不要删除文件，使用 git mv 确保历史可追溯。
  </action>
  <verify>
    <automated>test -f src/app/layout/_archived/RightPanel.tsx && test -f src/app/layout/_archived/BottomPanel.tsx && ! test -f src/app/layout/RightPanel.tsx && ! test -f src/app/layout/BottomPanel.tsx</automated>
  </verify>
  <done>RightPanel 和 BottomPanel 已移动到 _archived 目录，原位置文件不存在</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| N/A | 纯文件移动操作，无信任边界 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-03 | Information Disclosure | 归档组件可能包含敏感逻辑 | accept | 组件仅移动位置，不改变访问权限，git 历史本就公开 |
</threat_model>

<verification>
1. _archived 目录已创建
2. RightPanel.tsx 和 BottomPanel.tsx 存在于 _archived 目录
3. 原 layout 目录下不再有这两个文件
4. git status 显示为 renamed 而非 deleted + added
</verification>

<success_criteria>
- src/app/layout/_archived/RightPanel.tsx 存在
- src/app/layout/_archived/BottomPanel.tsx 存在
- src/app/layout/RightPanel.tsx 不存在
- src/app/layout/BottomPanel.tsx 不存在
- git 历史保留完整
</success_criteria>

<output>
After completion, create `.planning/phases/08-core-layout-refactor/08-02-SUMMARY.md`
</output>
