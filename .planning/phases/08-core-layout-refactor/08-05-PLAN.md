---
phase: 08-core-layout-refactor
plan: 05
type: execute
wave: 2
depends_on: [08-01, 08-02]
files_modified:
  - src/app/layout/LeftSidebar.tsx
  - src/app/components/SidebarTopActions/SidebarTopActions.tsx
  - src/app/components/SidebarTopActions/SidebarTopActions.module.css
autonomous: true
requirements: [LAYOUT-02]

must_haves:
  truths:
    - "左侧栏顶部包含操作区（New session/Search/Customize）"
    - "左侧栏宽度固定为 360px"
    - "左侧栏包含 Projects 列表和 Sessions 列表"
  artifacts:
    - path: "src/app/layout/LeftSidebar.tsx"
      provides: "重写的左侧栏组件"
      min_lines: 50
    - path: "src/app/components/SidebarTopActions/SidebarTopActions.tsx"
      provides: "顶部操作区组件"
      min_lines: 30
  key_links:
    - from: "LeftSidebar"
      to: "SidebarTopActions"
      via: "component composition"
      pattern: "<SidebarTopActions"
    - from: "SidebarTopActions"
      to: "appShellStore.createProjectSession"
      via: "New session button"
      pattern: "createProjectSession"
---

<objective>
重写 LeftSidebar 组件，添加顶部操作区，调整为 360px 固定宽度

Purpose: 实现官方 UI 的左侧栏布局，包含顶部操作区和会话列表
Output: 新的 LeftSidebar 和 SidebarTopActions 组件
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-core-layout-refactor/08-RESEARCH.md
@src/app/layout/LeftSidebar.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: 创建 SidebarTopActions 组件</name>
  <files>src/app/components/SidebarTopActions/SidebarTopActions.tsx, src/app/components/SidebarTopActions/SidebarTopActions.module.css</files>
  <action>
创建 SidebarTopActions 组件（参考 RESEARCH.md Phase 4）：

SidebarTopActions.tsx:
```typescript
import { Plus, Search, Settings } from 'lucide-react'
import { useAppShellStore } from '../../state/appShellStore'
import styles from './SidebarTopActions.module.css'

export function SidebarTopActions() {
  const { createProjectSession } = useAppShellStore()

  const handleNewSession = () => {
    createProjectSession().catch(() => undefined)
  }

  const handleSearch = () => {
    // Stub for Phase 9
    console.log('Search clicked - to be implemented in Phase 9')
  }

  const handleCustomize = () => {
    // Stub for Phase 9
    console.log('Customize clicked - to be implemented in Phase 9')
  }

  return (
    <div className={styles.topActions}>
      <button
        onClick={handleNewSession}
        className={styles.primaryButton}
        aria-label="New session"
      >
        <Plus size={20} />
        <span>New session</span>
      </button>
      <div className={styles.iconButtons}>
        <button
          onClick={handleSearch}
          className={styles.iconButton}
          aria-label="Search"
        >
          <Search size={20} />
        </button>
        <button
          onClick={handleCustomize}
          className={styles.iconButton}
          aria-label="Customize"
        >
          <Settings size={20} />
        </button>
      </div>
    </div>
  )
}
```

SidebarTopActions.module.css:
```css
.topActions {
  display: flex;
  flex-direction: column;
  gap: 8px;
  padding: 12px;
  border-bottom: 1px solid var(--border-subtle);
}

.primaryButton {
  display: flex;
  align-items: center;
  gap: 8px;
  padding: 8px 12px;
  border: none;
  border-radius: 8px;
  background: var(--primary-bg);
  color: var(--primary-text);
  font-size: 14px;
  font-weight: 500;
  cursor: pointer;
  transition: background-color 0.2s ease-out;
}

.primaryButton:hover {
  background: var(--primary-bg-hover);
}

.iconButtons {
  display: flex;
  gap: 4px;
}

.iconButton {
  width: 36px;
  height: 36px;
  display: flex;
  align-items: center;
  justify-content: center;
  border: none;
  border-radius: 6px;
  background: transparent;
  color: var(--text-secondary);
  cursor: pointer;
  transition: background-color 0.2s ease-out;
}

.iconButton:hover {
  background: var(--hover-bg);
}
```
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <done>SidebarTopActions 组件已创建，包含 New session/Search/Customize 按钮</done>
</task>

<task type="auto">
  <name>Task 2: 重写 LeftSidebar 组件</name>
  <files>src/app/layout/LeftSidebar.tsx</files>
  <action>
重写 LeftSidebar.tsx，添加 SidebarTopActions，保留 Projects 和 Sessions 列表：

```typescript
import { SidebarTopActions } from '../components/SidebarTopActions/SidebarTopActions'
import { useAppShellStore } from '../state/appShellStore'
// ... 保留其他必要的 imports

export function LeftSidebar() {
  const {
    activeProjectPath,
    recentProjects,
    sessionHistory,
    resumeSession,
    sidebarProjectsExpanded,
    sidebarSessionsExpanded,
    setSidebarProjectsExpanded,
    setSidebarSessionsExpanded,
  } = useAppShellStore()

  return (
    <div className="left-sidebar">
      <SidebarTopActions />
      
      {/* Projects 列表 - 保留现有逻辑 */}
      <div className="left-sidebar__section">
        <button
          className="left-sidebar__section-header"
          onClick={() => setSidebarProjectsExpanded(!sidebarProjectsExpanded)}
        >
          <span>Projects</span>
          {/* 展开/折叠图标 */}
        </button>
        {sidebarProjectsExpanded && (
          <div className="left-sidebar__projects">
            {/* 保留现有 Projects 渲染逻辑 */}
          </div>
        )}
      </div>

      {/* Sessions 列表 - 保留现有逻辑 */}
      <div className="left-sidebar__section">
        <button
          className="left-sidebar__section-header"
          onClick={() => setSidebarSessionsExpanded(!sidebarSessionsExpanded)}
        >
          <span>Sessions</span>
        </button>
        {sidebarSessionsExpanded && (
          <div className="left-sidebar__sessions">
            {/* 保留现有 Sessions 渲染逻辑 */}
          </div>
        )}
      </div>
    </div>
  )
}
```

在 app-shell.css 中更新 LeftSidebar 样式：
```css
.left-sidebar {
  display: flex;
  flex-direction: column;
  width: 360px;
  height: 100%;
  background: var(--bg-secondary);
  overflow-y: auto;
}

.left-sidebar__section {
  display: flex;
  flex-direction: column;
}

.left-sidebar__section-header {
  display: flex;
  align-items: center;
  justify-content: space-between;
  padding: 12px 16px;
  border: none;
  background: transparent;
  color: var(--text-primary);
  font-size: 14px;
  font-weight: 600;
  cursor: pointer;
  text-align: left;
}

.left-sidebar__section-header:hover {
  background: var(--hover-bg);
}
```

保留现有的项目选择器和会话列表渲染逻辑，只添加顶部操作区。
  </action>
  <verify>
    <automated>npx tsc --noEmit && grep -q "SidebarTopActions" src/app/layout/LeftSidebar.tsx</automated>
  </verify>
  <done>LeftSidebar 已重写，包含顶部操作区和现有的 Projects/Sessions 列表</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| N/A | 纯 UI 组件，无信任边界 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-06 | Denial of Service | 快速点击 New session 按钮 | mitigate | createProjectSession 已有防重入保护（检查 activeSession） |
</threat_model>

<verification>
1. SidebarTopActions 组件存在并包含三个按钮
2. LeftSidebar 在顶部渲染 SidebarTopActions
3. LeftSidebar 保留 Projects 和 Sessions 列表
4. 左侧栏宽度为 360px（在 app-shell.css 中定义）
5. TypeScript 编译通过
</verification>

<success_criteria>
- 左侧栏顶部显示 New session/Search/Customize 按钮
- 左侧栏宽度固定为 360px
- Projects 和 Sessions 列表正常显示
- New session 按钮可以创建新会话
- Search 和 Customize 按钮显示占位提示（Phase 9 实现）
</success_criteria>

<output>
After completion, create `.planning/phases/08-core-layout-refactor/08-05-SUMMARY.md`
</output>
