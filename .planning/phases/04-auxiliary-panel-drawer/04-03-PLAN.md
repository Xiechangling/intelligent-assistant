---
phase: 04-auxiliary-panel-drawer
plan: 03
type: execute
wave: 2
depends_on: [04-01]
files_modified:
  - src/styles/app-shell.css
  - src/app/layout/CenterWorkspace.tsx
  - src/app/layout/RightPanel.tsx
autonomous: true
requirements: [REQ-12]
must_haves:
  truths:
    - Buttons match official style (height, padding, border-radius, colors)
    - Input fields match official style (height, padding, focus states)
    - Cards match official style (border, background, spacing)
    - List items match official style (hover states, active states)
  artifacts:
    - path: src/styles/app-shell.css
      provides: Aligned component styles
      contains: ".workspace__primary-action"
  key_links:
    - from: src/app/layout/CenterWorkspace.tsx
      to: src/styles/app-shell.css
      via: "button and input classes"
      pattern: "workspace__primary-action|workspace__input"
---

<objective>
对齐按钮、输入框、卡片、列表项等组件样式与官方 Claude Code Desktop 一致。

Purpose: 提升视觉一致性，确保所有交互组件的样式与官方设计系统对齐，提供统一的用户体验。

Output: 样式对齐的组件库，所有按钮、输入框、卡片、列表项使用统一的设计 token 和视觉风格。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/research/VISUAL-DESIGN.md
@.planning/phases/01-topbar-visual-foundation/01-01-SUMMARY.md
</context>

<interfaces>
<!-- Official design tokens from Phase 1 -->

From src/styles/app-shell.css:
```css
:root {
  --color-permission: #b1b9f9;
  --color-inactive: #999999;
  --color-subtle: #505050;
  --color-success: #4eba65;
  --space-1: 8px;
  --space-2: 16px;
  --space-3: 24px;
  --space-4: 32px;
  --radius-sm: 10px;
  --radius-md: 14px;
  --radius-lg: 18px;
}
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Align button styles</name>
  <files>src/styles/app-shell.css</files>
  <action>
Update button styles to match official design:

1. Primary action buttons (`.workspace__primary-action`, `.right-panel__inline-action`):
   - Height: 36px (consistent with official)
   - Padding: 0 var(--space-2) (16px horizontal)
   - Border-radius: var(--radius-sm) (10px)
   - Background: var(--accent) (#7d98ff)
   - Color: var(--text-primary)
   - Font-size: 14px
   - Font-weight: 500
   - Border: none
   - Hover: background var(--accent-strong) (#96abff)
   - Active: transform translateY(1px), opacity 0.9
   - Disabled: background var(--color-inactive), cursor not-allowed

2. Secondary action buttons (`.workspace__secondary-action`):
   - Height: 36px
   - Padding: 0 var(--space-2)
   - Border-radius: var(--radius-sm)
   - Background: transparent
   - Color: var(--text-secondary)
   - Border: 1px solid var(--border-subtle)
   - Hover: border-color var(--border-highlight), background rgba(255,255,255,0.03)
   - Active: transform translateY(1px)

3. Icon buttons (`.toolbar__action`, `.sidebar__action`):
   - Width: 32px, height: 32px
   - Padding: 0
   - Border-radius: var(--radius-sm)
   - Background: transparent
   - Hover: background rgba(255,255,255,0.06)
   - Active: background rgba(255,255,255,0.08)

Implementation:
```css
.workspace__primary-action,
.right-panel__inline-action {
  height: 36px;
  padding: 0 var(--space-2);
  border-radius: var(--radius-sm);
  background: var(--accent);
  color: var(--text-primary);
  font-size: 14px;
  font-weight: 500;
  border: none;
  cursor: pointer;
}

.workspace__primary-action:hover,
.right-panel__inline-action:hover {
  background: var(--accent-strong);
}

.workspace__primary-action:active,
.right-panel__inline-action:active {
  transform: translateY(1px);
  opacity: 0.9;
}

.workspace__primary-action:disabled,
.right-panel__inline-action:disabled {
  background: var(--color-inactive);
  cursor: not-allowed;
  opacity: 0.5;
}

.workspace__secondary-action {
  height: 36px;
  padding: 0 var(--space-2);
  border-radius: var(--radius-sm);
  background: transparent;
  color: var(--text-secondary);
  border: 1px solid var(--border-subtle);
  font-size: 14px;
  cursor: pointer;
}

.workspace__secondary-action:hover {
  border-color: var(--border-highlight);
  background: rgba(255, 255, 255, 0.03);
}

.toolbar__action,
.sidebar__action {
  width: 32px;
  height: 32px;
  padding: 0;
  border-radius: var(--radius-sm);
  background: transparent;
  border: none;
  cursor: pointer;
  display: flex;
  align-items: center;
  justify-content: center;
}

.toolbar__action:hover,
.sidebar__action:hover {
  background: rgba(255, 255, 255, 0.06);
}

.toolbar__action:active,
.sidebar__action:active {
  background: rgba(255, 255, 255, 0.08);
}
```
  </action>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <done>All button styles aligned with official design, consistent heights, padding, border-radius, hover/active states</done>
</task>

<task type="auto">
  <name>Task 2: Align input and textarea styles</name>
  <files>src/styles/app-shell.css</files>
  <action>
Update input and textarea styles to match official design:

1. Text inputs (`.workspace__input`, `.right-panel__credential-input`):
   - Min-height: 36px
   - Padding: var(--space-1) var(--space-2) (8px 16px)
   - Border-radius: var(--radius-sm) (10px)
   - Background: var(--bg-elevated)
   - Color: var(--text-primary)
   - Border: 1px solid var(--border-subtle)
   - Font-size: 14px
   - Focus: border-color var(--accent), outline as per global focus-visible
   - Placeholder: color var(--text-tertiary)

2. Textarea (`.workspace__prompt-input`):
   - Min-height: 80px
   - Padding: var(--space-2) (16px all sides)
   - Border-radius: var(--radius-md) (14px)
   - Background: var(--bg-elevated)
   - Border: 1px solid var(--border-subtle)
   - Font-size: 14px
   - Line-height: 1.5
   - Resize: vertical
   - Focus: border-color var(--accent)

3. Select dropdowns (`.toolbar__model-select`):
   - Height: 36px
   - Padding: 0 var(--space-2)
   - Border-radius: var(--radius-sm)
   - Background: var(--bg-elevated)
   - Border: 1px solid var(--border-subtle)
   - Font-size: 14px
   - Hover: border-color var(--border-highlight)

Implementation:
```css
.workspace__input,
.right-panel__credential-input,
input[type="text"],
input[type="password"] {
  min-height: 36px;
  padding: var(--space-1) var(--space-2);
  border-radius: var(--radius-sm);
  background: var(--bg-elevated);
  color: var(--text-primary);
  border: 1px solid var(--border-subtle);
  font-size: 14px;
  width: 100%;
}

.workspace__input::placeholder,
.right-panel__credential-input::placeholder {
  color: var(--text-tertiary);
}

.workspace__input:focus,
.right-panel__credential-input:focus {
  border-color: var(--accent);
}

.workspace__prompt-input,
textarea {
  min-height: 80px;
  padding: var(--space-2);
  border-radius: var(--radius-md);
  background: var(--bg-elevated);
  color: var(--text-primary);
  border: 1px solid var(--border-subtle);
  font-size: 14px;
  line-height: 1.5;
  resize: vertical;
  width: 100%;
}

.workspace__prompt-input:focus,
textarea:focus {
  border-color: var(--accent);
}

.toolbar__model-select,
select {
  height: 36px;
  padding: 0 var(--space-2);
  border-radius: var(--radius-sm);
  background: var(--bg-elevated);
  color: var(--text-primary);
  border: 1px solid var(--border-subtle);
  font-size: 14px;
  cursor: pointer;
}

.toolbar__model-select:hover,
select:hover {
  border-color: var(--border-highlight);
}
```
  </action>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <done>All input styles aligned, consistent heights, padding, border-radius, focus states match official design</done>
</task>

<task type="auto">
  <name>Task 3: Align card and list item styles</name>
  <files>src/styles/app-shell.css</files>
  <action>
Update card and list item styles to match official design:

1. Inline cards (`.workspace__inline-surface`):
   - Padding: var(--space-2) (16px all sides)
   - Border-radius: var(--radius-md) (14px)
   - Background: var(--bg-elevated)
   - Border: 1px solid var(--border-subtle)
   - Clickable cards: hover border-color var(--border-highlight)

2. Settings list items (`.right-panel__settings-item`):
   - Padding: var(--space-2) (16px all sides)
   - Border-radius: var(--radius-sm) (10px)
   - Background: transparent
   - Border: 1px solid transparent
   - Hover: background rgba(255,255,255,0.03), border-color var(--border-subtle)
   - Active: background var(--bg-accent-soft), border-color var(--accent)

3. Session list items (`.sidebar__session-item`):
   - Padding: var(--space-1) var(--space-2) (8px 16px)
   - Border-radius: var(--radius-sm) (10px)
   - Background: transparent
   - Hover: background rgba(255,255,255,0.04)
   - Active: background var(--bg-accent-soft), color var(--accent-strong)

4. Review file items (`.bottom-panel__review-file`):
   - Padding: var(--space-1) var(--space-2)
   - Border-radius: var(--radius-sm)
   - Background: transparent
   - Hover: background rgba(255,255,255,0.04)
   - Active: background var(--bg-accent-soft), border-left 3px solid var(--accent)

Implementation:
```css
.workspace__inline-surface {
  padding: var(--space-2);
  border-radius: var(--radius-md);
  background: var(--bg-elevated);
  border: 1px solid var(--border-subtle);
}

.workspace__inline-surface--clickable:hover {
  border-color: var(--border-highlight);
  background: linear-gradient(135deg, 
    var(--bg-elevated) 0%, 
    rgba(255, 255, 255, 0.02) 100%);
}

.right-panel__settings-item {
  padding: var(--space-2);
  border-radius: var(--radius-sm);
  background: transparent;
  border: 1px solid transparent;
  cursor: pointer;
  text-align: left;
  width: 100%;
}

.right-panel__settings-item:hover {
  background: rgba(255, 255, 255, 0.03);
  border-color: var(--border-subtle);
}

.right-panel__settings-item--active {
  background: var(--bg-accent-soft);
  border-color: var(--accent);
}

.sidebar__session-item {
  padding: var(--space-1) var(--space-2);
  border-radius: var(--radius-sm);
  background: transparent;
  cursor: pointer;
  text-align: left;
  width: 100%;
  border: none;
}

.sidebar__session-item:hover {
  background: rgba(255, 255, 255, 0.04);
}

.sidebar__session-item--active {
  background: var(--bg-accent-soft);
  color: var(--accent-strong);
}

.bottom-panel__review-file {
  padding: var(--space-1) var(--space-2);
  border-radius: var(--radius-sm);
  background: transparent;
  cursor: pointer;
  text-align: left;
  width: 100%;
  border: none;
  border-left: 3px solid transparent;
}

.bottom-panel__review-file:hover {
  background: rgba(255, 255, 255, 0.04);
}

.bottom-panel__review-file--active {
  background: var(--bg-accent-soft);
  border-left-color: var(--accent);
}
```
  </action>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <done>All card and list item styles aligned, consistent padding, border-radius, hover/active states match official design</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CSS → UI rendering | Style changes affect visual presentation only |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-06 | Denial of Service | CSS transitions | accept | CSS transitions are browser-optimized, no performance risk |
</threat_model>

<verification>
Manual verification:
1. Check all primary buttons (submit, save, approve) have consistent height (36px) and styling
2. Check all secondary buttons have consistent border and hover states
3. Check all icon buttons have consistent size (32x32) and hover states
4. Check all text inputs have consistent height (36px) and focus states
5. Check textarea has proper padding and focus state
6. Check inline cards have consistent padding and border-radius
7. Check settings list items have proper hover/active states
8. Check session list items have proper hover/active states
9. Check review file items have proper hover/active states with left border
10. Verify all components use design tokens (--space-*, --radius-*, --color-*)

Build verification:
- npm run build passes
- No CSS syntax errors
- No visual regression in existing components
</verification>

<success_criteria>
- All buttons use consistent heights (36px for action buttons, 32px for icon buttons)
- All inputs use consistent heights (36px) and padding
- All cards use consistent border-radius (10-14px) and padding (16px)
- All list items use consistent hover/active states
- All components use design tokens from Phase 1
- Visual consistency across all interactive elements
- No layout shifts or visual glitches
</success_criteria>

<output>
After completion, create `.planning/phases/04-auxiliary-panel-drawer/04-03-SUMMARY.md`
</output>
