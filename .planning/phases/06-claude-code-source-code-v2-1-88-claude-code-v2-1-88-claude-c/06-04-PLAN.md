---
phase: 06-claude-code-source-code-v2-1-88-claude-code-v2-1-88-claude-c
plan: 04
type: execute
wave: 4
depends_on:
  - 06-01
  - 06-02
  - 06-03
files_modified:
  - package.json
  - playwright.config.ts
  - tests/e2e/startup.spec.ts
  - tests/e2e/chooser.spec.ts
  - tests/e2e/status-system.spec.ts
  - tests/e2e/approval-flow.spec.ts
  - tests/e2e/review-flow.spec.ts
  - .planning/phases/06-claude-code-source-code-v2-1-88-claude-code-v2-1-88-claude-c/06-04-VERIFICATION-CHECKLIST.txt
autonomous: false
requirements:
  - PH6-01
  - PH6-02
  - PH6-03
  - PH6-04
  - PH6-05
  - PH6-06
  - PH6-07
user_setup:
  - service: playwright
    why: Automated validation of startup, chooser, status, approval, and review flows for the redesigned workbench
    env_vars: []
    dashboard_config: []
must_haves:
  truths:
    - The Phase 6 redesign has repeatable validation for startup, chooser, status-system, approval, and review flows.
    - The repo can build after the redesign and the desktop shell can be manually verified in a Tauri window.
    - Manual verification steps are concrete and reference exact UI states and expected copy.
  artifacts:
    - path: package.json
      provides: Validation dependency and scripts for Playwright if missing
    - path: playwright.config.ts
      provides: Baseline Phase 6 E2E configuration
    - path: tests/e2e/startup.spec.ts
      provides: Startup/recovery/no-workspace flow checks
    - path: tests/e2e/chooser.spec.ts
      provides: Chooser/workspace/session row checks
    - path: tests/e2e/status-system.spec.ts
      provides: Canonical status label checks across regions
    - path: tests/e2e/approval-flow.spec.ts
      provides: Approval-to-output tray workflow checks
    - path: tests/e2e/review-flow.spec.ts
      provides: Review-ready tray/file-rail/diff checks
    - path: .planning/phases/06-claude-code-source-code-v2-1-88-claude-code-v2-1-88-claude-c/06-04-VERIFICATION-CHECKLIST.txt
      provides: Manual desktop verification steps for the executor and human reviewer
  key_links:
    - from: tests/e2e/startup.spec.ts
      to: src/app/layout/CenterWorkspace.tsx
      via: startup/recovery/chooser assertions
      pattern: Resume session|Open session chooser|No workspace selected
    - from: tests/e2e/status-system.spec.ts
      to: src/app/layout/TopToolbar.tsx
      via: exact canonical status-label assertions
      pattern: Ready|Connecting|Connected|Attached|Working|Awaiting approval|Review ready|Failed
    - from: tests/e2e/review-flow.spec.ts
      to: src/app/layout/BottomPanel.tsx
      via: review-ready file rail and diff preview assertions
      pattern: Open review|No changed files are available yet
---

<objective>
Finish Phase 6 with executable validation, missing-tool installation, build verification, and a precise manual verification checklist for the redesigned desktop workbench.

Purpose: Make the redesign shippable and testable per the approved design’s tooling/verification requirements, including Playwright.
Output: Validation tooling, E2E spec scaffolds, build commands, and a blocking manual verification checklist for startup/chooser/session/approval/review behavior.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@E:/work/ai/agent/.planning/phases/06-claude-code-source-code-v2-1-88-claude-code-v2-1-88-claude-c/06-RESEARCH.md
@E:/work/ai/agent/.planning/phases/06-claude-code-source-code-v2-1-88-claude-code-v2-1-88-claude-c/06-UI-SPEC.md
@E:/work/ai/agent/docs/superpowers/specs/2026-04-06-claude-code-desktop-alignment-design.md
@E:/work/ai/agent/package.json
@E:/work/ai/agent/src/app/layout/CenterWorkspace.tsx
@E:/work/ai/agent/src/app/layout/TopToolbar.tsx
@E:/work/ai/agent/src/app/layout/BottomPanel.tsx

<interfaces>
Validation must assert exact copy and state labels introduced in Plans 01-03, especially:
- `No workspace selected`
- `Resume session`
- `Open session chooser`
- `Start new session`
- `Approve and run`
- `Reject command`
- `Open review`
- canonical status chip labels
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install validation tooling and add executable Phase 6 E2E coverage</name>
  <read_first>
    - E:/work/ai/agent/package.json
    - E:/work/ai/agent/.planning/phases/06-claude-code-source-code-v2-1-88-claude-code-v2-1-88-claude-c/06-RESEARCH.md
    - E:/work/ai/agent/.planning/phases/06-claude-code-source-code-v2-1-88-claude-code-v2-1-88-claude-c/06-UI-SPEC.md
  </read_first>
  <files>package.json, playwright.config.ts, tests/e2e/startup.spec.ts, tests/e2e/chooser.spec.ts, tests/e2e/status-system.spec.ts, tests/e2e/approval-flow.spec.ts, tests/e2e/review-flow.spec.ts</files>
  <action>Inspect the existing repo tooling first, then install only what is missing for Phase 6 validation. If Playwright is not already present, add it as a dev dependency and create `playwright.config.ts` plus E2E specs under `tests/e2e/`. The specs must cover exact Phase 6 behavior: startup recovery card vs no-workspace state, chooser row content and actions, canonical toolbar/status labels, approval tray auto-focus with `Approve and run` / `Reject command`, and review tray split layout with changed-file rail and diff preview. Use deterministic fixtures/mocks or controlled local state setup appropriate to the current app architecture; do not add flaky tests that depend on live Anthropic calls.</action>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <done>The repo contains runnable Phase 6 validation scaffolding with explicit specs for startup, chooser, status system, approval flow, and review flow.</done>
  <acceptance_criteria>
    - Playwright is installed only if missing.
    - Each required Phase 6 flow has its own spec file.
    - Specs assert exact user-facing labels introduced by Plans 01-03 instead of generic existence checks.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Wire build and verification commands for executor use</name>
  <read_first>
    - E:/work/ai/agent/package.json
    - E:/work/ai/agent/playwright.config.ts
    - E:/work/ai/agent/tests/e2e/startup.spec.ts
  </read_first>
  <files>package.json</files>
  <action>Update `package.json` with the minimum scripts needed to run the new validation stack from the terminal. Preserve existing scripts, add Playwright commands only if they do not already exist, and keep `npm run build` as the baseline required gate. Include a script for the full Phase 6 browser validation suite and, if useful, targeted scripts for startup/chooser/approval/review checks. Do not add scripts that assume a globally installed Tauri CLI; prefer repo-local npm entry points consistent with current tooling.</action>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <done>Executors can run build and the new Phase 6 UI validation from package scripts without guessing command names.</done>
  <acceptance_criteria>
    - Existing scripts remain intact.
    - New scripts reference local repo tooling rather than undocumented global binaries.
    - `npm run build` stays the universal gate for every execution attempt.
  </acceptance_criteria>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Run build and desktop verification, then collect manual approval</name>
  <read_first>
    - E:/work/ai/agent/.planning/phases/06-claude-code-source-code-v2-1-88-claude-code-v2-1-88-claude-c/06-UI-SPEC.md
    - E:/work/ai/agent/docs/superpowers/specs/2026-04-06-claude-code-desktop-alignment-design.md
    - E:/work/ai/agent/tests/e2e/startup.spec.ts
    - E:/work/ai/agent/tests/e2e/approval-flow.spec.ts
    - E:/work/ai/agent/tests/e2e/review-flow.spec.ts
  </read_first>
  <what-built>Complete validation/tooling pass for the Phase 6 desktop alignment, including build gate, Playwright coverage, and a manual desktop verification checklist.</what-built>
  <files>.planning/phases/06-claude-code-source-code-v2-1-88-claude-code-v2-1-88-claude-c/06-04-VERIFICATION-CHECKLIST.txt</files>
  <action>Create `.planning/phases/06-claude-code-source-code-v2-1-88-claude-code-v2-1-88-claude-c/06-04-VERIFICATION-CHECKLIST.txt` containing exact numbered manual checks for: startup recovery path, no-workspace entry path, chooser row content, attached session header, toolbar canonical status labels, approval tray command/workspace/working-directory visibility, output waiting text, review-ready file rail/diff preview, and right-panel support-only role. Then run the automated build and validation commands added in Tasks 1-2, launch the desktop app through the repo-local Tauri script, and use the checklist to verify the flows manually before pausing for human sign-off.</action>
  <verify>
    <automated>npm run build && npm run test:e2e</automated>
  </verify>
  <done>Automated validation has run, the desktop shell has been manually checked against the checklist, and the executor is ready for final human approval of the aligned workbench.</done>
  <acceptance_criteria>
    - Checklist file exists with explicit numbered manual steps and expected results.
    - Automated commands are runnable from `package.json` and complete before the checkpoint.
    - Human verification focuses only on confirming the finished work, not performing setup or implementation steps.
  </acceptance_criteria>
  <how-to-verify>
    1. Run `npm run build` and the full Phase 6 E2E suite from the repo root.
    2. Launch the desktop app with the repo-local Tauri script defined in `package.json`.
    3. Follow `.planning/phases/06-claude-code-source-code-v2-1-88-claude-code-v2-1-88-claude-c/06-04-VERIFICATION-CHECKLIST.txt` exactly.
    4. Confirm the UI uses canonical labels (`Ready`, `Connected`, `Attached`, `Working`, `Awaiting approval`, `Review ready`, `Failed`) and exact CTA copy (`Resume session`, `Open session chooser`, `Approve and run`, `Reject command`, `Open review`).
    5. Confirm the right panel is supportive only and the bottom tray owns detailed approval/output/review workflow.
  </how-to-verify>
  <resume-signal>Type "approved" if the manual verification passes, or describe the failing step and observed mismatch.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Test harness -> UI | Automated validation asserts user-facing behavior and must not rely on live secret-backed services. |
| package scripts -> local environment | Build and test commands execute local tooling that may be missing or misconfigured. |
| Manual verification -> release confidence | Human reviewer relies on the checklist to confirm final workflow accuracy. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-13 | T | tests/e2e/*.spec.ts | mitigate | Use deterministic local setup or mocks so tests validate UI contracts rather than mutating live external systems. |
| T-06-14 | I | package.json / test config | mitigate | Do not inject secrets into Playwright or test output; keep validation independent of plaintext credential values. |
| T-06-15 | R | 06-04-VERIFICATION-CHECKLIST.txt | mitigate | Write exact numbered checks with expected copy/state so human verification leaves an auditable acceptance trail. |
| T-06-16 | D | package scripts | accept | Tool installation can fail on some machines; provide repo-local commands and keep `npm run build` as the minimal fallback gate. |
</threat_model>

<verification>
Required automated gate: `npm run build && npm run test:e2e`. Required manual gate: run the Tauri app and complete the checklist covering startup, chooser, session, approval, output, review, and support-panel behavior.
</verification>

<success_criteria>
- Phase 6 has explicit tooling for repeatable validation.
- Build and E2E commands are discoverable from `package.json`.
- Human reviewer gets a concrete checklist for final desktop verification.
</success_criteria>

<output>
After completion, create `.planning/phases/06-claude-code-source-code-v2-1-88-claude-code-v2-1-88-claude-c/06-04-SUMMARY.md`
</output>
