1. Launch the desktop app with `npm run tauri:dev` from `E:/work/ai/agent`.
2. Startup recovery path: confirm a recovery card appears when a restorable session exists, with exact CTAs `Resume session` and `Open session chooser`.
3. No-workspace entry path: confirm the center workspace shows heading `No workspace selected`, body text `Open a local workspace to resume a coding session, attach to recent work, or start a new session.`, and primary CTA `Open workspace`.
4. Chooser row content: in project mode with a workspace selected, confirm the chooser shows current workspace summary, a resumable spotlight when applicable, recent session rows with title, workspace label, model, last activity, lifecycle status, and summary line.
5. Chooser actions: confirm the chooser exposes exact actions `Resume`, `Attach` when relevant, and `Start new session`.
6. Attached session header: open a project session and confirm the session header shows title, workspace, model, last activity, and a canonical state chip.
7. Toolbar status labels: confirm the top toolbar only uses canonical labels `Ready`, `Connecting`, `Connected`, `Attached`, `Working`, `Awaiting approval`, `Review ready`, and `Failed`.
8. Approval tray behavior: trigger an approval-required command and confirm the bottom tray auto-expands, shows command summary, exact command string, `Workspace path`, `Working directory`, and CTAs `Approve and run` and `Reject command`.
9. Output waiting text: after approval and before output arrives, confirm the bottom tray output surface shows exact text `Waiting for execution output.` when appropriate.
10. Review-ready behavior: after an execution with changed files, confirm transcript and toolbar indicate `Review ready`, the bottom tray switches to review, and the inline summary offers `Open review`.
11. Review split layout: confirm the review tray shows a left changed-file rail and right diff preview; selecting a file updates the diff preview.
12. Review empty state: confirm exact review empty text `No changed files are available yet.` appears when no changed files exist.
13. Right panel support-only role: open the right panel and confirm it contains supportive context/settings metadata only, and that primary approval/output/review actions remain in center workspace or bottom tray.
14. Record pass/fail for each step above and note any mismatched copy, state label, or panel ownership behavior.
