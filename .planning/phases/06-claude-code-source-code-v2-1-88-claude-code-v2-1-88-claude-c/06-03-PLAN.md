---
phase: 06-claude-code-source-code-v2-1-88-claude-code-v2-1-88-claude-c
plan: 03
type: execute
wave: 3
depends_on:
  - 06-01
  - 06-02
files_modified:
  - src/app/layout/TopToolbar.tsx
  - src/app/layout/LeftSidebar.tsx
  - src/app/layout/RightPanel.tsx
  - src/app/layout/BottomPanel.tsx
  - src/styles/app-shell.css
autonomous: true
requirements:
  - PH6-03
  - PH6-04
  - PH6-05
must_haves:
  truths:
    - Toolbar, sidebar, right panel, and bottom tray all use the same canonical workflow language.
    - Approval, output, and review feel like one lifecycle with structured tray behavior.
    - Supporting surfaces reinforce the center workspace instead of competing with it.
  artifacts:
    - path: src/app/layout/TopToolbar.tsx
      provides: Canonical context strip with one top-level status chip and workspace/session summary
    - path: src/app/layout/LeftSidebar.tsx
      provides: Workspace/session navigation with attention markers and current selection
    - path: src/app/layout/RightPanel.tsx
      provides: Supportive context/settings surfaces with compact desktop wording
    - path: src/app/layout/BottomPanel.tsx
      provides: Unified approval/output/review lifecycle tray
    - path: src/styles/app-shell.css
      provides: Shared token normalization for spacing, typography, color semantics, status chips, and tray/sidebar rows
  key_links:
    - from: src/app/layout/TopToolbar.tsx
      to: src/app/state/appShellStore.ts
      via: canonical desktop status and active session/workspace summary selectors
      pattern: desktopStatus|activeSession
    - from: src/app/layout/LeftSidebar.tsx
      to: src/app/state/appShellStore.ts
      via: chooser/session row metadata and attention markers
      pattern: sessionHistory|recentProjects
    - from: src/app/layout/BottomPanel.tsx
      to: src/app/state/appShellStore.ts
      via: tray mode, pending proposal, execution record, selected review file
      pattern: bottomPanel|pendingProposal|executionRecord
---

<objective>
Align every supporting shell region with the Phase 6 workflow model so the whole app reads as one official-style workbench instead of center-first redesign plus leftover MVP panels.

Purpose: Implement D-06, D-10, D-11, and D-12 while normalizing visual tokens from the UI-SPEC.
Output: Updated toolbar/sidebar/right panel/bottom tray behavior and shared CSS token normalization for spacing, typography, and status semantics.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@E:/work/ai/agent/.planning/phases/06-claude-code-source-code-v2-1-88-claude-code-v2-1-88-claude-c/06-CONTEXT.md
@E:/work/ai/agent/.planning/phases/06-claude-code-source-code-v2-1-88-claude-code-v2-1-88-claude-c/06-UI-SPEC.md
@E:/work/ai/agent/docs/superpowers/specs/2026-04-06-claude-code-desktop-alignment-design.md
@E:/work/ai/agent/src/app/layout/TopToolbar.tsx
@E:/work/ai/agent/src/app/layout/LeftSidebar.tsx
@E:/work/ai/agent/src/app/layout/RightPanel.tsx
@E:/work/ai/agent/src/app/layout/BottomPanel.tsx
@E:/work/ai/agent/src/styles/app-shell.css
@E:/work/ai/agent/src/app/state/appShellStore.ts

<interfaces>
Consume from prior plans:
- canonical `DesktopWorkflowStatus`
- chooser/session attention metadata
- derived tray mode and active-session header data

Supporting surface rules:
- TopToolbar shows one canonical status chip only.
- LeftSidebar shows navigation and attention markers only.
- RightPanel stays supportive context/settings, not a primary flow surface.
- BottomPanel unifies approval, output, and review as one lifecycle tray.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Normalize the toolbar and sidebar around canonical status and navigation semantics</name>
  <read_first>
    - E:/work/ai/agent/src/app/layout/TopToolbar.tsx
    - E:/work/ai/agent/src/app/layout/LeftSidebar.tsx
    - E:/work/ai/agent/src/app/state/appShellStore.ts
    - E:/work/ai/agent/.planning/phases/06-claude-code-source-code-v2-1-88-claude-code-v2-1-88-claude-c/06-UI-SPEC.md
  </read_first>
  <files>src/app/layout/TopToolbar.tsx, src/app/layout/LeftSidebar.tsx, src/styles/app-shell.css</files>
  <behavior>
    - Test 1: toolbar uses one canonical chip from the exact allowed labels and never shows MVP phrases such as `Approval required`, `Execution completed`, `Responding`, or freeform execution text as the primary chip.
    - Test 2: toolbar context shows current mode, workspace name/path summary, session title or chooser context, and quick inspector/settings access per D-06.
    - Test 3: sidebar groups workspaces and recent/resumable sessions, highlights current selection, and shows attention markers for pending approval, review-ready, failure, or recovery issue.
  </behavior>
  <action>Refactor `src/app/layout/TopToolbar.tsx` and `src/app/layout/LeftSidebar.tsx` to consume the canonical selectors from Plan 01 and the center-workspace hierarchy from Plan 02. Replace `Chat`/`Code` wording with the final mode labels you choose, but keep the project-mode path clearly primary per D-09. Toolbar must show current mode, current workspace, current session title or chooser context, one canonical status chip, and quick context/settings actions only. Sidebar must shift from a simple recent list to navigation with workspace grouping, resumable/recent session rows, current selection state, and small attention badges for `Awaiting approval`, `Review ready`, `Failed`, or `Needs attention`. Do not duplicate the full chooser detail already present in the center workspace.</action>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <done>Toolbar and sidebar speak the same canonical lifecycle language as the center workspace and reinforce, rather than duplicate, the main workflow.</done>
  <acceptance_criteria>
    - Toolbar primary chip uses only the canonical labels from the UI-SPEC.
    - Sidebar rows expose current selection plus lightweight attention markers.
    - Supporting surfaces no longer use `Idle`, `Active`, `Needs review`, or `Responding` as primary user-facing states.
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Unify bottom panel approval, output, and review into one lifecycle tray</name>
  <read_first>
    - E:/work/ai/agent/src/app/layout/BottomPanel.tsx
    - E:/work/ai/agent/src/app/state/appShellStore.ts
    - E:/work/ai/agent/.planning/phases/06-claude-code-source-code-v2-1-88-claude-code-v2-1-88-claude-c/06-UI-SPEC.md
    - E:/work/ai/agent/src/app/layout/CenterWorkspace.tsx
  </read_first>
  <files>src/app/layout/BottomPanel.tsx, src/styles/app-shell.css</files>
  <behavior>
    - Test 1: pending proposal auto-expands the tray, switches to approval detail, and shows summary, command, workspace path, working directory, impact label, and approve/reject actions.
    - Test 2: approved execution switches the tray to output mode with canonical `Working` semantics and output waiting copy `Waiting for execution output.` when no lines have arrived yet.
    - Test 3: review-ready switches the tray to review mode with changed-file rail, selected diff preview, `No changed files are available yet.` empty state, and follow-up path back to the composer.
  </behavior>
  <action>Refactor `src/app/layout/BottomPanel.tsx` into the single lifecycle tray required by D-11 and the UI-SPEC. Replace generic utility-tray framing with explicit approval/output/review workflow language. Approval tab must show command summary, exact command string, workspace path, working directory, approval status, and `Approve and run` / `Reject command` controls. Output tab must keep monospace chronological output with stream labels `system`, `stdout`, and `stderr`. Review tab must present left file rail plus right diff preview, auto-select the first changed file when review becomes available, and keep review tied to the active session/execution record. Respect the store-derived tray mode instead of relying on manual tab selection alone.</action>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <done>The bottom panel now reads as the detailed lifecycle companion to the center workspace: approval first, output during work, review when artifacts are ready.</done>
  <acceptance_criteria>
    - Pending approval always auto-focuses the approval surface.
    - Execution output uses the exact waiting copy from the UI-SPEC when empty.
    - Review mode uses a split file-rail/diff-preview layout and auto-selects the first file when available.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Rebalance the right panel and normalize Phase 6 visual tokens</name>
  <read_first>
    - E:/work/ai/agent/src/app/layout/RightPanel.tsx
    - E:/work/ai/agent/src/styles/app-shell.css
    - E:/work/ai/agent/.planning/phases/06-claude-code-source-code-v2-1-88-claude-code-v2-1-88-claude-c/06-UI-SPEC.md
    - E:/work/ai/agent/docs/superpowers/specs/2026-04-06-claude-code-desktop-alignment-design.md
  </read_first>
  <files>src/app/layout/RightPanel.tsx, src/styles/app-shell.css</files>
  <action>Update `src/app/layout/RightPanel.tsx` so the `Context` view shows only supportive metadata: mode, workspace, model, credential state, active session metadata, pending approval summary, and execution summary. Keep `Settings` ownership for credentials, endpoint/base URL, presets, and workflow capabilities, but rewrite headings, helper text, button labels, and section density so it reads like compact desktop settings rather than the primary work surface. In `src/styles/app-shell.css`, normalize visible spacing to the 4-point scale, reduce typography to the UI-SPEC’s four sizes and two weights, standardize status-chip heights, and apply the required color semantics for accent/connected state, warning/approval, destructive/failure, and restrained review-ready emphasis. Do not introduce Tailwind or another design system; fix the existing CSS token layer in place per D-02 and D-04.</action>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <done>The right panel becomes a compact support surface, and the shell’s spacing/type/color tokens align with the Phase 6 visual contract instead of the MVP token drift.</done>
  <acceptance_criteria>
    - Right panel no longer carries primary workflow actions needed to continue the main task flow.
    - Visible spacing values follow the 4-point contract and remove MVP-only 10/14/18 drift where those tokens affect Phase 6 surfaces.
    - Typography and color semantics match the UI-SPEC across toolbar, sidebar, center summaries, right panel, and bottom tray.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Store -> toolbar/sidebar/right panel/tray | Canonical lifecycle state is surfaced across multiple supporting regions. |
| Approval tray -> user decision | The user approves or rejects impactful commands based on displayed command/workspace context. |
| Review tray -> changed-file data | Git-derived review artifacts are presented as follow-up workflow context. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-09 | S | src/app/layout/TopToolbar.tsx | mitigate | Use only centralized selectors for workspace/session/status so the toolbar cannot misreport current context. |
| T-06-10 | T | src/app/layout/BottomPanel.tsx | mitigate | Keep exact command string, workspace path, and working directory visible before approval so the user can verify execution context. |
| T-06-11 | R | src/app/layout/BottomPanel.tsx | mitigate | Bind review and output surfaces to the active execution record so diffs remain attributable to the session action that produced them. |
| T-06-12 | I | src/app/layout/RightPanel.tsx | mitigate | Keep secrets out of the panel by showing credential status/settings only; never render plaintext stored credentials. |
</threat_model>

<verification>
Run `npm run build`. Confirm the toolbar chip, sidebar attention markers, bottom tray labels, and right-panel context all use the same canonical vocabulary. Confirm the approval tray still shows exact command/workspace/working-directory details before execution.
</verification>

<success_criteria>
- All supporting regions align with the center workspace’s workflow language.
- Approval, output, and review read as one lifecycle.
- Shell tokens feel denser and more official without re-platforming the UI stack.
</success_criteria>

<output>
After completion, create `.planning/phases/06-claude-code-source-code-v2-1-88-claude-code-v2-1-88-claude-c/06-03-SUMMARY.md`
</output>
