---
phase: 06-claude-code-source-code-v2-1-88-claude-code-v2-1-88-claude-c
plan: 02
type: execute
wave: 2
depends_on:
  - 06-01
files_modified:
  - src/app/layout/CenterWorkspace.tsx
  - src/app/layout/AppShell.tsx
  - src/styles/app-shell.css
autonomous: true
requirements:
  - PH6-01
  - PH6-02
  - PH6-03
  - PH6-04
  - PH6-05
must_haves:
  truths:
    - On startup the user immediately sees recovery, chooser, or no-workspace state that answers where to work and what can be resumed.
    - The center workspace becomes the primary official-style surface for chooser, attached session, transcript, approval summary, and review-ready summary.
    - Conversation mode remains available but visually subordinate to the coding workspace path.
  artifacts:
    - path: src/app/layout/CenterWorkspace.tsx
      provides: Recovery callout, workspace summary, chooser, session surface, transcript hierarchy, and composer labels
    - path: src/app/layout/AppShell.tsx
      provides: Shell composition that preserves five-region layout while giving the center workspace primary emphasis
    - path: src/styles/app-shell.css
      provides: Center workspace and chooser/session surface styling needed for the redesign
  key_links:
    - from: src/app/layout/CenterWorkspace.tsx
      to: src/app/state/appShellStore.ts
      via: canonical chooser/status/session selectors from Plan 01
      pattern: useAppShellStore
    - from: src/app/layout/CenterWorkspace.tsx
      to: src/styles/app-shell.css
      via: new chooser/session/recovery class names
      pattern: workspace__chooser|workspace__session-surface|workspace__recovery
    - from: src/app/layout/AppShell.tsx
      to: src/app/layout/CenterWorkspace.tsx
      via: center region ownership preserved per D-04/D-05
      pattern: CenterWorkspace
---

<objective>
Redesign the primary center workspace into the official-style startup, chooser, and active-session surface for Phase 6 while preserving the existing five-region shell.

Purpose: Implement D-04, D-05, D-07, D-08, D-09, D-10, D-11, and D-12 in the product’s main work area.
Output: New startup/recovery/chooser/session flow in `CenterWorkspace.tsx`, any required shell composition adjustment in `AppShell.tsx`, and matching center-surface CSS.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@E:/work/ai/agent/.planning/PROJECT.md
@E:/work/ai/agent/.planning/STATE.md
@E:/work/ai/agent/.planning/phases/06-claude-code-source-code-v2-1-88-claude-code-v2-1-88-claude-c/06-CONTEXT.md
@E:/work/ai/agent/.planning/phases/06-claude-code-source-code-v2-1-88-claude-code-v2-1-88-claude-c/06-UI-SPEC.md
@E:/work/ai/agent/docs/superpowers/specs/2026-04-06-claude-code-desktop-alignment-design.md
@E:/work/ai/agent/src/app/layout/CenterWorkspace.tsx
@E:/work/ai/agent/src/app/layout/AppShell.tsx
@E:/work/ai/agent/src/styles/app-shell.css
@E:/work/ai/agent/src/app/state/appShellStore.ts

<interfaces>
Consume Plan 01 selectors/contracts for:
- canonical desktop status
- startup/recovery state
- chooser spotlight and chooser rows
- active session header model
- tray mode / attention markers

Center workspace must expose these Phase 6 sub-surfaces, whether inline or extracted locally:
- `RecoveryCallout`
- `WorkspaceSummaryCard`
- `ResumeSessionSpotlight`
- `SessionList`
- `SessionSurface`
- `InlineApprovalSummary`
- `InlineReviewSummary`
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Rebuild startup and chooser states inside CenterWorkspace</name>
  <read_first>
    - E:/work/ai/agent/src/app/layout/CenterWorkspace.tsx
    - E:/work/ai/agent/src/app/state/appShellStore.ts
    - E:/work/ai/agent/.planning/phases/06-claude-code-source-code-v2-1-88-claude-code-v2-1-88-claude-c/06-UI-SPEC.md
    - E:/work/ai/agent/docs/superpowers/specs/2026-04-06-claude-code-desktop-alignment-design.md
  </read_first>
  <files>src/app/layout/CenterWorkspace.tsx, src/styles/app-shell.css</files>
  <behavior>
    - Test 1: startup with recovery available shows a recovery card with session title, workspace, last activity, model, workflow state, `Resume session`, and `Open session chooser` per D-07/D-08.
    - Test 2: no recovery but active workspace shows a chooser surface with workspace summary, resumable spotlight if present, recent sessions list, `Start new session`, and de-emphasized conversation entry.
    - Test 3: no workspace selected shows heading `No workspace selected`, body copy from the UI-SPEC, primary CTA `Open workspace`, and subordinate conversation-only path.
  </behavior>
  <action>Refactor `src/app/layout/CenterWorkspace.tsx` from the current history list into a true Phase 6 landing surface. Preserve the file as the composition root per D-05, but restructure it around startup/recovery/chooser states from Plan 01 selectors. Implement exact copy contracts from `06-UI-SPEC.md` for `No workspace selected`, `No sessions yet`, `Session recovery failed`, `Resume session`, `Open session chooser`, `Start new session`, and `Open workspace`. Make chooser rows show session title, workspace label, model, last activity time, canonical lifecycle status, one-line recent activity summary, and attention marker for approval/review/failure. Update CSS in `src/styles/app-shell.css` only for these center-surface classes; do not redesign toolbar/sidebar/right panel/bottom panel in this task.</action>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <done>The center workspace shows recovery-first startup, a structured workspace/session chooser, and exact official-feeling copy instead of the MVP session-history list.</done>
  <acceptance_criteria>
    - Startup answers where the user is working, what can be resumed, and whether the app is ready/connected/attached.
    - Chooser rows expose `Resume`, `Attach`, or `Start new session` according to one deterministic rule from Plan 01 state.
    - Conversation-only entry remains available but visually subordinate to the project workspace flow per D-09.
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Turn the active session view into an attached coding session surface</name>
  <read_first>
    - E:/work/ai/agent/src/app/layout/CenterWorkspace.tsx
    - E:/work/ai/agent/.planning/phases/06-claude-code-source-code-v2-1-88-claude-code-v2-1-88-claude-c/06-UI-SPEC.md
    - E:/work/ai/agent/src/app/state/types.ts
    - E:/work/ai/agent/src/app/state/appShellStore.ts
  </read_first>
  <files>src/app/layout/CenterWorkspace.tsx, src/styles/app-shell.css</files>
  <behavior>
    - Test 1: active project session header shows session title, workspace name, model, canonical lifecycle chip, last activity time, and optional task/activity summary.
    - Test 2: transcript keeps user and assistant messages primary while stage/tool/approval/review events are quieter but still chronological per D-10.
    - Test 3: composer uses `Send instruction` in project mode, `Send message` in conversation mode, shows slash-command hint, and disables correctly during streaming or pending approval.
  </behavior>
  <action>Rebuild the active-session portion of `CenterWorkspace.tsx` into the four-strata SessionSurface defined in the UI-SPEC: session header, inline lifecycle summaries, transcript, and composer. Replace MVP labels such as `Code workflow`, `Coding workflow`, `Needs review`, `Responding`, and `Run task` with canonical status/copy per D-12 and the UI-SPEC. Inline approval summary must explicitly reference the bottom panel as the detailed approval surface and present `Approve and run` plus `Reject command`. Inline review summary must state `Review ready` and expose `Open review`. Keep transcript conversation-first while making stage status, execution updates, approval resolution, and review-ready events visually subordinate system surfaces rather than message bubbles.</action>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <done>The center workspace now reads as an attached coding-session workbench rather than a generic chat view with extra cards.</done>
  <acceptance_criteria>
    - Project-mode primary CTA label is `Send instruction`; conversation-mode label is `Send message`.
    - Session header and transcript use canonical lifecycle vocabulary from Plan 01.
    - Approval/review summaries are visible inline without replacing the transcript as the main reading surface.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Preserve shell hierarchy while giving the center workspace primary emphasis</name>
  <read_first>
    - E:/work/ai/agent/src/app/layout/AppShell.tsx
    - E:/work/ai/agent/src/app/layout/CenterWorkspace.tsx
    - E:/work/ai/agent/src/styles/app-shell.css
    - E:/work/ai/agent/.planning/phases/06-claude-code-source-code-v2-1-88-claude-code-v2-1-88-claude-c/06-UI-SPEC.md
  </read_first>
  <files>src/app/layout/AppShell.tsx, src/styles/app-shell.css</files>
  <action>Adjust `src/app/layout/AppShell.tsx` and related shell CSS only where necessary to preserve D-04’s five-region frame while making the center workspace read as the dominant product surface. Keep sticky toolbar, left navigation, right drawer, and bottom tray architecture intact, but rebalance widths, spacing, overflow behavior, and panel layering so the center workspace feels primary and the right drawer does not overpower it. Do not add new shell regions or move primary workflow controls into the drawer/tray.</action>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <done>The five-region shell is preserved, but the center workspace clearly owns the startup/chooser/active-session story and visually dominates the workbench.</done>
  <acceptance_criteria>
    - AppShell still renders the same five top-level regions.
    - Center workspace receives the strongest visual hierarchy and largest content emphasis.
    - No new controls in AppShell compete with the center chooser/session flow.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Store -> Center workspace | Canonical lifecycle state is presented to the user as authoritative startup/session context. |
| Center workspace -> user actions | Resume, attach, start-new, approve, reject, and send-instruction actions trigger session and execution workflows. |
| CSS hierarchy -> user attention | Visual emphasis can mislead users if approval/review/failure states are under-emphasized. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-05 | S | src/app/layout/CenterWorkspace.tsx | mitigate | Show explicit workspace/session identity and canonical status in recovery, chooser, and session header so users cannot mistake context. |
| T-06-06 | T | src/app/layout/CenterWorkspace.tsx | mitigate | Keep approval and review summaries tied to the actual store proposal/execution records; do not render hardcoded status copy. |
| T-06-07 | I | src/app/layout/CenterWorkspace.tsx | accept | Center workspace shows local session metadata already available in the app; no new secret surface is added. |
| T-06-08 | D | src/styles/app-shell.css | mitigate | Preserve transcript readability and non-blocking tray/drawer behavior so the primary workflow remains usable during approval/output/review events. |
</threat_model>

<verification>
Run `npm run build`. Before moving to Plan 03, confirm the center workspace alone can demonstrate startup recovery, no-workspace state, chooser, active session header, inline approval summary, inline review summary, canonical status labels, and the new composer CTA text.
</verification>

<success_criteria>
- Startup and chooser feel like a session-first desktop entry surface.
- Active session surface communicates attached work context, not generic chat.
- Center workspace becomes the undeniable focal region of the product.
</success_criteria>

<output>
After completion, create `.planning/phases/06-claude-code-source-code-v2-1-88-claude-code-v2-1-88-claude-c/06-02-SUMMARY.md`
</output>
