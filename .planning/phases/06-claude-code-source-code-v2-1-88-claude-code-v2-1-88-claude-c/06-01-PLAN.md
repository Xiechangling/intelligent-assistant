---
phase: 06-claude-code-source-code-v2-1-88-claude-code-v2-1-88-claude-c
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/state/types.ts
  - src/app/state/appShellStore.ts
  - src/app/services/sessionService.ts
  - src/app/services/assistantService.ts
autonomous: true
requirements:
  - PH6-01
  - PH6-02
  - PH6-03
must_haves:
  truths:
    - Desktop UI can derive one canonical workflow state instead of per-panel wording drift.
    - Startup, recovery, chooser, attached session, approval, execution, review, and failure states all exist in the store as explicit view state.
    - Every shell region can consume the same chooser/session/status selectors without inventing local mappings.
  artifacts:
    - path: src/app/state/types.ts
      provides: Canonical workflow vocabulary, chooser metadata, attention flags, tray mode, and session surface contracts
    - path: src/app/state/appShellStore.ts
      provides: Derived desktop workflow selectors and state transitions for startup, chooser, attach/resume, approval, execution, and review
    - path: src/app/services/sessionService.ts
      provides: Typed service boundary aligned with new recovery/chooser metadata if the store contract needs it
    - path: src/app/services/assistantService.ts
      provides: Typed service boundary aligned with normalized lifecycle signals consumed by the store
  key_links:
    - from: src/app/state/appShellStore.ts
      to: src/app/state/types.ts
      via: imported unions/interfaces for desktop workflow view model
      pattern: DesktopWorkflow
    - from: src/app/state/appShellStore.ts
      to: src/app/services/sessionService.ts
      via: recovery/session-history/service payload mapping
      pattern: loadRecoverySnapshot|listSessions|loadSession
    - from: src/app/state/appShellStore.ts
      to: src/app/services/assistantService.ts
      via: assistant turn and approved execution lifecycle mapping
      pattern: streamAssistantResponse|runApprovedCommand
---

<objective>
Establish the canonical desktop workflow contract for Phase 6 so every later UI refactor consumes one official-style state machine instead of re-deriving labels and status semantics.

Purpose: Implement D-01, D-02, D-07, D-10, D-11, and D-12 before any surface redesign so the new chooser/session work lands on stable contracts.
Output: Updated state/store/service contracts for canonical workflow status, chooser metadata, attention markers, recovery spotlight data, and bottom tray mode.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@E:/work/ai/agent/.planning/PROJECT.md
@E:/work/ai/agent/.planning/REQUIREMENTS.md
@E:/work/ai/agent/.planning/ROADMAP.md
@E:/work/ai/agent/.planning/STATE.md
@E:/work/ai/agent/.planning/phases/06-claude-code-source-code-v2-1-88-claude-code-v2-1-88-claude-c/06-CONTEXT.md
@E:/work/ai/agent/.planning/phases/06-claude-code-source-code-v2-1-88-claude-code-v2-1-88-claude-c/06-UI-SPEC.md
@E:/work/ai/agent/.planning/phases/06-claude-code-source-code-v2-1-88-claude-code-v2-1-88-claude-c/06-RESEARCH.md
@E:/work/ai/agent/docs/superpowers/specs/2026-04-06-claude-code-desktop-alignment-design.md
@E:/work/ai/agent/src/app/state/types.ts
@E:/work/ai/agent/src/app/state/appShellStore.ts
@E:/work/ai/agent/src/app/services/sessionService.ts
@E:/work/ai/agent/src/app/services/assistantService.ts

<interfaces>
From src/app/state/types.ts:
- Current top-level state primitives include `AppMode`, `ShellView`, `SessionStatus`, `ExecutionStatus`, `SessionRecord`, `SessionDetail`, and `SessionRecoverySnapshot`.
- Current transcript kinds already include `user-message`, `assistant-message`, `stage-status`, `tool-summary`, `approval-request`, `approval-resolution`, `execution-update`, and `review-available`.

Create/normalize these Phase 6 contracts in this plan:
- `DesktopWorkflowStatus = 'Ready' | 'Connecting' | 'Connected' | 'Attached' | 'Working' | 'Awaiting approval' | 'Review ready' | 'Failed' | 'Needs attention'`
- chooser row metadata with spotlight/recovery/action affordances
- tray mode derived from lifecycle, not manual tab state alone
- explicit startup/recovery/chooser/session-surface view model consumed by all layout regions
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Define canonical desktop workflow interfaces and derived view-model types</name>
  <read_first>
    - E:/work/ai/agent/.planning/phases/06-claude-code-source-code-v2-1-88-claude-code-v2-1-88-claude-c/06-CONTEXT.md
    - E:/work/ai/agent/.planning/phases/06-claude-code-source-code-v2-1-88-claude-code-v2-1-88-claude-c/06-UI-SPEC.md
    - E:/work/ai/agent/src/app/state/types.ts
  </read_first>
  <files>src/app/state/types.ts</files>
  <behavior>
    - Test 1: canonical workflow states exactly match the UI-SPEC labels `Ready`, `Connecting`, `Connected`, `Attached`, `Working`, `Awaiting approval`, `Review ready`, `Failed`, and `Needs attention` per D-12.
    - Test 2: chooser/session contracts can represent recovery available, no workspace selected, active attached session, pending approval, and review-ready attention without falling back to ad hoc booleans.
    - Test 3: tray mode and row attention metadata are typed centrally so later layout plans do not introduce local string unions.
  </behavior>
  <action>Expand `src/app/state/types.ts` to introduce Phase 6 UI-facing contracts per D-07, D-08, D-10, D-11, and D-12: add canonical `DesktopWorkflowStatus`, `DesktopStartupState`, `DesktopTrayMode`, chooser spotlight/row metadata, session attention flags, session surface header model, and any normalized status mapping helper types required by the store. Preserve existing transcript/execution payloads; do not remove project-mode or conversation-mode support from the type surface. Rename or replace MVP-only wording such as generic idle/active display unions only when the new contract provides a direct canonical equivalent. Keep all exported names stable and explicit so `TopToolbar`, `LeftSidebar`, `CenterWorkspace`, `RightPanel`, and `BottomPanel` can import them directly in later plans.</action>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <done>`src/app/state/types.ts` exports one canonical Phase 6 vocabulary and typed chooser/session/tray contracts that match the exact UI-SPEC wording and can drive every shell region without extra local unions.</done>
  <acceptance_criteria>
    - `DesktopWorkflowStatus` contains the exact nine canonical labels from `06-UI-SPEC.md`.
    - New exported types cover startup, recovery, chooser rows, session header, attention markers, and tray mode.
    - No deferred idea appears in the type contract: no cloud sync, multi-user state, plugin marketplace, or upstream-runtime embedding fields.
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Refactor the Zustand store into the canonical Phase 6 workflow state machine</name>
  <read_first>
    - E:/work/ai/agent/src/app/state/types.ts
    - E:/work/ai/agent/src/app/state/appShellStore.ts
    - E:/work/ai/agent/.planning/phases/06-claude-code-source-code-v2-1-88-claude-code-v2-1-88-claude-c/06-UI-SPEC.md
    - E:/work/ai/agent/.planning/phases/06-claude-code-source-code-v2-1-88-claude-code-v2-1-88-claude-c/06-RESEARCH.md
  </read_first>
  <files>src/app/state/appShellStore.ts</files>
  <behavior>
    - Test 1: startup/recovery with no snapshot derives `Ready` or chooser state instead of generic idle wording.
    - Test 2: restored local session derives `Attached`, assistant streaming or approved execution derives `Working`, pending proposal derives `Awaiting approval`, changed files derive `Review ready`, and failed assistant/execution derives `Failed` or `Needs attention` per D-10/D-12.
    - Test 3: bottom tray auto-mode derives from lifecycle events instead of relying only on manual tab state.
  </behavior>
  <action>Refactor `src/app/state/appShellStore.ts` so it becomes the canonical workflow source of truth described in the alignment design and research. Add derived selectors or computed state for: startup state, recovery availability/failure, chooser readiness, active workspace summary, spotlight session, chooser rows, active session header, canonical desktop status, tray mode, and session attention markers. Update transition logic in `attemptRecovery`, `createProjectSession`, `createConversationSession`, `resumeSession`, `submitPrompt`, `approvePendingCommand`, and `rejectPendingCommand` so their persisted recent-activity summaries and runtime state map cleanly to the canonical labels. Preserve local-first behavior and existing approval safety; do not add cloud/session-sharing logic. If service return types are currently too thin, extend the frontend service adapters only enough to satisfy the new typed store contract.</action>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <done>The store can express every Phase 6 lifecycle state explicitly and exposes selectors/view-model data that later UI plans can consume without inventing panel-specific labels or tray heuristics.</done>
  <acceptance_criteria>
    - `appShellStore.ts` contains one canonical status derivation path used for toolbar, chooser, session header, sidebar markers, and tray behavior.
    - Recovery, chooser, attach/resume, approval, execution, review, and failure states are represented explicitly.
    - Tray mode can auto-resolve to approval, output, review, or collapsed from runtime state.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Align frontend service contracts with the normalized store lifecycle</name>
  <read_first>
    - E:/work/ai/agent/src/app/services/sessionService.ts
    - E:/work/ai/agent/src/app/services/assistantService.ts
    - E:/work/ai/agent/src/app/state/appShellStore.ts
    - E:/work/ai/agent/.planning/phases/06-claude-code-source-code-v2-1-88-claude-code-v2-1-88-claude-c/06-RESEARCH.md
  </read_first>
  <files>src/app/services/sessionService.ts, src/app/services/assistantService.ts</files>
  <action>Update `src/app/services/sessionService.ts` and `src/app/services/assistantService.ts` only where needed to support the new typed store selectors from Tasks 1-2. Normalize return typing for recovery snapshot data, session list payloads, command proposal payloads, and execution/review callbacks so the store no longer casts or infers unofficial states. Keep these modules as thin invoke wrappers per D-02; do not move UI derivation logic into services and do not expand into native runtime changes in this plan.</action>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <done>Service adapters compile cleanly against the new Phase 6 types and provide the exact data shape the store needs for chooser/recovery/lifecycle derivation without embedding presentation logic.</done>
  <acceptance_criteria>
    - Service files remain thin wrappers with typed payload boundaries.
    - No UI copy strings or component-only logic are added to service modules.
    - `npm run build` passes with the new type/store contract in place.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| UI -> Zustand store | Untrusted user actions and persisted session metadata are mapped into canonical lifecycle state. |
| Store -> Tauri service adapters | Typed frontend requests cross into native command invocation. |
| Recovery snapshot -> visible chooser state | Previously persisted local data is surfaced as authoritative resume/attach context. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-01 | T | src/app/state/appShellStore.ts | mitigate | Keep lifecycle derivation centralized and deterministic so approval/review/failure state cannot be hidden by panel-local wording. |
| T-06-02 | I | src/app/services/sessionService.ts | mitigate | Keep recovery/chooser payload typing explicit; do not leak credential values or unrelated native data into the store. |
| T-06-03 | R | src/app/state/types.ts | mitigate | Add typed attention/review/approval contracts so review-ready and approval states remain attributable to session/execution records. |
| T-06-04 | E | src/app/services/assistantService.ts | accept | No new privilege boundary is introduced in this plan; service wrappers remain thin over existing approval-gated execution flow. |
</threat_model>

<verification>
Run `npm run build` after each task. Confirm the store exposes canonical selectors for status, chooser spotlight, session attention, tray mode, and startup state before moving to Plan 02.
</verification>

<success_criteria>
- Canonical workflow status exists once in the shared state contract.
- Store transitions cover startup, recovery, chooser, attached session, approval, execution, review, and failure.
- Later plans can redesign the shell without re-inventing lifecycle semantics.
</success_criteria>

<output>
After completion, create `.planning/phases/06-claude-code-source-code-v2-1-88-claude-code-v2-1-88-claude-c/06-01-SUMMARY.md`
</output>
