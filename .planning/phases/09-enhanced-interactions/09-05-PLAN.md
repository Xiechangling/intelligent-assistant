---
phase: 09-enhanced-interactions
plan: 05
type: execute
wave: 4
depends_on: [09-01, 09-02]
files_modified:
  - src/app/layout/CenterWorkspace.tsx
  - src/app/components/Composer/Composer.tsx
  - src/app/components/Composer/Composer.module.css
  - src/app/components/Composer/index.ts
autonomous: true
requirements: [INTERACT-08, CONTENT-03]
must_haves:
  truths:
    - "输入框右侧显示附件按钮、模型选择器、语音输入按钮"
    - "输入框集成 VoiceInput 组件（Phase 7 已创建）"
    - "输入框集成 AttachmentList 组件（Phase 7 已创建）"
    - "发送按钮在输入框内右下角"
  artifacts:
    - path: "src/app/components/Composer/Composer.tsx"
      provides: "增强的输入框组件"
      min_lines: 100
  key_links:
    - from: "Composer"
      to: "VoiceInput"
      via: "onTranscript callback"
      pattern: "VoiceInput.*onTranscript"
    - from: "Composer"
      to: "AttachmentList"
      via: "attachments state"
      pattern: "AttachmentList"
---

<objective>
增强输入框，集成语音输入、附件管理和模型选择器。

Purpose: 提供完整的消息输入体验，支持多种输入方式
Output: 重构的 Composer 组件，集成 Phase 7 创建的组件
</objective>

<execution_context>
@C:\Users\chris\.claude\get-shit-done\workflows\execute-plan.md
@C:\Users\chris\.claude\get-shit-done\templates\summary.md
</execution_context>

<context>
@E:\work\ai\agent\.planning\PROJECT.md
@E:\work\ai\agent\.planning\ROADMAP.md
@E:\work\ai\agent\.planning\STATE.md
@E:\work\ai\agent\.planning\phases\09-enhanced-interactions\09-RESEARCH.md
@E:\work\ai\agent\src\app\layout\CenterWorkspace.tsx
@E:\work\ai\agent\src\app\components\VoiceInput\VoiceInput.tsx
@E:\work\ai\agent\src\app\components\AttachmentList\AttachmentList.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: 提取 Composer 为独立组件</name>
  <files>
    src/app/components/Composer/Composer.tsx,
    src/app/components/Composer/Composer.module.css,
    src/app/components/Composer/index.ts
  </files>
  <action>
    从 CenterWorkspace.tsx 提取 Composer 为独立组件：
    
    **Composer.tsx**:
    - 复制 CenterWorkspace 中的 Composer 函数组件
    - 保持现有 props 接口：
      ```typescript
      interface ComposerProps {
        mode: 'project' | 'conversation'
        draftPrompt: string
        pendingAttachments: SessionAttachment[]
        setDraftPrompt: (prompt: string) => void
        addFileAttachments: () => Promise<void>
        addImageAttachments: () => Promise<void>
        removePendingAttachment: (attachmentId: string) => void
        submitPrompt: () => Promise<void>
        disabled: boolean
      }
      ```
    - 保持现有功能：
      - CommandHint 组件
      - PendingAttachments 组件
      - useInputHistory hook 集成
      - 表单提交和快捷键处理
    - 移动相关子组件（CommandHint, PendingAttachments）到同一文件或单独文件
    
    **Composer.module.css**:
    - 复制 CenterWorkspace 中的 composer 相关样式
    - 确保样式完整且独立
    
    **index.ts**: 导出 Composer 组件
    
    **注意**:
    - 这是重构步骤，不改变功能
    - 确保所有依赖正确导入
    - 保持现有的 CSS class 名称
  </action>
  <verify>
    <automated>npm run build 2>&1 | grep -E "(error|warning)" || echo "Build passed"</automated>
  </verify>
  <done>Composer 组件提取完成，功能与原 CenterWorkspace 中一致</done>
</task>

<task type="auto">
  <name>Task 2: 增强 Composer 集成 VoiceInput 和模型选择器</name>
  <files>src/app/components/Composer/Composer.tsx, src/app/components/Composer/Composer.module.css</files>
  <action>
    增强 Composer 组件，添加右侧工具栏：
    
    **Composer.tsx 修改**:
    1. 导入 VoiceInput 组件
    2. 从 appShellStore 获取：
       - globalDefaultModel, activeSessionModelOverride
       - setActiveSessionModelOverride
    3. 添加语音输入处理：
       ```typescript
       const handleVoiceTranscript = (text: string) => {
         setDraftPrompt(prev => prev ? `${prev}\n${text}` : text)
       }
       ```
    4. 在 textarea 右侧添加工具栏：
       ```tsx
       <div className={styles.inputTools}>
         <select
           value={activeSessionModelOverride || globalDefaultModel}
           onChange={(e) => setActiveSessionModelOverride(e.target.value as ModelId)}
           className={styles.modelSelector}
           disabled={disabled}
         >
           <option value="claude-sonnet">Claude Sonnet</option>
           <option value="claude-opus">Claude Opus</option>
           <option value="claude-haiku">Claude Haiku</option>
         </select>
         <VoiceInput onTranscript={handleVoiceTranscript} />
       </div>
       ```
    5. 调整布局：
       - textarea 和工具栏在同一行
       - 工具栏固定在右侧
       - 发送按钮在 textarea 右下角（保持现有位置）
    
    **Composer.module.css 修改**:
    - 添加 .inputTools 样式：flex 布局，垂直居中
    - 添加 .modelSelector 样式：下拉框样式，与主题一致
    - 调整 .field 布局：支持 textarea + tools 并排
    - 确保响应式：移动端工具栏换行或隐藏部分选项
    
    **注意**:
    - VoiceInput 已在 Phase 7 创建，直接导入使用
    - 模型选择器选项根据实际支持的模型调整
    - 保持现有的附件按钮和工具栏（File/Image 按钮）
  </action>
  <verify>
    <automated>npm run build 2>&1 | grep -E "(error|warning)" || echo "Build passed"</automated>
  </verify>
  <done>Composer 集成 VoiceInput 和模型选择器，布局合理</done>
</task>

<task type="auto">
  <name>Task 3: 更新 CenterWorkspace 使用新 Composer</name>
  <files>src/app/layout/CenterWorkspace.tsx</files>
  <action>
    更新 CenterWorkspace 使用独立的 Composer 组件：
    
    **修改点**:
    1. 导入新的 Composer 组件：`import { Composer } from '../components/Composer'`
    2. 删除原 Composer 函数组件定义
    3. 删除 CommandHint 和 PendingAttachments 子组件定义（已移到 Composer 中）
    4. 保持 SessionSurface 中的 Composer 调用不变（props 接口相同）
    5. 验证所有 Composer 使用处（project 和 conversation 模式）
    
    **注意**:
    - 这是简单的导入替换，不改变功能
    - 确保所有 props 正确传递
    - 删除未使用的导入
  </action>
  <verify>
    <automated>npm run build 2>&1 | grep -E "(error|warning)" || echo "Build passed"</automated>
  </verify>
  <done>CenterWorkspace 使用新 Composer 组件，代码更简洁</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 语音输入 → draftPrompt | 语音转文本结果追加到输入框 |
| 模型选择 → appShellStore | 用户选择的模型存储到状态 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-11 | Tampering | VoiceInput transcript | accept | 语音输入仅本地处理，无注入风险 |
| T-09-12 | Information Disclosure | 模型选择器 | accept | 模型选择仅影响 API 调用，无敏感信息泄露 |
</threat_model>

<verification>
1. 运行 `npm run build` 验证 TypeScript 编译通过
2. 检查 Composer 组件独立且功能完整
3. 验证 VoiceInput 集成和语音转文本功能
4. 测试模型选择器切换
5. 确认布局合理，工具栏在右侧
</verification>

<success_criteria>
- Composer 组件独立，代码结构清晰
- VoiceInput 集成完成，语音转文本追加到输入框
- 模型选择器显示在输入框右侧，支持切换
- CenterWorkspace 使用新 Composer，功能不变
- TypeScript 编译通过，无错误或警告
- 所有样式使用 CSS Modules
</success_criteria>

<output>
完成后创建 `.planning/phases/09-enhanced-interactions/09-05-SUMMARY.md`
</output>
