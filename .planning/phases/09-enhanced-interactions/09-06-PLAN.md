---
phase: 09-enhanced-interactions
plan: 06
type: execute
wave: 5
depends_on: [09-03]
files_modified:
  - src/app/layout/TopToolbar.tsx
  - src/app/components/ModeTabs/ModeTabs.tsx
  - src/app/components/ModeTabs/ModeTabs.module.css
  - src/app/hooks/useKeyboardShortcuts.ts
  - src/app/layout/AppShell.tsx
autonomous: true
requirements: [INTERACT-01, INTERACT-02, INTERACT-05]
must_haves:
  truths:
    - "用户点击 Chat/Cowork/Code 标签切换模式"
    - "用户按 Ctrl+1/2/3 快捷键切换模式"
    - "用户点击 New session 按钮创建新会话"
    - "用户按 Ctrl+N 快捷键创建新会话"
    - "前进后退按钮支持 Alt+Left/Right 快捷键"
  artifacts:
    - path: "src/app/components/ModeTabs/ModeTabs.tsx"
      provides: "三模式标签组件"
      min_lines: 40
  key_links:
    - from: "ModeTabs"
      to: "appShellStore.setCurrentMode"
      via: "mode switching"
      pattern: "setCurrentMode"
    - from: "useKeyboardShortcuts"
      to: "mode switching"
      via: "Ctrl+1/2/3 shortcuts"
      pattern: "ctrl\\+[123]"
---

<objective>
实现三模式切换、新建会话和导航快捷键。

Purpose: 完善顶栏交互功能，支持模式切换和快捷键操作
Output: ModeTabs 组件和完整的快捷键系统
</objective>

<execution_context>
@C:\Users\chris\.claude\get-shit-done\workflows\execute-plan.md
@C:\Users\chris\.claude\get-shit-done\templates\summary.md
</execution_context>

<context>
@E:\work\ai\agent\.planning\PROJECT.md
@E:\work\ai\agent\.planning\ROADMAP.md
@E:\work\ai\agent\.planning\STATE.md
@E:\work\ai\agent\.planning\phases\09-enhanced-interactions\09-RESEARCH.md
@E:\work\ai\agent\src\app\layout\TopToolbar.tsx
@E:\work\ai\agent\src\app\components\ModeTabs\ModeTabs.tsx
@E:\work\ai\agent\src\app\state\appShellStore.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: 增强 ModeTabs 组件</name>
  <files>
    src/app/components/ModeTabs/ModeTabs.tsx,
    src/app/components/ModeTabs/ModeTabs.module.css
  </files>
  <action>
    增强 ModeTabs 组件（Phase 7 已创建基础版本）：
    
    **ModeTabs.tsx 修改**:
    1. 检查现有实现，确认支持以下功能：
       - 从 appShellStore 获取 currentMode 和 setCurrentMode
       - 渲染三个标签：Chat, Cowork, Code
       - 点击标签调用 setCurrentMode('chat' | 'cowork' | 'code')
       - 当前激活标签高亮显示
    2. 如果缺失，添加以上功能
    3. 添加键盘快捷键提示（可选）：
       - Chat 标签显示 "Ctrl+1"
       - Cowork 标签显示 "Ctrl+2"
       - Code 标签显示 "Ctrl+3"
    4. 添加 aria 属性提升可访问性：
       - role="tablist"
       - aria-selected 标记当前标签
    
    **ModeTabs.module.css 修改**:
    - 确保标签样式清晰（激活/非激活状态）
    - 添加悬停效果
    - 添加过渡动画（200ms ease-out，符合 VISUAL-06）
    - 响应式：移动端调整标签宽度
    
    **注意**:
    - Phase 7 可能已创建基础版本，检查后增强
    - 如果 Phase 7 版本完整，仅需验证功能
  </action>
  <verify>
    <automated>npm run build 2>&1 | grep -E "(error|warning)" || echo "Build passed"</automated>
  </verify>
  <done>ModeTabs 组件支持三模式切换，样式和交互完整</done>
</task>

<task type="auto">
  <name>Task 2: 集成 ModeTabs 到 TopToolbar</name>
  <files>src/app/layout/TopToolbar.tsx</files>
  <action>
    更新 TopToolbar 集成 ModeTabs 组件：
    
    **修改点**:
    1. 检查 TopToolbar 是否已包含 ModeTabs（Phase 8 可能已添加）
    2. 如果未包含，导入并添加 ModeTabs：
       ```tsx
       import { ModeTabs } from '../components/ModeTabs'
       
       // 在 WindowControls 和 NavigationButtons 之后添加
       <ModeTabs />
       ```
    3. 调整布局：
       - WindowControls 在最左侧
       - NavigationButtons 在 WindowControls 右侧
       - ModeTabs 在中间（flex-grow 或居中）
       - 右侧预留空间（未来可能添加其他按钮）
    4. 更新 TopToolbar 样式：
       - 使用 flex 布局
       - 确保 48px 高度（符合 LAYOUT-01）
       - 对齐所有子元素
    
    **注意**:
    - Phase 8 可能已部分实现，检查后补充
    - 保持 TopToolbar 简洁，仅包含核心元素
  </action>
  <verify>
    <automated>npm run build 2>&1 | grep -E "(error|warning)" || echo "Build passed"</automated>
  </verify>
  <done>TopToolbar 集成 ModeTabs，布局合理</done>
</task>

<task type="auto">
  <name>Task 3: 完善全局快捷键系统</name>
  <files>
    src/app/hooks/useKeyboardShortcuts.ts,
    src/app/layout/AppShell.tsx
  </files>
  <action>
    完善全局快捷键系统，支持所有 Phase 9 快捷键：
    
    **useKeyboardShortcuts.ts 修改**（如果 Plan 09-03 未完全实现）:
    1. 确保支持以下快捷键格式：
       - 'ctrl+f', 'ctrl+n', 'ctrl+1', 'ctrl+2', 'ctrl+3'
       - 'alt+left', 'alt+right'
       - 'escape'
    2. 实现快捷键解析逻辑：
       ```typescript
       const parseShortcut = (shortcut: string) => {
         const parts = shortcut.toLowerCase().split('+')
         return {
           ctrl: parts.includes('ctrl'),
           alt: parts.includes('alt'),
           meta: parts.includes('meta'),
           key: parts[parts.length - 1]
         }
       }
       ```
    3. 在 keydown 事件处理中匹配快捷键
    4. 检查 event.target 避免输入框冲突
    
    **AppShell.tsx 修改**:
    1. 导入 useKeyboardShortcuts hook
    2. 从 appShellStore 获取所需 actions：
       - setCurrentMode, createProjectSession, createConversationSession
       - goBack, goForward
    3. 注册所有快捷键：
       ```typescript
       useKeyboardShortcuts({
         'ctrl+f': () => setSearchOpen(true),
         'ctrl+n': () => {
           if (mode === 'conversation') {
             createConversationSession()
           } else {
             createProjectSession()
           }
         },
         'ctrl+1': () => setCurrentMode('chat'),
         'ctrl+2': () => setCurrentMode('cowork'),
         'ctrl+3': () => setCurrentMode('code'),
         'alt+left': () => goBack(),
         'alt+right': () => goForward()
       })
       ```
    4. 确保快捷键不与现有 useGlobalKeybindings 冲突
    
    **注意**:
    - Plan 09-03 可能已部分实现，检查后补充
    - 考虑将快捷键配置提取为常量
    - Mac 用户使用 Cmd 键（metaKey），Windows 用户使用 Ctrl 键
  </action>
  <verify>
    <automated>npm run build 2>&1 | grep -E "(error|warning)" || echo "Build passed"</automated>
  </verify>
  <done>全局快捷键系统完善，支持所有 Phase 9 快捷键</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 快捷键输入 → 应用操作 | 用户按键触发应用功能 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-13 | Denial of Service | useKeyboardShortcuts | mitigate | 检查 event.target 避免输入框冲突，使用 cleanup 函数 |
| T-09-14 | Tampering | 模式切换 | accept | 模式切换仅影响 UI 状态，无安全风险 |
</threat_model>

<verification>
1. 运行 `npm run build` 验证 TypeScript 编译通过
2. 检查 ModeTabs 渲染和点击切换功能
3. 验证 Ctrl+1/2/3 快捷键切换模式
4. 测试 Ctrl+N 创建新会话
5. 测试 Alt+Left/Right 前进后退
6. 确认快捷键不在输入框中触发
</verification>

<success_criteria>
- ModeTabs 组件支持三模式切换，样式完整
- TopToolbar 集成 ModeTabs，布局合理
- useKeyboardShortcuts 支持所有 Phase 9 快捷键
- Ctrl+1/2/3 切换模式，Ctrl+N 创建会话，Alt+Left/Right 导航
- 快捷键避免输入框冲突
- TypeScript 编译通过，无错误或警告
</success_criteria>

<output>
完成后创建 `.planning/phases/09-enhanced-interactions/09-06-SUMMARY.md`
</output>
