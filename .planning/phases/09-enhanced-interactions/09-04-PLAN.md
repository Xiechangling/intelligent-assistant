---
phase: 09-enhanced-interactions
plan: 04
type: execute
wave: 3
depends_on: [09-03]
files_modified:
  - src/app/components/CustomizeMenu/CustomizeMenu.tsx
  - src/app/components/CustomizeMenu/CustomizeMenu.module.css
  - src/app/components/CustomizeMenu/index.ts
  - src/app/components/SidebarTopActions/SidebarTopActions.tsx
  - src/app/state/appShellStore.ts
autonomous: true
requirements: [INTERACT-04]
must_haves:
  truths:
    - "用户点击 Customize 按钮打开设置菜单"
    - "菜单支持主题切换（Light/Dark/System）"
    - "菜单包含设置和关于选项"
    - "点击菜单外部或再次点击按钮关闭菜单"
  artifacts:
    - path: "src/app/components/CustomizeMenu/CustomizeMenu.tsx"
      provides: "设置菜单下拉组件"
      min_lines: 60
  key_links:
    - from: "CustomizeMenu"
      to: "appShellStore.setTheme"
      via: "theme state management"
      pattern: "setTheme"
---

<objective>
实现 Customize 菜单，支持主题切换和设置访问。

Purpose: 提供用户自定义界面的入口，集成主题切换功能
Output: CustomizeMenu 组件和主题切换逻辑
</objective>

<execution_context>
@C:\Users\chris\.claude\get-shit-done\workflows\execute-plan.md
@C:\Users\chris\.claude\get-shit-done\templates\summary.md
</execution_context>

<context>
@E:\work\ai\agent\.planning\PROJECT.md
@E:\work\ai\agent\.planning\ROADMAP.md
@E:\work\ai\agent\.planning\STATE.md
@E:\work\ai\agent\.planning\phases\09-enhanced-interactions\09-RESEARCH.md
@E:\work\ai\agent\src\app\state\appShellStore.ts
@E:\work\ai\agent\src\app\components\SidebarTopActions\SidebarTopActions.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: 创建 CustomizeMenu 组件</name>
  <files>
    src/app/components/CustomizeMenu/CustomizeMenu.tsx,
    src/app/components/CustomizeMenu/CustomizeMenu.module.css,
    src/app/components/CustomizeMenu/index.ts
  </files>
  <action>
    创建设置菜单下拉组件：
    
    **CustomizeMenu.tsx**:
    - Props: isOpen (boolean), onClose (() => void), anchorRef (RefObject<HTMLElement>)
    - 从 appShellStore 获取: theme, setTheme
    - 使用 useEffect + useRef 实现点击外部关闭（参考 09-RESEARCH.md Pattern 2）
    - 渲染结构：
      - 下拉菜单容器（绝对定位，相对于 anchorRef）
      - **Theme 部分**:
        - 标题 "Theme"
        - 三个选项按钮：Light, Dark, System
        - 当前选中主题高亮显示
        - 点击切换主题：setTheme('light' | 'dark' | 'auto')
      - **Settings 部分**:
        - "Preferences" 按钮（暂时无操作，Phase 10 实现）
        - "Keyboard Shortcuts" 按钮（暂时无操作）
      - **About 部分**:
        - "About" 按钮（显示版本信息，可选）
    - 菜单定位逻辑：
      - 计算 anchorRef 位置
      - 调整菜单位置避免超出视口（参考 09-RESEARCH.md Pitfall 2）
    
    **CustomizeMenu.module.css**:
    - 下拉菜单：白色背景、圆角、阴影、z-index 高
    - 分组标题：小字体、灰色、上边距
    - 菜单项：悬停高亮、点击效果
    - 主题选项：选中状态显示勾选图标或背景色
    - 响应式：移动端调整宽度
    
    **index.ts**: 导出 CustomizeMenu 组件
    
    参考 09-RESEARCH.md Code Examples 中的 CustomizeMenu 实现。
  </action>
  <verify>
    <automated>npm run build 2>&1 | grep -E "(error|warning)" || echo "Build passed"</automated>
  </verify>
  <done>CustomizeMenu 组件创建完成，支持主题切换和点击外部关闭</done>
</task>

<task type="auto">
  <name>Task 2: 增强 appShellStore 主题管理</name>
  <files>src/app/state/appShellStore.ts</files>
  <action>
    增强主题管理功能（如果尚未完整实现）：
    
    **检查并补充**:
    1. 确认 theme 状态类型为 ThemeMode ('light' | 'dark' | 'auto')
    2. 确认 setTheme action 存在并正确更新状态
    3. 添加主题应用逻辑（如果缺失）：
       - 在 setTheme 中更新 localStorage
       - 在 setTheme 中更新 document.documentElement.dataset.theme
       - 如果 theme 为 'auto'，检测系统主题：
         ```typescript
         const systemTheme = window.matchMedia('(prefers-color-scheme: dark)').matches ? 'dark' : 'light'
         document.documentElement.dataset.theme = systemTheme
         ```
    4. 添加系统主题变化监听器（可选）：
       ```typescript
       window.matchMedia('(prefers-color-scheme: dark)').addEventListener('change', (e) => {
         if (get().theme === 'auto') {
           document.documentElement.dataset.theme = e.matches ? 'dark' : 'light'
         }
       })
       ```
    
    **注意**:
    - 检查现有代码，避免重复实现
    - 主题应用逻辑应在 store 初始化时执行
    - localStorage key 使用 'theme'
  </action>
  <verify>
    <automated>npm run build 2>&1 | grep -E "(error|warning)" || echo "Build passed"</automated>
  </verify>
  <done>appShellStore 主题管理完善，支持 light/dark/auto 切换和持久化</done>
</task>

<task type="auto">
  <name>Task 3: 集成 CustomizeMenu 到 SidebarTopActions</name>
  <files>src/app/components/SidebarTopActions/SidebarTopActions.tsx</files>
  <action>
    更新 SidebarTopActions 集成 CustomizeMenu：
    
    **修改点**:
    1. 添加状态: `const [menuOpen, setMenuOpen] = useState(false)`
    2. 添加 ref: `const customizeButtonRef = useRef<HTMLButtonElement>(null)`
    3. 更新 Customize 按钮：
       - 添加 ref={customizeButtonRef}
       - 更新 onClick: `() => setMenuOpen(!menuOpen)`
       - 移除 stub 注释
    4. 导入并渲染 CustomizeMenu：
       ```tsx
       <CustomizeMenu
         isOpen={menuOpen}
         onClose={() => setMenuOpen(false)}
         anchorRef={customizeButtonRef}
       />
       ```
    5. 确保按钮和菜单的相对定位正确
    
    **注意**:
    - CustomizeMenu 应渲染在按钮下方
    - 使用 Portal 渲染菜单（可选，避免 z-index 问题）
    - 点击按钮切换菜单开关状态
  </action>
  <verify>
    <automated>npm run build 2>&1 | grep -E "(error|warning)" || echo "Build passed"</automated>
  </verify>
  <done>CustomizeMenu 集成完成，Customize 按钮可用，主题切换功能正常</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 主题选择 → localStorage | 用户选择的主题存储到本地 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-09 | Tampering | localStorage theme | accept | 主题设置仅影响 UI，无安全风险 |
| T-09-10 | Denial of Service | CustomizeMenu | mitigate | 使用 useEffect cleanup 移除点击监听器 |
</threat_model>

<verification>
1. 运行 `npm run build` 验证 TypeScript 编译通过
2. 检查 CustomizeMenu 正确定位在按钮下方
3. 验证主题切换功能（Light/Dark/System）
4. 测试点击外部关闭菜单
5. 确认主题持久化到 localStorage
</verification>

<success_criteria>
- CustomizeMenu 组件支持主题切换和点击外部关闭
- appShellStore 主题管理支持 light/dark/auto 和持久化
- Customize 按钮打开/关闭菜单
- 主题切换立即生效并保存到 localStorage
- TypeScript 编译通过，无错误或警告
- 所有新组件使用 CSS Modules
</success_criteria>

<output>
完成后创建 `.planning/phases/09-enhanced-interactions/09-04-SUMMARY.md`
</output>
