---
phase: 09-enhanced-interactions
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - package-lock.json
autonomous: true
requirements: [CONTENT-03, CONTENT-04]
must_haves:
  truths:
    - "消息内容支持 Markdown 渲染（标题、列表、链接等）"
    - "代码块具有语法高亮显示"
    - "时间戳显示为相对时间格式（如 '2m ago'）"
  artifacts:
    - path: "package.json"
      provides: "react-markdown, highlight.js, date-fns, remark-gfm 依赖"
      contains: "react-markdown"
    - path: "src/app/components/MarkdownRenderer/MarkdownRenderer.tsx"
      provides: "Markdown 渲染组件"
      min_lines: 30
    - path: "src/app/components/CodeBlock/CodeBlock.tsx"
      provides: "代码块语法高亮组件"
      min_lines: 25
  key_links:
    - from: "MarkdownRenderer"
      to: "react-markdown"
      via: "ReactMarkdown component"
      pattern: "ReactMarkdown"
    - from: "CodeBlock"
      to: "highlight.js"
      via: "hljs.highlightElement"
      pattern: "hljs\\.highlightElement"
---

<objective>
安装内容渲染依赖并创建 Markdown 和代码高亮组件。

Purpose: 为消息内容提供富文本渲染能力，支持 Markdown 格式和代码语法高亮
Output: 可复用的 MarkdownRenderer 和 CodeBlock 组件
</objective>

<execution_context>
@C:\Users\chris\.claude\get-shit-done\workflows\execute-plan.md
@C:\Users\chris\.claude\get-shit-done\templates\summary.md
</execution_context>

<context>
@E:\work\ai\agent\.planning\PROJECT.md
@E:\work\ai\agent\.planning\ROADMAP.md
@E:\work\ai\agent\.planning\STATE.md
@E:\work\ai\agent\.planning\phases\09-enhanced-interactions\09-RESEARCH.md
@E:\work\ai\agent\.planning\phases\08-core-layout-refactor\08-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: 安装内容渲染依赖</name>
  <files>package.json, package-lock.json</files>
  <action>
    安装以下 npm 包：
    - react-markdown@9.0.1 (Markdown 渲染)
    - highlight.js@11.9.0 (代码语法高亮)
    - date-fns@3.0.6 (日期格式化)
    - remark-gfm@4.0.0 (GitHub Flavored Markdown 支持)
    
    运行命令：npm install react-markdown@9.0.1 highlight.js@11.9.0 date-fns@3.0.6 remark-gfm@4.0.0
    
    验证 package.json 中已添加这些依赖。
  </action>
  <verify>
    <automated>grep -E "(react-markdown|highlight\.js|date-fns|remark-gfm)" package.json</automated>
  </verify>
  <done>package.json 包含所有 4 个新依赖，版本号正确</done>
</task>

<task type="auto">
  <name>Task 2: 创建 CodeBlock 组件</name>
  <files>
    src/app/components/CodeBlock/CodeBlock.tsx,
    src/app/components/CodeBlock/CodeBlock.module.css,
    src/app/components/CodeBlock/index.ts
  </files>
  <action>
    创建代码块组件，使用 highlight.js 进行语法高亮：
    
    **CodeBlock.tsx**:
    - 接收 props: language (string), code (string)
    - 使用 useRef 和 useEffect 调用 hljs.highlightElement
    - 仅在 code 变化时重新高亮（避免性能问题）
    - 渲染 pre > code 结构，className 为 `language-{language}`
    
    **CodeBlock.module.css**:
    - 使用 highlight.js 默认主题样式
    - 添加 border-radius、padding、背景色
    - 支持横向滚动（overflow-x: auto）
    
    **index.ts**: 导出 CodeBlock 组件
    
    参考 09-RESEARCH.md Pattern 3 的实现示例。
  </action>
  <verify>
    <automated>npm run build 2>&1 | grep -E "(error|warning)" || echo "Build passed"</automated>
  </verify>
  <done>CodeBlock 组件创建完成，TypeScript 编译通过，支持语法高亮</done>
</task>

<task type="auto">
  <name>Task 3: 创建 MarkdownRenderer 组件</name>
  <files>
    src/app/components/MarkdownRenderer/MarkdownRenderer.tsx,
    src/app/components/MarkdownRenderer/MarkdownRenderer.module.css,
    src/app/components/MarkdownRenderer/index.ts
  </files>
  <action>
    创建 Markdown 渲染组件，集成 react-markdown 和 CodeBlock：
    
    **MarkdownRenderer.tsx**:
    - 接收 props: content (string)
    - 使用 ReactMarkdown 组件，配置 remarkPlugins: [remarkGfm]
    - 自定义 components.code 渲染器：
      - inline code: 渲染为 <code className={styles.inlineCode}>
      - block code: 使用 CodeBlock 组件，提取 language 从 className
    - 处理边界情况：空内容、无语言标识的代码块
    
    **MarkdownRenderer.module.css**:
    - 样式化 inline code（背景色、padding、border-radius）
    - 样式化 Markdown 元素（h1-h6、p、ul、ol、blockquote）
    - 使用 CSS Variables 适配主题
    
    **index.ts**: 导出 MarkdownRenderer 组件
    
    参考 09-RESEARCH.md Pattern 3 的实现示例。避免 XSS 风险（react-markdown 内置保护）。
  </action>
  <verify>
    <automated>npm run build 2>&1 | grep -E "(error|warning)" || echo "Build passed"</automated>
  </verify>
  <done>MarkdownRenderer 组件创建完成，支持 GFM，代码块使用 CodeBlock 组件</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| User message content → MarkdownRenderer | 用户输入的消息内容可能包含恶意 Markdown/HTML |
| Assistant response → MarkdownRenderer | AI 生成的内容需要安全渲染 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-01 | Tampering | MarkdownRenderer | mitigate | 使用 react-markdown 内置 XSS 保护，不使用 rehype-raw 插件 |
| T-09-02 | Denial of Service | CodeBlock | mitigate | 使用 useEffect 依赖 [code] 避免无限重渲染 |
| T-09-03 | Information Disclosure | highlight.js | accept | 代码高亮仅客户端，无敏感信息泄露风险 |
</threat_model>

<verification>
1. 运行 `npm run build` 验证 TypeScript 编译通过
2. 检查 package.json 包含所有 4 个新依赖
3. 验证 CodeBlock 和 MarkdownRenderer 组件文件存在
4. 确认组件使用 CSS Modules（与 Phase 8 一致）
</verification>

<success_criteria>
- package.json 包含 react-markdown, highlight.js, date-fns, remark-gfm
- CodeBlock 组件支持语法高亮，使用 useRef 避免性能问题
- MarkdownRenderer 组件支持 GFM，集成 CodeBlock
- TypeScript 编译通过，无错误或警告
- 所有组件使用 CSS Modules 统一样式方法论
</success_criteria>

<output>
完成后创建 `.planning/phases/09-enhanced-interactions/09-01-SUMMARY.md`
</output>
