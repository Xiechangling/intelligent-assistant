---
phase: 09-enhanced-interactions
plan: 02
type: execute
wave: 2
depends_on: [09-01]
files_modified:
  - src/app/hooks/useSessionGrouping.ts
  - src/app/components/MessageTimestamp/MessageTimestamp.tsx
  - src/app/components/MessageTimestamp/MessageTimestamp.module.css
  - src/app/components/MessageTimestamp/index.ts
  - src/app/layout/CenterWorkspace.tsx
  - src/app/layout/LeftSidebar.tsx
autonomous: true
requirements: [CONTENT-01, CONTENT-02, INTERACT-07]
must_haves:
  truths:
    - "Sessions 列表按日期分组显示（Today/Yesterday/Last 7 days/Last 30 days/Older）"
    - "消息时间戳显示为相对时间（如 '2 minutes ago'）"
    - "时间戳悬停显示完整日期时间"
  artifacts:
    - path: "src/app/hooks/useSessionGrouping.ts"
      provides: "会话日期分组逻辑"
      min_lines: 40
    - path: "src/app/components/MessageTimestamp/MessageTimestamp.tsx"
      provides: "时间戳显示组件"
      min_lines: 20
  key_links:
    - from: "useSessionGrouping"
      to: "date-fns"
      via: "isToday, isYesterday, isWithinInterval"
      pattern: "isToday|isYesterday"
    - from: "MessageTimestamp"
      to: "date-fns"
      via: "formatDistanceToNow"
      pattern: "formatDistanceToNow"
---

<objective>
实现会话日期分组和时间戳显示功能。

Purpose: 改善会话列表的组织结构，提供更友好的时间显示
Output: useSessionGrouping hook 和 MessageTimestamp 组件
</objective>

<execution_context>
@C:\Users\chris\.claude\get-shit-done\workflows\execute-plan.md
@C:\Users\chris\.claude\get-shit-done\templates\summary.md
</execution_context>

<context>
@E:\work\ai\agent\.planning\PROJECT.md
@E:\work\ai\agent\.planning\ROADMAP.md
@E:\work\ai\agent\.planning\STATE.md
@E:\work\ai\agent\.planning\phases\09-enhanced-interactions\09-RESEARCH.md
@E:\work\ai\agent\src\app\layout\LeftSidebar.tsx
@E:\work\ai\agent\src\app\layout\CenterWorkspace.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: 创建 useSessionGrouping hook</name>
  <files>src/app/hooks/useSessionGrouping.ts</files>
  <action>
    创建会话日期分组 hook，使用 date-fns 进行日期计算：
    
    **useSessionGrouping.ts**:
    - 接收参数: sessions (SessionRecord[])
    - 使用 useMemo 缓存分组结果（依赖 sessions）
    - 导入 date-fns: isToday, isYesterday, isWithinInterval, subDays
    - 返回分组对象：
      ```typescript
      {
        today: SessionRecord[],
        yesterday: SessionRecord[],
        last7Days: SessionRecord[],
        last30Days: SessionRecord[],
        older: SessionRecord[]
      }
      ```
    - 遍历 sessions，根据 session.updatedAt 或 session.lastActivityAt 分组
    - 按时间倒序排列每个分组内的会话
    
    参考 09-RESEARCH.md Pattern 4 的实现示例。使用 useMemo 避免重复计算（Pitfall 4）。
  </action>
  <verify>
    <automated>npm run build 2>&1 | grep -E "(error|warning)" || echo "Build passed"</automated>
  </verify>
  <done>useSessionGrouping hook 创建完成，支持 5 个日期分组，使用 useMemo 优化性能</done>
</task>

<task type="auto">
  <name>Task 2: 创建 MessageTimestamp 组件</name>
  <files>
    src/app/components/MessageTimestamp/MessageTimestamp.tsx,
    src/app/components/MessageTimestamp/MessageTimestamp.module.css,
    src/app/components/MessageTimestamp/index.ts
  </files>
  <action>
    创建时间戳显示组件，使用 date-fns 格式化相对时间：
    
    **MessageTimestamp.tsx**:
    - 接收 props: timestamp (string | Date)
    - 导入 date-fns: formatDistanceToNow
    - 转换 timestamp 为 Date 对象
    - 使用 formatDistanceToNow(date, { addSuffix: true }) 生成相对时间
    - 渲染 <time> 元素：
      - dateTime 属性：ISO 格式（date.toISOString()）
      - title 属性：完整日期时间（date.toLocaleString()）
      - 显示文本：相对时间（如 "2 minutes ago"）
    
    **MessageTimestamp.module.css**:
    - 样式化时间戳（字体大小、颜色、opacity）
    - 悬停效果（显示 title tooltip）
    
    **index.ts**: 导出 MessageTimestamp 组件
    
    参考 09-RESEARCH.md Code Examples 中的 MessageTimestamp 实现。
  </action>
  <verify>
    <automated>npm run build 2>&1 | grep -E "(error|warning)" || echo "Build passed"</automated>
  </verify>
  <done>MessageTimestamp 组件创建完成，支持相对时间显示和完整时间 tooltip</done>
</task>

<task type="auto">
  <name>Task 3: 集成日期分组到 LeftSidebar</name>
  <files>src/app/layout/LeftSidebar.tsx</files>
  <action>
    更新 LeftSidebar 使用 useSessionGrouping hook：
    
    **修改点**:
    1. 导入 useSessionGrouping hook
    2. 在组件内调用：`const sessionGroups = useSessionGrouping(sessionHistory)`
    3. 更新 "Recent sessions" 部分的渲染逻辑：
       - 遍历分组对象（today, yesterday, last7Days, last30Days, older）
       - 为每个非空分组添加日期标题（如 "Today", "Yesterday", "Last 7 Days"）
       - 在标题下渲染该分组的会话列表
       - 保持现有的会话项渲染逻辑（按钮、图标、状态等）
    4. 如果所有分组都为空，显示 "No recent sessions yet"
    
    **注意**:
    - 保持现有的 formatRelativeTime 函数（用于单个会话的时间显示）
    - 不改变会话项的点击行为和样式
    - 日期标题使用语义化 HTML（如 <h3> 或 <div role="heading">）
  </action>
  <verify>
    <automated>npm run build 2>&1 | grep -E "(error|warning)" || echo "Build passed"</automated>
  </verify>
  <done>LeftSidebar 集成日期分组，会话按 Today/Yesterday/Last 7 days 等分组显示</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Session timestamp → date-fns | 时间戳数据来自本地存储，可信 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-04 | Denial of Service | useSessionGrouping | mitigate | 使用 useMemo 缓存分组结果，避免每次渲染重新计算 |
| T-09-05 | Information Disclosure | MessageTimestamp | accept | 时间戳仅显示相对时间，无敏感信息泄露 |
</threat_model>

<verification>
1. 运行 `npm run build` 验证 TypeScript 编译通过
2. 检查 useSessionGrouping 返回 5 个分组
3. 验证 MessageTimestamp 使用 <time> 元素和正确的属性
4. 确认 LeftSidebar 渲染日期分组标题
</verification>

<success_criteria>
- useSessionGrouping hook 支持 5 个日期分组，使用 useMemo 优化
- MessageTimestamp 组件显示相对时间，悬停显示完整时间
- LeftSidebar 按日期分组显示会话列表
- TypeScript 编译通过，无错误或警告
- 所有新组件使用 CSS Modules
</success_criteria>

<output>
完成后创建 `.planning/phases/09-enhanced-interactions/09-02-SUMMARY.md`
</output>
